---
name: loom-{mode}
description: {trigger}
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/{mode}.md` and follow it exactly. Begin with the mode's first command.{target_line}
