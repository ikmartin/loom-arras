---
name: loom-brainstorm
description: when the user wants to explore, brainstorm, or plan a topic before proving anything
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/brainstorm.md` and follow it exactly. Begin with the mode's first command.
