---
name: loom-review
description: a referee reading to improve the source: citations, hypotheses, errors, wording, each finding graded by severity; the author reaches for this most
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/review.md` and follow it exactly. Begin with the mode's first command. The target is `$ARGUMENTS`.
