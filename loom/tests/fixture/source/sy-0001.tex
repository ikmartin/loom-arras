\begin{definition}[Widget]\label{sy-0001}
A \emph{widget} is a pair of a set and an involution of it, so that \eqref{eq:prose} holds. Its fixed locus is
\begin{equation}\label{eq:fix}
\Fix(\sigma) = \{x \in X : \sigma x = x\}.
\end{equation}
\end{definition}