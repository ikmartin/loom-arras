\section{Appendix: an incomplete argument}\label{sy-0300}

This file is nested, so its section renders one level down.

\begin{lemma}[Incomplete]\label{sy-000C}
Every gadget has even order.
\end{lemma}
\begin{proof}
\uses{sy-0008}
Orbits have size two. \incomplete{Show that there are no fixed points using Definition~\ref{sy-0008}.}
\end{proof}

\begin{lemma}[No proof yet]\label{sy-000D}
Every widget map preserves orbit sizes.
\end{lemma}

\begin{remark}
An unlabelled remark: gadgets never have odd order.
\end{remark}

\begin{definition}[Half-gadget]\label{sy-000E}
A widget with exactly one fixed point.
\end{definition}
\begin{proof}
Definitions need no proof; this one has a stray proof anyway.
\end{proof}

% !LOOM priority: high

\begin{proof}[Proof of Lemma~\ref{sy-000D} and Lemma~\ref{sy-0007}]
A proof naming two results attaches to the first.
\end{proof}
