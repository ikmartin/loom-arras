---
name: loom-quick
description: when the user wants a brief answer about a key or the quilt
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/quick.md` and follow it exactly. Begin with the mode's first command.
