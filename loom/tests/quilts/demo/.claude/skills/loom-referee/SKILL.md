---
name: loom-referee
description: when the user asks to referee, review, or find gaps or errors in a node of this quilt
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/referee.md` and follow it exactly. Begin with the mode's first command. The target is `$ARGUMENTS`.
