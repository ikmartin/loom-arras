---
name: loom-draft
description: when the user asks to write a node from a plan they supply
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/draft.md` and follow it exactly. Begin with the mode's first command. The target is `$ARGUMENTS`.
