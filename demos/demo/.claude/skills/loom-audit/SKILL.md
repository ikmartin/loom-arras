---
name: loom-audit
description: when the user asks to audit a key of this quilt: its hypotheses, citations, uses, or self-containedness
---

Run `loom ai orient --run $LOOM_RUN` if you have not done so this session; if `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Read `ai/modes/audit.md` and follow it exactly. Begin with the mode's first command. The target is `$ARGUMENTS`.
