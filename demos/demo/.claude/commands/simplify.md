---
description: Apply the loom simplify mode to a key of this quilt
argument-hint: KEY
---

Apply `ai/modes/simplify.md` to `$ARGUMENTS` in run `$LOOM_RUN`. If `$LOOM_RUN` is unset, ask for the run directory (`loom ai start` creates one). Follow the mode file exactly; its checklist is the output contract.
