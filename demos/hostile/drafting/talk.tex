\documentclass{beamer}
\usepackage{loom}
\begin{document}
% a second master that claims the same nodes, so every id is defined twice over
\input{sections/injection}
\input{sections/scale}
\end{document}
