# M7. Acceptance

## Book says

MVP done: the relative localization paper through every criterion of 2.3, the external user's paper through criterion 10, the Overleaf manual test on the demo quilt, READMEs, CONTRIBUTING files, the release checklist.

## Commands run

The relloc migration of 13.6 was carried out on `demos/relloc/` (a copy; nothing under `~/notes/` was read), every compile through `demos/hermetic.sh`. Steps 1 to 3 had been done at M4 and M5; the rest followed here.

```
# step 1 triage: one dangling \ref repointed at the equation it meant; two proofs after \red{} notes attached with [Proof of Lemma~\ref{rl-000F}]
demos/hermetic.sh loom lint --quilt demos/relloc                                   # 0 errors
# step 2
demos/hermetic.sh loom atomize drafts/draft3.tex drafts/draft4.tex --sections --quilt demos/relloc ; rm demos/relloc/drafts/draft3.tex ; main = "drafts/draft4.tex"
demos/hermetic.sh loom inline drafts/draft4.tex drafts/draft4-flat.tex --all --quilt demos/relloc ; demos/hermetic.sh loom check --quilt demos/relloc
# step 3
loom digest fetch romagny_AlgebraicitySmoothnessFixed2022 --pdf ; loom digest fetch aranha-khan-latyntsev-etal_VirtualLocalizationRevisited2025 --pdf     # [refs] fetch = true in the copy
demos/hermetic.sh loom digest extract aranha-khan-latyntsev-etal_VirtualLocalizationRevisited2025 refs/src/.../virloc.tex --quilt demos/relloc
loom ai init --skills --permissions ; loom ai start ingest-romagny --no-launch ; # ingest-romagny_....tex written by me as the agent from the fetched source ; loom ai promote
# step 4: five referee runs, one per section, a record on every key, acceptances
loom ai start referee-<section> ; loom bundle <section> --run ... ; loom comment --batch --run ... ; loom accept <every statement> --proofs --yes --author ikmartin
# step 5
# nodes/rl-000G.tex edited ; loom status --stale ; loom status --explain rl-000G/proof ; loom accept --stale --yes --author ikmartin
# step 6
loom serve --quilt demos/relloc   # every page kind visited in a headless browser (scratchpad/pagecheck.mjs)
# READMEs, CONTRIBUTING, docs/RELEASE.md, docs/cli-reference.md ; the fresh-clone test (scratchpad/fresh-clone.sh)
```

## Output

Criterion 1 (init and identity): met at M4; identity passes on `draft3.tex` with `--fix-anchoring`. Criterion 2 (every environment and section carries an id, existing labels survive as aliases): 41 environments and 11 headings labelled; `\label{lem:...}` aliases resolve. Criterion 3 (atomize and inline pass the identity test): `atomize --sections` moved 52 nodes and 3 deferred proofs into `nodes/`, wrote a 104-line spine for the 1030-line master, identity pass; `inline --all` rebuilt a 1087-line master, identity pass; `loom check` on `drafts/draft4.tex` is `ok`.

Criterion 4 (every key reported, no dangling references, every proof attached): after the triage `loom lint` reports `0 errors, 5 warnings, 36 infos`; the warnings are three statements with no proof (`rl-000M`, `rl-000P`, `rl-001G`), the digest's `MnSymbol` requirement, and an unknown `\theoremstyle`.

Criterion 5 (every key reviewed and accepted or explicitly incomplete): five referee runs (`ai/runs/2026-09-16T05-38-referee-rl-...`), one per section, left a record on every statement and proof through `loom comment --batch --run` (clean reads as `ok`, three `question`s on the unproved statements, one `objection` on the proof the author's own note calls unfinished, which was first marked `\incomplete`), with a notes file and a thread each; then `loom accept` of every statement with `--proofs`:

```
snapshots: 92 written, 66 already present
0 stale of 57 accepted; 198 draft; 2 incomplete; 199 loose; 37 proved, 233 settled
```

(the 198 drafts and 199 loose are the three digests' 190 external nodes and section nodes).

Criterion 6 (an upstream edit makes acceptances stale, explained with a diff, re-accepted): a sentence added to Lemma `rl-000G`:

```
rl-000G (Lemma)        accepted, stale    own-text-changed (2026-09-16)
rl-000G/proof (Proof)  accepted, stale    dependency-changed rl-000G (2026-09-16)
rl-0018/proof (Proof)  accepted, stale    dependency-changed rl-000G (2026-09-16)
3 stale of 57 accepted
```

`loom status --explain rl-000G/proof` printed the cause and the unified diff of the lemma; `loom accept --stale --yes` restored `0 stale of 57 accepted`.

Criterion 7 (digests for the principal references, one extracted from source, one by ingest, a postnote resolving): three digests. Manolache extracted at M5 (96 results). The virtual-localization paper fetched from arXiv and extracted: 93 results, 27 sections, 41 `\uses`, 72 macros expanded through the paper's `\nc`/`\renc` aliases (DR-73), three list environments carried in the macro block (DR-74); every one of its eleven postnotes resolves. Romagny fetched and ingested by me as the agent from its source in a run, promoted with `loom ai promote`: seven results and a setup node, with the earlier version's numbers as alias ids (DR-75), so all six Romagny postnotes resolve. Bundles of `rl-0016/proof`, `rl-000D/proof`, `rl-000X/proof`, `rl-001F`, and `rl-0015` compile with the cited results inside them, after the author-side steps the book prescribes: `\PassOptionsToPackage{inline}{enumitem}` and `\usepackage{xparse}` in the preamble, and `MnSymbol` struck from the digest's `requires:` since it clashes with `amssymb`.

Criterion 8 (arras served by `loom serve` shows every page and re-renders on save): a headless browser visited every page kind on the served quilt: home, three node pages (45 and 24 typeset formulas), a digest section node, a digest node, the master view of `draft4.tex` (1258 formulas), two digest views (828 and 753 formulas), review, problems, blockers, graph, threads, one thread, tags, taxa, references, loose. Every page rendered without a console error; the manifest carried 302 nodes, 257 keys, 236 edges, 6 threads, and 309 search entries. Re-rendering on save was demonstrated at M2. The first pass had found four faults, all fixed: node pages for ids with dots were 404 (DR-78); the master view tried to load a MathJax extension from the network (arras now bundles the full component); digest pages crashed on duplicate citer keys; and 33 fallback blocks failed to compile (DR-79, DR-80). None fails now.

Criterion 9 (Overleaf) and criterion 10 (an external user) are blocked on the user; see below. Step 7 of 13.6 likewise.

READMEs: loom's README carries the dependency table per platform, the two install paths (`pipx install git+...`, `uv sync`), a quickstart from `loom doctor` to `loom serve`, the paper workflow, the repository layout, and links to the generated `docs/cli-reference.md` (35 commands) and `docs/RELEASE.md`, the release checklist that stops before `uv publish`. arras's README says end users need neither the repository nor Node; both repositories have a `CONTRIBUTING.md`. `scripts/gen_cli_reference.py --check` and the test `test_cli_reference_matches_checked_in` keep the reference equal to the command tree.

The fresh-clone test: in a temporary directory, both repositories cloned from GitHub; `pipx install git+https://github.com/ikmartin/loom` into a throwaway pipx home gave `loom 0.1.0.dev0`; `loom doctor` reported every tool and the vendored bundle (`arras d1fb3aa interface 1`) and exited 0; `loom init demo --demo`, `loom check` (`ok`), and `loom serve` (manifest and a node route both 200) worked from the README alone. The `uv sync` path ran the unit tier: 180 tests, one flaky failure of the outside-writes check under load, since hardened (the test now moves a file's mtime a minute past the run's start). In arras, `npm ci && npm run build` and the unit tests passed. A fresh arras build differs from the vendored bundle only in two chunk file names: Vite's chunk hashes are not reproducible across installs, so the `VERSION` file's commit, not byte equality, is what ties a bundle to its source.

## Tests

| repo | tier | count | command |
|---|---|---|---|
| loom | unit (shim) | 183 | `uv run pytest tests/unit` |
| loom | tex | 10 | `uv run pytest -m tex` |
| loom | paper | 4 | `LOOM_PAPER_FIXTURES=… uv run pytest tests/papers` |
| arras | unit | 10 | `npm run test:unit -- --run` |
| arras | e2e | 27 | `npm run test:e2e` |

## Deviations recorded

- DR-73 to DR-77 (definition aliases, environments in the macro block and presentation packages out of `requires:`, alias ids in postnotes, digest files exempt from the loose-reference check, fetch respecting `eprinttype` with retries).
- DR-78 to DR-80 (every non-asset path is a viewer route, how a fallback document is built, the macro block is loaded and never shown).
- Discovery: Romagny's paper declares its results with `\newenvironment` wrappers around a `counter` environment, not `\newtheorem`, so the extractor finds nothing in it; ingest is the route for such papers, and the extractor's report says so by extracting zero results.
- Discovery: the paper's citations of Romagny follow an earlier arXiv version's numbering; the bibliography entry has no version, so `loom:version-mismatch` cannot fire, and the digest carries both numberings as id and alias.
- Discovery: `loom check` names a failed bundle under `loom:bundle-failed`; `LOOM_SVG_KEEP=DIR` keeps failing fallback documents; the shim fails selectively with `FAKE_TEX_FAIL_MATCH`.
- Discovery: Vite chunk hashes are not reproducible across installs; the vendored bundle is identified by the arras commit in `VERSION`.

## Blocked on the user

- Criterion 9, Overleaf: `loom init demo --demo`, zip without `build/`, `refs/pdf/`, and `refs/src/`, upload to a new Overleaf project, set `drafts/main.tex` as the main document, compile, and compare `pdftotext` with the local PDF (book 14.5); record Overleaf's TeX Live version in the release notes.
- Criterion 10 and step 7 of 13.6: a second person runs `loom init --from` on their own paper following `README.md` and reaches `loom status`; anything they needed to ask is a README bug.
- The Codex half of M6, and publishing (`docs/RELEASE.md` step 9).

## Status

Demonstrated 2026-09-16 for criteria 1 to 8; criteria 9 and 10 are blocked on the user.
