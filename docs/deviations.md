# Deviations from the book

Every place the implementation departs from a `[decided]` statement, or settles an `[assumed]` or `[deferred]` one in a way worth recording. Each row also has a decision record in `docs/book/A-decision-records.md` (DR-39 onward). The final book revision walks this table and marks each row `book updated: y`.

| date | section | book says | implemented | why | DR | book updated |
|---|---|---|---|---|---|---|
| 2026-09-15 | 10.8 | arras source never contains the words quilt, digest, proof, or any loom command | guard test forbids quilt, atomize, unravel, and "loom <command>" phrases; digest and proof are exempt as manifest field names and the spec's route | `specs/manifest.md` §3, §4, §13 and route `/digest/<citekey>` use them; `crypto.subtle.digest()` too | DR-39 | y |
| 2026-09-15 | 5.1.4, 5.2.3, 6.2 | import and atomize refuse non-anchored environments | scanner tolerant by character offset; atomize refuses; import --fix-anchoring (M4) | 43 % of Manolache's nodes; user decision | DR-40 | y |
| 2026-09-15 | 5.6.1, 5.7.1 | proof attaches by adjacency or \ref only | plus enclosure; nested statements are nodes with `nested` proof-edges | Manolache and ACGS nesting | DR-41 | y |
| 2026-09-15 | 5.5.2 | external node = \cite in the title | or \cite as the first body token | all real instances | DR-42 | y |
| 2026-09-15 | 5.4.2 | heading label on the same or next non-blank line | unless that line opens an environment or heading | label theft | DR-43 | y |
| 2026-09-15 | 5.9.1 | \input{path} with .tex appended if absent | exact path, then .tex; braceless form; kpsewhich names ignored; non-.tex opaque | pspdftex figures, \input xy | DR-44 | y |
| 2026-09-15 | 5.3.1, 5.4 | digest prefix is the citekey | citekey slug with collision error; labels whitespace-normalised | punctuated citekeys, wrapped labels | DR-45 | y |
| 2026-09-15 | 5.5.1, 5.5.3 | closure = preamble plus local \usepackage files | transitive through .sty, comma lists; macro names expanded; unknown styles plain with warning | relloc's math-env.sty two hops away | DR-46 | y |
| 2026-09-15 | 5.1 | scan .tex files | decode Mac Roman then Latin-1 with a warning | Mac Roman arXiv source | DR-47 | y |
| 2026-09-15 | 5.13 | comments removed for hashing | comments blanked before every stage | commented duplicate label | DR-48 | y |
| 2026-09-15 | 5.9.2, 5.9.3 | sectioning on the expanded text; own text per node | hierarchy per master, ownership per file | hashes independent of the reaching master | DR-49 | y |
| 2026-09-15 | 5.7.1, 5.8 | edges from every own-text region | none from a master's preamble; none to oneself; single-label commands not split | macro bodies, labels with commas | DR-50 | y |
| 2026-09-15 | diagnostics.md, 8.1.2 | unreachable for a node or file | once per loose file, never for digests | noise | DR-51 | y |
| 2026-09-15 | diagnostics.md | fixed code table | five codes added | new conditions | DR-52 | y |
| 2026-09-15 | 5.5.1 | unknown-environment when a node uses an undeclared ENV | detected only for common theorem-like names | needs a heuristic | DR-53 | y |
| 2026-09-15 | 10.1.1, 10.7 | prerender every route to static files so a site is crawlable | shell prerender: one index.html per route plus the build directory; content loads client-side | needs a second data path; deferred | DR-54 | y |
| 2026-09-15 | 9.4.3, 10.1 (assumed) | MathJax 3 | MathJax 3 tex-svg, bundled | no font assets offline | DR-55 | y |
| 2026-09-15 | 8.3.5, 10.1 | arras takes one macro set per page | per-fragment set applied via \renewcommand inside the fragment's math | MathJax macros are global | DR-56 | y |
| 2026-09-15 | 10.1.2 | hash routing under file:// | path routing; SPA fallback in loom serve; file:// unsupported | router type is build-time | DR-57 | y |
| 2026-09-15 | specs/fixture.md §1 | missing \input and beamer talk redeclaring lemma | \iffalse-guarded missing include; talk declares proposition | both masters must compile | DR-58 | y |
| 2026-09-15 | 7.6.3 | proved needs an accepted proof | nodes that owe no proof are proved by acceptance | definitions could never be settled | DR-59 | y |
| 2026-09-15 | 7.4.2 | id counter per file | counter over every record | quilt-wide uniqueness | DR-60 | y |
| 2026-09-15 | 5.14 | lint = scanner checks | lint and check also report record-derived codes | codes need the records | DR-61 | y |
| 2026-09-15 | 5.9.2.3, 8.3.1 | loose files sectioned per file (stated, unimplemented) | files reached by no master get their own section units; digest headings become `<citekey>-sec-<n>` nodes | `loom id` on loose files; digests | DR-62 | y |
| 2026-09-15 | 5.6.1 | a `\ref` in the proof's optional argument decides attachment | when every named label is unknown, position decides and the label is a dangling link | deferred proofs with a typo'd label were unattached | DR-63 | y |
| 2026-09-15 | 6.2.4 | id label inserted after the heading | inserted directly after the heading's arguments, before existing labels | the id must be the first label | DR-64 | y |
| 2026-09-15 | 6.3, 6.4, 6.6 | identity test on the master | for a non-master SRC, the first reaching master is compiled with DEST in SRC's place in a scratch copy | section files have no PDF of their own | DR-65 | y |
| 2026-09-16 | 8.7 | `\cite[postnote]` in any region creates an edge | not when the citing node is in the digest of that citekey (its own locator title) | a digest node would depend on itself or its section | DR-66 | y |
| 2026-09-16 | 8.5.3 | ids `<citekey>-<abbrev>-<number>` | unnumbered results get `-star-<n>` and the locator `(unnumbered)` | `\newtheorem*` results have no number | DR-67 | y |
| 2026-09-16 | 8.5.2 | emulate counters only when compilation fails | emulate always, resynchronised at each `.aux` number; `numbering: emulated` only without an `.aux` | the `.aux` numbers only labelled results | DR-68 | y |
| 2026-09-16 | 8.1.7 | map environments by display name | prefer the numbered environment when a display name is declared twice | starred twins such as `thm*` | DR-69 | y |
| 2026-09-16 | 5.1 | scan every `.tex` under the root except `build/` | `ai/`, `refs/src/`, `refs/pdf/`, and `.claude/` are skipped too | run outputs and fetched sources are not the quilt's text | DR-70 | y |
| 2026-09-16 | 11.12 | skill stubs and slash commands | both are generated; skills carry the target as `$ARGUMENTS`, commands are one-line wrappers Claude Code now calls legacy | commands and skills were unified upstream | DR-71 | y |
| 2026-09-16 | 11.4.1 | the agent is launched with the orientation as its initial prompt | launched with a one-sentence pointer to `loom ai orient --run RUN`, `LOOM_RUN` set | the orientation is long and `orient` adds live state | DR-72 | y |
| 2026-09-16 | 5.5.1, 8.3.1 | macros are read from `\newcommand`, `\def`, `\DeclareMathOperator`, `\let` | also through aliases such as `\nc` declared by `\newcommand{\nc}{\newcommand}` or `\let`, resolved transitively | real preambles define hundreds of macros through one-letter aliases | DR-73 | y |
| 2026-09-16 | 8.3.2, 8.5.6 | the macro block holds unexpandable macros; `requires:` lists the reference's `\usepackage` lines | the block also carries `\newenvironment` and enumitem `\newlist`/`\setlist` definitions the statements use; page, font, and bibliography packages are left out of `requires:`; `\xspace` is dropped from expansions | bundles of the fetched papers would not compile otherwise | DR-74 | y |
| 2026-09-16 | 8.7 | a postnote matches a locator or `<taxon> <number>` from the id | also from any id-shaped alias of the node, so a digest can carry a result's number in another version of the paper | citations followed an earlier arXiv version | DR-75 | y |
| 2026-09-16 | 5.11 | `loom:reference-to-loose` for a statement referring to a node no master reaches | not when the target is in a digest file (digest sections included) | digests are loose by construction (8.1.2) | DR-76 | y |
| 2026-09-16 | 8.9 | `digest fetch` retrieves by the bib entry's arXiv identifier | only when `eprinttype`/`archiveprefix` is absent or arXiv; requests retry after a pause on 406 and 5xx; the PDF is fetched even when the source fails | JSTOR eprints and arXiv's burst refusals | DR-77 | y |
| 2026-09-16 | 10.1.2, 12.5 | `loom serve` falls back to index.html for paths without an extension | for every path that is not a bundle file or an asset, dots included; missing assets stay 404 | ids such as `ro-thm-1.0.1` have dots | DR-78 | y |
| 2026-09-16 | 9.4.2 | the fallback block compiles with the master's preamble closure | with the master's own preamble text compiled from the quilt root (`TEXINPUTS`), `@` made a letter, geometry and microtype neutralised, in a 16 cm minipage; a digest statement adds its macro block and required packages; three attempts before giving up | the closure's text defined every macro twice and lists cannot sit in LR mode | DR-79 | y |
| 2026-09-16 | 8.3.2 | the macro block is emitted at every extraction site | and never rendered as text: the digest document starts after `% !LOOM end macros` | the block's definitions were compiled a second time as a fallback figure | DR-80 | y |
| 2026-09-16 | 8.10 | `digest import` remaps environment names by display name | it does not remap; it reports undeclared environments and renames only the citekey, labels, `\uses`, and `\cite` keys | a digest file carries no display names | DR-81 | y |
| 2026-09-16 | 9.1, 9.2.1, 9.7 | `build/derived/` statement and proof files written by build | not written; regions come from the scan | nothing read them | DR-82 | y |
| 2026-09-16 | 9.2.2, 9.4.1, 9.8, 10.2, 10.5, 10.7 | staging directory; recompile only reached files; sidenote footnotes; specific panel filters; scroll kept on reload; npm and pip publishing | per-file `.tmp` rename; recompile after any source change; `span.footnote`; the filters as built; cache emptied; npm private, pip wrapper minimal | simpler mechanisms, same decisions | DR-83 | y |
| 2026-09-16 | 4.2.4 | unknown config keys warn | `loom:unknown-config-key` (warning) in lint | the warnings were collected but never printed | none (bug fix, M7) | y |
| 2026-09-16 | 5.9.6 | `% !TEX root` recorded for loose files | parsed, not consumed; marked deferred | no consumer yet | none (deferred, M7) | y |
| 2026-09-16 | 9.4.2 | the fallback gobbles the title with `\@gobble` | `\loomgobble`, plus the packages and names the stripped document class provided | `\input xy` leaves `@` an ordinary character | DR-84 | y |
| 2026-09-16 | 9.4.2 | (unstated) two LaTeX passes per fallback block, failures retried every build | one pass unless the log asks for a rerun; `-halt-on-error`; failures cached | the second pass changed nothing on any measured block | DR-85 | y |
| 2026-09-16 | 9.4.1, 9.4.2 | a theorem-like environment nested in own text falls back to SVG | an environment written inline in a container renders as HTML | a picture of a theorem is not a theorem, and the branch fired far too widely | DR-86 | y |
| 2026-09-16 | 9.8 | serve builds, then prints its URL | prints the URL, then builds, reporting progress and the error count | a cold first build looked like a hang | DR-87 | y |
| 2026-09-16 | 6.4, 12.3 | atomize advises the author to ignore or delete the source | `--ignore-src` does it, and the note names the flag | the advice was missed and produced a quilt with three copies of every node | DR-88 | y |
| 2026-09-16 | 15.6, 15.7 | the serif stack, the dark values and the graph library are open; an earlier request put display preferences in `config.toml` | the sitegen serif, a derived dark palette, and preferences in `localStorage` under `arras.prefs` with a `?shell=` override | chapter 15 is ground truth and says preferences are arras's, never the corpus's | DR-90 | y
| 2026-09-16 | specs/manifest.md §3 | a section node entry has no depth | `"level"`, additive, interface version unchanged | the inclusion tree gives order but not outline depth, so the contents rail could not indent or stop at a depth | DR-91 | y
| 2026-09-16 | specs/dialect.md §2.2, §2.4, §2.5 | node elements carry `data-id` and `data-key` | and a real `id`, the key slugged as display equations are | no fragment had an anchor target, so every link into a document dead-ended | DR-92 | y
| 2026-09-16 | 15.5 | the layout library and the scoping threshold are deferred | d3-force for force, elkjs for layered; no automatic threshold, scope by selection and depth | force is the behaviour org-roam-ui is described from and ELK was already in the viewer; the useful scope depends on the question | DR-93 | y
| 2026-09-16 | 10.3, 15.3.2 | every key is a link | a key with no page renders as plain text | diagnostics name file containers, which have no page, and linking them produced "Unknown key" | DR-94 | y
| 2026-09-16 | 9.4.3, specs/manifest.md §14 | a macro body is published as written | `\ifinner` becomes `\mathchoice`, `\ifmmode` takes its first branch, other conditionals are untouched | MathJax implements no conditionals, so the ACGS paper showed `\ifinner \else \fi` as error text throughout | DR-95 | y
| 2026-09-16 | specs/manifest.md §3 | a title is plain text | it keeps its `$…$` spans; the viewer typesets them | stripping macros turned `Structure of $\Sigma$` into `Structure of $ $` | DR-96 | y
| 2026-09-16 | 5.6.2, 5.7.2, 5.11.3, 10.2.1, 12.4, specs/manifest.md §16, specs/diagnostics.md | (unstated) a node-level relation that is not a dependency | `% !LOOM see: ID, ID`, resolved like a reference, published as a top-level `relations` list, printed by `deps` in a section marked not-a-dependency, shown by the viewer on both nodes | a detailed version of a result must not make the compact version stale, and `\uses` is the only way to declare a dependency | DR-89 | y |
| 2026-09-16 | 4.7, 11.5, specs/fixture.md §1 | (unstated) candidate taxa and a mode for exploring | the minimal master declares `conjecture` and `question`; the demo gains an outline master; an eighth mode, brainstorm, with four blocks | a quilt should be usable for planning from the first minute | DR-89 | y |
| 2026-09-16 | 5.7.3, 5.9.1, 9.1 | (unstated) offsets are computed and discarded; `scan` reads the disk | `\ref` and `\uses` sites carry offset and column, nodes record their label offsets, `kpsewhich` is memoised, and `scan(quilt, overlay)` reads unsaved buffers | a language server needs ranges, not lines, and must see the buffer | DR-97 | y
| 2026-09-16 | 15.3.1, 15.6, 15.7 | an 86px margin column inside the text; comments in the shell's right rail | a measured column between two gutters on the site generator's algebra, the margin annotation in the left gutter, comments in the right, long ones inline as boxes; the width scale is 36/44/52rem | the margin column took its width from the text and the rail put a comment a third of a screen from its sentence | DR-98 | y |
| 2026-09-16 | 15.2, 15.6, 15.8 | rails of 178, 160 and 168px; the default box model; a scrollbar always drawn | 267, 240 and 252px; `box-sizing: border-box` throughout; a thin scrollbar with a transparent thumb until the rail is used | a rail of `height: 100vh` plus padding was 24px taller than the window, and real contents entries wrapped onto three lines | DR-99 | y |
| 2026-09-16 | 9.4.1, 9.4.2 | a nested theorem-like environment or an untranslatable block falls back to SVG | so does a display block holding a diagram or a drawing command, decided by an allowlist of what a renderer handles | a diagram inside `\begin{equation}` reached the renderer and set the block in error colour: three such blocks in one paper, sixteen in another | DR-100 | y |
| 2026-09-16 | 9.4.3 | the macro table is the preamble closure's definitions | plus alphabets declared with `\DeclareMathAlphabet`, and definitions for the commands a renderer lacks that a published body uses | `\sHom` was `\scalebox{1.2}{\ensuremath{\mathpzc{Hom}}}`, none of which MathJax implements | DR-101 | y |
| 2026-09-16 | 9.4.1 | an expanded macro is mathematics when the expansion looks like it | when the macro's own body looks like it | a citekey's underscore made a paragraph of prose into a formula | DR-102 | y |
| 2026-09-16 | specs/dialect.md §2.6 | `\textcolor` renders its text and drops the colour | it keeps the colour as `span.tex-color[data-color]` | an author marking unverified text in red meant it to be visible | DR-103 | y |
| 2026-09-16 | 15.3.1 | (unstated) a display block scrolls when its formula is too wide | it scrolls sideways only, and the glyph cache is kept once for the page | an off-screen glyph cache and the hidden accessibility copy were counted as content, so ten of eighty-eight blocks had a vertical scrollbar with nothing to scroll to | DR-104 | y |
| 2026-09-16 | 4.7, 6.1, 12.3 | `git init` unless `--no-git` | no repository unless `--git`; the ignore file always, and both acts reported | loom reads no history and needs none, and a default-on silent side effect outside its own files is the one place it reached past its remit | DR-105 | y |
| 2026-09-16 | 4.7, 6.1 | `init --from` creates the quilt, then imports | the two are one transaction: a refused import removes the skeleton, and nothing is announced until the import completes | the refusal's own advice, re-running with `--fix-anchoring`, was then refused as `already inside a quilt` | DR-106 | y |
| 2026-09-16 | 00-front-matter, B, all chapters | the book carries `[decided]`, `[assumed]` and `[deferred]` statements, and each chapter ends with "Open questions" | only `[decided]` survives, as a normative marker; unfinished work moves to `docs/work-queue/` with an observable trigger per item | a book that describes the implementation cannot also carry its wishes, and a deferral with no trigger can never become work | DR-107 | y |
| 2026-09-16 | 4.2, 4.7, 8.9 | digests live in `refs/` beside fetched sources and PDFs | digests live in `digests/`; `refs/` is wholly fetched, ignored, and keyed by the work's global identifier | the tracked and ignored halves of one directory needed different rules, and a crawled work has no citekey to key on | DR-108 | y |
| 2026-09-16 | 8.4, 8.5 | a digest's ids are prefixed by the citekey's slug, and `source:` names the paper | the prefix is declared in the header; `source:` splits into `extracted-from:` and `published-as:` | a Zotero key made every `\uses` unreadable and fragile, and one field could not hold both the artifact parsed and the work cited | DR-109 | y |
| 2026-09-16 | 9.8, 10.5 | `loom serve` serves the bundle and `build/` | it also serves `refs/`, and the manifest says which artifacts exist | a comment that points into a reference PDF needs a URL for it, and the viewer needs to know whether one is there | DR-110 | y |
| 2026-09-16 | 4.2, 11.9 | `config.toml` carries `[ai] runner`, specified in `specs/runner.md` | the key is withdrawn: not written, not carried, removed by `upgrade`, still tolerated | it was inert and advertised a subsystem that has since been declined | DR-111 | y |
| 2026-09-16 | 15.2.1, 15.2.3 | the strip's icons are unicode marks; the contents bar marks the section named by the URL fragment | the icons are drawn paths; the bar follows the reader's scroll and compares on the published element id | a glyph column sits unevenly and reads as punctuation, and a hash-driven bar appears only after a click, never moves, and never lights for a section with no allocated id | DR-112 | y |
| 2026-09-16 | 15.2, 15.2.2, 15.7, 15.8 | three shells, rail, tabs and strip | two, rail and strip; a stored or linked `b` falls back to the default | the tabs shell was the one nobody chose, which is what the deletion rule was for | DR-113 | y |
| 2026-09-16 | 10.4, 15.3.1, 15.7 | comments stand in the right gutter or below their node; a mark is `--mark` | a `comments` preference, `margin` or `inline`; inline, a mark expands its comment in the flow; marks coloured by kind in both | the author wanted a comment where its sentence is, and its kind visible before opening it | DR-114 | y |
| 2026-09-16 | 10.2.7, 15.5 | layered, upstream above; drag belongs to both layouts | root coordinates from ELK, edges from dependency to user, a section drawn once as its group, no drag in layered, zoom about the pointer | grouped edges were drawn away from their nodes, upstream was below, and a layered position moved by hand is only wrong | DR-115 | y |
| 2026-09-16 | 15.3.1, 15.3.2, 15.4, 15.5 | (unstated) a neighbourhood view | a live local graph heading the node rail and floating over the read view, following the result being read | the author asked for Quartz's local graph, changing with the position in the draft | DR-116 | y |
| 2026-09-16 | 10.2.1, 15.3 | (unstated) previews | hovering a link to a node or a work shows a card, with the site generator's timings | the author asked for the site generator's hover preview | DR-117 | y |
| 2026-09-16 | 10.2.4, 10.2.5, 10.2.6, 10.2.10, 15.2.3, 15.3.4, 15.3.5 | cards link to `/review`, `/blockers` and `/problems`; a blockers page; the strip panel lists the views elsewhere | filters in the URL, cards open filtered tables, blockers is the review table's incomplete view with a blocks column, the panel shows the document and its contents | three cards opened one unfiltered table, and the blockers page repeated the strip | DR-118 | y |
| 2026-09-16 | 10.2.3, 10.2.9 | a references index and a digest view with provenance | both link each work out by its identifiers and to a fetched PDF; a citation with no digest result links to its reference; bibliography text is cleaned | 26 of one paper's 58 citations linked nowhere, and titles showed BibTeX braces | DR-119 | y |
| 2026-09-16 | 10.1, 10.5 | a fragment is typeset whole; the manifest is polled every second | long fragments typeset in batches from what the reader sees; MathJax loads while idle; no poll while the tab is hidden | 1.1s before the first formula appeared on a 2,744-formula paper, now 0.37s cold and 0.17s on return | DR-120 | y |
| 2026-09-16 | 15.2.3, 15.3.5 | (unstated) how a floating box closes | a press outside it or Escape, for every floating box | the settings panel stayed open until its control was pressed again | DR-121 | y |
| 2026-09-17 | 4.8, 8.9, specs/manifest.md §13 | loom touches the network only in `digest fetch`; WQ-04 planned binding resolved identifiers and offering them as a diff to `refs.bib` | `loom refs resolve` under `[refs] resolve = true` records candidates from zbMATH Open and Crossref; lint and the manifest carry them; nothing binds and the bibliography is never written | the author ruled the bibliography out of loom's scope, and where a confirmed identity lives is WQ-23's open question | DR-122 | y |
| 2026-09-17 | 10.2.3, 10.4, 15.3 | (unstated) links into a cited work; a digest locator is text | `loom:` links and locator pages open the fetched PDF in a viewer, only in the artifact they name | a comment should point at the evidence, and a page in the wrong version is a confident wrong answer | DR-123 | y |
| 2026-09-17 | 10.2.7, 15.5 | cited results used here, all, or none | also as papers: each cited work one node, own results expanded | 200 external nodes against 185 own on relloc; the full quotient is a star for one corpus | DR-124 | y |
| 2026-09-17 | 5.9.3, 5.13 | a parent's own text keeps an inclusion line as written | an inclusion of a child leaves the same `% !LOOM child:` marker an inline child leaves | atomizing one node changed the enclosing section's hash, so a move became an edit and the section went stale for nothing | DR-124 | y |
| 2026-09-17 | 4.2, 4.8, 8.9 | these are all the config keys; loom touches the network only in `digest fetch` and `refs resolve`; WQ-02 bounded a crawl by references per paper | `[crawl] depth, subjects, cap`; `loom refs crawl plan` (needs `resolve`) and `fetch` (needs `fetch` too) | the author's redesign: subjects and depth as the controls, a cap raised only in config, the count known before downloading | DR-125 | y |
| 2026-09-17 | 4.2, 8.13.1 | (plan 0.8) default subjects are the families of the depth-1 works' MSC codes; unclassified works pass through loom's MSC-to-arXiv table | `[crawl] subjects` and `categories` set by the author, no table, no default; `plan` without subjects surveys what the cited works cite | the author: a default must be computed from a source or specified, not fixed; a table tuned on algebraic geometry would lose an analyst's papers | DR-126 | y |
| 2026-09-17 | 8.13.4 | (plan 0.8) a work is filed under its first identifier | every identifier indexed, records merged when a source links them | a work's key changed mid-walk as sources added identifiers, and EGA I was recorded twice | DR-127 | y |
| 2026-09-17 | 8.13.6 | (plan 0.8) a failed download does not stop the crawl | three arXiv refusals in a row do; on-disk downloads counted before fetching | arXiv refused every request for more than fifty minutes; a resumed run could exceed the cap | DR-128 | y |
| 2026-09-17 | 10.2.7, 15.5 | the graph has two layouts, force and layered, with statements grouped in a box per section | four drawings — Dots, Box, Sections, Reading Order — and Box draws no section groups | the grouped drawing is unreadable at paper scale: 71 results in 74 sections filled a canvas legible only at an unreadable zoom | DR-129 | y |
| 2026-09-17 | 9.4.1, specs/dialect.md §5 | TeX inside a math element is passed through verbatim | `\label` and `\ref` are rewritten, as before, and a math-only macro inside `\text{…}` is written between dollars | MathJax's text mode has no `\underline`, so `\text{nodes of \ul C}` cost the whole formula and showed the ACGS proof as its source | DR-130 | y |
| 2026-09-17 | 9.1 | the fragment cache hashes loom's version, the files, the numbering and the marks | in a `dev` version it hashes loom's own rendering code too, and `loom build --force` ignores the cache | a checkout's version never moves, so a converter fix left the old HTML published until `build/cache` was deleted by hand | DR-131 | y |
| 2026-09-17 | 3.2, 4.1, 4.2, 4.4 | the masters directory is `drafts/`, named by `[quilt] drafts` | `drafting/`, named by `[quilt] drafting`; `drafts` read as a deprecated alias with a warning and nothing moved | "drafts" reads as versions of the paper, which is what the canon holds | DR-132 | y |
| 2026-09-17 | 4.1, 4.2.1 | these are all the config keys | plus `name`, `drafting`, `canon`, and a documented `history` key init never writes | the workbench needs the project's name and two directories; the record's location is worth being able to say | DR-132 | y |
| 2026-09-17 | 4.3.4 | the user config has no keys besides `[author] name` | it may also set `[quilt] name`, `drafting`, `canon`, `history`; the quilt's value wins | a person's convention for their own directory names is not a quilt's contract | DR-132 | y |
| 2026-09-17 | 4.8.2 | loom never deletes anything outside `build/` | `loom upgrade` moves `.loom/snapshots/` into `<history>/texts/` | loom's own directory; the files are content-addressed, so every acceptance row still resolves | DR-132 | y |
| 2026-09-17 | 1.2 | twelve principles | thirteen | every check the workbench added had to answer "and then what?" | DR-133 | y |
| 2026-09-17 | 4.8.3, 7.2.2 | loom never edits the ledger except by appending acceptance rows; snapshots live in `.loom/snapshots/` | two ledgers, both append-only; snapshots and versions share `<history>/texts/` | the history is a second record, and one content-addressed store serves both | DR-134 | y |
| 2026-09-17 | 5.3.3.2 | an id is defined by at most one node; two definitions are `duplicate-id` | that, and the id is then published as conflicted: no text, no winner, a fix per file | resolution by walk order is the one outcome nobody can debug | DR-135 | y |
| 2026-09-17 | 5.3.2.3 | an id is reused when nothing can point at it | never, once the history has recorded it; a retired id written under again is recovery or reuse, by content | a record that can be pointed at is a thing that can point | DR-135 | y |
| 2026-09-17 | 4.9, 4.4 | `% !LOOM ignore` is the remedy for an old draft's inline copies | a conversion records that its output superseded its input; the ignore line stays for files loom did not produce | a conversion knows the fact with certainty, and loom no longer writes into an author's file | DR-138 | y |
| 2026-09-17 | 6.4.1.7 | `atomize --ignore-src` writes `% !LOOM ignore` above SRC | withdrawn; supersession is recorded, and `--retire` moves the file when asked | one mechanism per fact; moving a file is the author's call | DR-138 | y |
| 2026-09-17 | 4.8.1 | loom never moves a file the author wrote | `atomize --retire` moves the converted input into `retired/` | opt-in, at the author's request, for a file they have finished with | DR-138 | y |
| 2026-09-17 | 6.1, 6.2 | import copies the closure, moves the master into the masters directory, inserts the package line and ids, and reports | import writes one flat landmark and inserts nothing; `draft` does the rest | a landmark is the paper as it arrived; a working document is loom's own file | DR-137 | y |
| 2026-09-17 | 4.7 | with `--from` the imported paper's master takes the minimal master's place | the drafting directory starts empty; work begins with `loom draft` | the paper arrives as a landmark, which is not a document being worked on | DR-137 | y |
| 2026-09-17 | 4.4.3, 11.5, 12.2 | `loom assemble MASTER DEST` flattens a master | dropped; `loom linearize SPINE --to FILE`, which knows the identity rule | flattening a shared node file is how a node comes to be defined twice | DR-139 | y |
| 2026-09-17 | 4.5.5 | import inserts `\usepackage{loom}` into the copied master | `draft` inserts it; `canonize` replaces it with the package's macros in a marked block | a landmark must compile where loom.sty is not | DR-139 | y |
| 2026-09-17 | 11.7 | `ai promote` copies a draft node into `nodes/`, allocating its id | digest case only; a drafted node is previewed and pasted by the author | the author's choice: more intentional, and simpler | DR-140 | y |
| 2026-09-17 | specs/diagnostics.md §1 | a diagnostic has five fields | plus optional `subject` and `fixes` | three resolutions cannot be offered as one sentence of prose | DR-136 | y |
| 2026-09-17 | 9.3 | three fragment kinds | four: node, master, digest, canon | a landmark is a document a reader reads | DR-141 | y |
| 2026-09-17 | 10.2.2 | a master view per master | a read view per live document and per landmark, the picker in two groups | same | DR-141 | y |
| 2026-09-17 | specs/manifest.md §1, §8 | `corpus.name` is the quilt's directory name; four state labels | `corpus.name` is the project's name; five labels, `conflicted` among them | a corpus's name and its default document's title are different things | DR-141 | y |
| 2026-09-17 | 14.3, specs/fixture.md | the synthetic quilt and the demo are maintained as checked-in trees | both written by `scripts/gen_quilts.py` and checked against it; `demos/` rebuilt by `demos/build.py` | a fixture whose records nothing regenerates cannot be proved to be what the commands write | DR-142 | y |
| 2026-09-17 | 10.1, 10.7 | arras is served at an origin root with the corpus at `/build/` | the base path is build-time configurable and the data root is settable at run time; both default to today's behaviour | arras could not be served under a prefix, and a page embedding one node of a corpus served elsewhere had no way to say where it is | DR-143 | y |
| 2026-09-17 | 4.2, 4.8, 8.13, 12.10 | a quilt's library is crawled from its bibliography by depth and subject; `[crawl]` configures it | loom fetches only what its own bibliography cites; the crawl, its table and its commands are removed and go to weft | corpus machinery in a paper's tool makes every one of loom's promises conditional | DR-144 | y |
| 2026-09-17 | 4.2 | an unknown or malformed config table is warned about and ignored | also actually ignored: a scalar where a table belongs no longer raises | the warning path crashed before reaching the warning's point | DR-145 | y |
| 2026-09-17 | specs/manifest.md §1, 10.8.1 | every top-level manifest section is present; only `canon` and `relations` are optional | a publisher may omit any section but `interface_version` and `publisher`; the viewer normalises absence to empty | two incoming publishers write different subsets, and an absent section blanked the app | DR-146 | y |
| 2026-09-17 | specs/fixture.md | one conformance fixture, generated by loom and vendored by both tools | a second, `fixture-minimal`, hand-written and owned by the specification | one publisher's fixture cannot show that the interface is publisher-neutral | DR-146 | y |
| 2026-09-17 | 10.8.1 | every URL the core emits is composed in `src/lib/paths.ts` | true of three more sites, and the guard rule now catches a bare `href="/"` | R1 left them; the rule required a letter after the slash | DR-147 | y |

