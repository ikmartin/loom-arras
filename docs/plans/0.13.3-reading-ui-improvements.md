# 0.13.3 - the full design specification, unified and optimized

## Why

### The five principles

Reading mode is measured against these. They came out of the work rather than preceding it, and each one has cost something to follow.

**P1 · Open on the thing itself.** The first screenful belongs to what the reader came for. What the application has to say *about* that thing goes to an edge or behind a control.

**P2 · Say it once, in the place that governs it.** Where two surfaces state the same fact, one is deleted rather than restyled, and the survivor is whichever one controls the fact.

**P3 · Claim only what is known.** Where the viewer does not know something it shows nothing rather than something plausible. A marker says the one thing that cannot be inferred and no more.

**P4 · Name the question. One element, one question.** Every element exists because a reader asks something at that moment. If the question cannot be named in one short sentence, the element goes; if two can be named, it is two elements; if two elements answer one question, one goes. Two riders: **a place is not an element** — a rail or a panel groups elements, and the test for a place is whether its elements' questions are asked at the same moment — and **the question is the reader's, not the system's**, which is what condemns a section reporting that a node is "in no document".

**P5 · Following a connection must not cost the thing you followed it from.** The viewer's subject is how texts relate; a relation has two terms; a viewer that holds one term can only show a relation by destroying the other.

### What is wrong

Reading mode is seven routes that each invented their own chrome. Measured at 1440×900 against the running showcase:

| | left chrome | content | the thing itself |
| --- | --- | --- | --- |
| a document | 296px | 1144px | prose 733×566 — **32%** of the window, beginning 334px down |
| a node | 296px | 908px + a 236px rail | the statement, 856×**69** — 12% of its own column |
| a cited work | 296px | 1144px | the paper, **73.5%** |
| the library index | 296px | 1144px | a 238px table; **66%** blank, duplicating the side panel |
| a thread | 296px | 1144px | 1092×168 — 13%; **52%** blank, and no sign of its subject |

Three faults run through all of them.

**Chrome is owned by routes.** Each route builds its own rail, head and control row, so the same question is answered in five different places with five different layouts, and no answer can be reused.

**The split holds one kind of thing.** `?beside` is a boolean and the second pane is always the discussion. A document cannot be put beside a draft; two drafts cannot be compared. Following a citation therefore consumes the only pane there is — which is why the hover preview had to be built so elaborately: it is a mechanism for avoiding a navigation whose cost is too high.

**Sections exist because the data model permits them, not because a reader asks.** "In no document", "depends on nothing", a tick column repeating marks already visible, a counts line repeating a control, a library page repeating the panel.

### What this accomplishes

| | before | after |
| --- | --- | --- |
| chrome above a document's first line | 334px | 64px |
| a document's share of the first screen | 32% | ~47% |
| places a document's controls are drawn | one per route, per pane | **one**, in the frame |
| kinds of thing the second pane can hold | 1 | every reading view |
| routes owning chrome | 7 | **0** |
| a citation's cost | the draft you were reading | nothing |

And in the codebase: five route components stop being pages and become content renderers; `Beside`, `Split`, `Tabs`, the library's tab band, the node page's rail and the thread page collapse into one workspace; four transitional props introduced by the shipped work are deleted.

---

## The architecture

### One abstraction: the item

Everything reading mode shows is an **item**: a thing with an address, a renderer, a set of controls, and optionally a set of internal views.

```ts
type ItemKind = 'document' | 'work' | 'node' | 'session' | 'context';
interface Item {
  kind: ItemKind;
  id: string;            // master path, citekey, node key, session id
  view?: string;         // an internal view: 'paper' | 'digest' | 'info', 'discussion' | 'did'
  locator?: WorkLink;    // where in it: page, span, result, annot
}
```

One registry maps a kind to its renderer, its controls and its internal views. Nothing else in the viewer needs to know what kinds exist.

| kind | renders | internal views | controls |
| --- | --- | --- | --- |
| `document` | a master or a landmark | — | annotations, PDF |
| `work` | a cited work | Paper · Digest · Info | tool pair, fit, zoom, page, PDF |
| `node` | one result | — | context, source, annotations |
| `session` | a session | Discussion · What it did | — |
| `context` | what a node is in, uses and is used by | — | — |

**Internal views are the unification that pays for the registry.** A work's `Paper / Digest / Info` and a session's `Discussion / What it did` are the same mechanism — several readings of one thing — and both belong in the control cluster rather than in a band the route draws for itself.

### One place: the workspace

```ts
class Workspace {
  panes: [Pane, Pane];          // exactly two; 'the other pane' must be well defined
  focus: 0 | 1;
  get current(): Item | null;   // the focused pane's active item
  open(item: Item, where: 'here' | 'beside'): void;
  reveal(item: Item): void;     // already open: focus its pane, activate, scroll to locator
  move(pane: 0 | 1, id: string): void;
  close(pane: 0 | 1, id: string): void;
}
class Pane { items: Item[]; active: string }
```

`current` is **the** definition of "the current document". The panel's contents tree, the rail's control cluster, the `context` and `discussion` links and the preview's `open here` all read that one value. Nothing computes it independently.

### One surface: the popover

A button that opens a dismissible surface appears six times — `Settings`, `DevShelf`, `LocalGraphPanel`, `HelpDot`, `DeleteSession`, `PdfViewer` — each with its own markup around the same `dismiss` action, and the session picker makes seven. One `Popover` component takes a trigger and a body.

### What collapses into what

| these | become |
| --- | --- |
| `Beside`, `Split`, `BesideToggle`, `Tabs`, the library page's `.top`, the thread page | the workspace: `Workspace`, `Pane`, `PaneHead` |
| `master/[stem]`, `canon/[stem]`, `node/[...key]`, `library/[citekey]`, `thread/[id]` | item renderers, with no chrome of their own |
| `ReadingRail` (Part A) | the frame's global rail |
| the node page's right rail and `LocalGraphPanel`'s rail usage | a `context` item |
| `PdfView` (Part B), `PdfTools` | the `work` kind's control state and cluster |
| `LinkPreview`'s per-target branches | an item rendered small |
| six popover call sites | one `Popover` |

The preview is the neatest of these: **a hover card renders an item**, so it stops having its own node, reference and work cases and asks the registry like everything else.

### What is deleted

- **`?beside` as a boolean**, and `rail.beside` — the hack that hid a page's rail while the split was open.
- **`beside=0`** appended to citation links by Part C. That parameter exists to stop a citation dragging a discussion pane along; a citation now opens beside by construction.
- **Four transitional props** that the shipped work introduced precisely because chrome was already migrating out of components: `Beside.control`, `Fragment.head`, `Reading.toolbar`, `PdfDoc.toolbar`. Parts A and B were the first two steps of this refactor without naming it.
- **`Fragment`'s `content-head`** — the `expand all` / `hide all` / counts row, already gated off the document view.
- **The thread route's page**, the **library index's list**, the **node page's rail**, and the **annotation tick column** (already retired, DR-227).
- **`.page.reading`'s full-height rules** in the library route: a pane manages its own height.

### The URL

The two **active** items, one per pane, and their locators — the arrangement a reader would send someone. The rest of each pane's list is session-local and does not survive a reload. A shared link reproduces what the sender was looking at rather than their whole working set; the alternative is a URL that grows without bound. This is a deliberate loss, recorded so it is not discovered later.

### The boundary

`/review`, `/graph`, `/problems`, `/tags`, `/taxa`, `/loose`, `/threads`, `/library` and `/` are **not** reading mode. They render full width, outside the workspace, with no panes and no tabs. Reading mode is the set of things a reader reads: documents, works, nodes, sessions and a node's context.

---

## The design

### The frame

![a single pane](images/0.13.3-workspace-single.png)

**F1.** Four regions, left to right: a 44px icon rail; the side panel at 232px, collapsible; the workspace. Above the workspace, one 34px global rail.

**F2.** The global rail holds exactly three things: the annotation filter at the left, the **control cluster** for `workspace.current` in the middle, and `open session discussion` at the right. Nothing else is global.

**F3 · The filter.** `showing annotations from [this session | all]`. It governs which annotations are drawn, which is a fact about documents, not about the panel.

**F4 · The control cluster** renders the controls and internal views of `workspace.current`, from the registry. It is drawn once. Two panes each carrying a control row would be the same widgets twice, permanently, for a document only one of which is being read.

**F5 · `open session discussion`** opens the selected session as an item beside. It is gated on `writable()` — the same precondition as the composer — and reads `select a session first` when refused, so the viewer declines in one voice.

### The side panel

**S1 · One scrolling column**, in order: the corpus name with its collapse control; **Documents** with Working Drafts and Canon; **Nodes**; **Library**. No tab strip and no view switcher: every section answers a question asked while reading, so there is nothing to switch between.

![the side panel](images/0.13.3-panel-d.png)

**S2 · The contents are open.** The current document's contents hang under its row, expanded by default, with `hide ⌄` / `show ›` on the row. *Where am I in this* is the question asked most often while reading and should not cost a click.

**S3 · The panel follows `workspace.current`.** The tree belongs to a document, and which document is whichever the focused pane holds. When `current` is not a document, no tree is drawn.

**S4 · A click in the panel opens in the focused pane.** Choosing what to read is not a connection between texts, and splitting must never be a side effect of it.

**S5 · The footer is pinned and states the write target** — a state dot, the session's name, its unread count, a chevron — outside the scroll region, because writes land in the selected session whatever is shown and a reader must never go looking for where their work will go.

**S6 · The footer's tone is `writable()`'s answer.** Empty means writable: no fill, blue dot, name in ink. Anything else means refused: amber fill, hollow ring, name in warning ink. The footer carries **only the state**; the sentence belongs beside the refused control.

![the picker](images/0.13.3-panel-d-picker.png)

**S7 · The footer opens the picker**, a `Popover` 320px wide — wider than the column, because a session row carries a name, a count and a date and 232px cannot hold three. It contains a find field and `+ new`; `WRITING INTO`, most recently touched first; `CLOSED (n)`, folded, whose rows carry `⟳`; and `select none`.

![no session selected](images/0.13.3-panel-d-refused.png)

**S8 · `⟳` reopens and selects.** The refusal's wording promises this path and nothing else performs it.

**S9 · Two deletions.** The paragraph explaining what `N new` counts — documentation inside a control is the book's job done badly. The `44 nodes · 1 errors · 4 warnings` line becomes a link on the icon rail's warning glyph.

### A pane

![two panes](images/0.13.3-workspace-split.png)

**W1 · Two panes of equal rank.** Both hold the same kinds of item and behave identically; neither is primary. *What I am working on* is a fact about the reader's intent, not a property of a document, and a layout that encodes it asserts something only the reader knows. Default 50/50; the divider is draggable and its ratio remembered.

**W2 · A pane head carries its tab strip and nothing else.** One question: which item am I looking at. Everything about the item is in the global rail.

**W3 · Tabs are a fixed 124px**, so a target does not move as its neighbours open and close. The strip clips and scrolls horizontally on hover, with `scrollbar-gutter: stable` so revealing the bar shifts nothing.

**W4 · A tab's controls sit over it.** `⇄` and `×` are absolutely positioned at the tab's right behind a gradient in the tab's own colour, shown always on the active tab and on hover or keyboard focus for the others. A tab that grew on hover would move the thing the pointer was travelling towards.

**W5 · Controls appear only where they can act.** The single tab of a single pane shows neither: moving it would empty one pane and refill the other with the same thing; closing it would leave nothing.

**W6 · `⇄` moves a tab to the other pane and focus follows it.** With exactly two panes there is one destination, so nothing has to be aimed at and no drop target is drawn. Moving the last item of a pane closes that pane; the other takes the full width.

**W7 · Everything adds; the only question is which pane.**

- a **link inside an item** opens in the other pane — it is a connection between two texts, so both ends survive it;
- a **click in the side panel** opens in the focused pane.

A link's destination is a fact about the pane it lives in, not about what had focus a moment ago.

**W8 · An item is opened once.** Where the target is already open in either pane, that pane takes focus, that tab becomes active, and the view scrolls to the locator. A second tab of one document is one document in two states.

**W9 · Tabs accumulate and the author closes them.** No eviction, no cap. A viewer that closed a reader's tabs by a rule it invented would be guessing which of them mattered.

**W10 · Focus is taken by any interaction with a pane** — clicking its text, scrolling it, or tabbing into it — on `pointerdown` in the capture phase, so that anything relative to the focused pane reads the new value in the same frame.

**W11 · The focused pane is marked** while both are open: a soft lift, a white head, an accent line above it. The unfocused pane's content is **not** dimmed — making a document harder to read in order to say it is not the current one is the wrong trade in a reading tool.

### An item: a document

**D1.** A master or a landmark, rendered in a measured column, with each result's id and state in the left gutter and no tick column in the right. A landmark's step — `landmark @3 (flows-v2)` — stands in its tab, because it is the document's identity rather than a note about it; its path and commit message do not.

**D2 · The gutter is off by default**, behind **Settings ▸ Show ids**: a reader reading the paper does not need every key beside it, and the reader who does is working on the corpus rather than reading it.

**D3 · Controls:** `annotations` — open or close every annotation at its mark, the label naming what the click gives — and `PDF`, a plain link, the cheap escape from a manual latexmk. **`e` and `h` do the same two things from the keyboard and must survive**: they are the affordance for a reader who does not want a control at all.

**D4.** No document-level annotation block and no document-level composer. An annotation is defined by its anchor; one whose anchor is "the whole document" renders at the top and reads as a comment on the title. The capability stays and those annotations are read in the session's discussion.

### An item: a cited work

**K1 · Internal views `Paper · Digest · Info`**, paper first. A work in the Library is a document a reader came to read; the digest is a derived index of it and never what you meant when you clicked the title.

**K2 · Controls:** the internal views, then the tool pair (select, box), then fit-width, zoom and page, then `PDF`.

**K3 · Page and zoom are typed into**, as a desktop viewer's are. Enter commits, Escape restores, out-of-range clamps.

**K4 · The paper is full bleed** inside its pane, with no gutter of its own.

**K5 · A pane holding a work needs roughly 420px** for its cluster to be usable; the divider has a floor.

### An item: a node

![a node and its context](images/0.13.3-workspace-node.png)

**N1 · The statement leads**, at reading size, alone. Its proofs follow, then its annotations in the flow.

**N2 · No right rail.** A node's context is an item you open beside it.

**N3 · Controls:** `context`, `source`, `annotations`.

**N4 · No filter row above the annotations.** Three selects over a single annotation is the system asking a question the reader has not got.

### An item: a node's context

**C1.** What the node is in, what uses it, which discussions touched it, and the local graph — in one column, opened beside the node by `context`.

**C2 · Absences are omitted, not reported.** A node that no document reaches shows no `IN` heading; a node nothing depends on shows no `DEPENDS ON` heading. Nobody asks whether a thing is in nothing.

### An item: a session

**E1 · Internal views `Discussion · What it did`.** Two errands: a session read while working is a conversation beside its subject; a session read on its own is being audited.

**E2 · `Discussion`** is the transcript in a measured column: targets as chips under the title, a date gutter carrying **relative dates**, findings marked by a coloured edge in the flow, attachments as an entry, the composer at the foot. No raw ISO timestamps anywhere.

**E3 · `What it did`** is what happened in a sentence; the still-open findings as a scannable list carrying kind and target; what it touched; what settled; the report.

**E4 · The discussion is scoped by session, not by document.** One selected session is one place you are talking, which is why the control that opens it is global rather than an attachment of a document.

### The hover preview

![the preview over both panes](images/0.13.3-workspace-preview.png)

**H1 · Hover**, with a 300ms delay and a 200ms grace to travel into the card, which stays while the pointer is inside it. Escape or any scroll dismisses. Absent on touch. This is P5's cheapest form: the reader has not acted at all.

**H2 · Positioned against the viewport**, never inside a pane. A pane clips, and a card a divider can cut in half is worse than no card.

**H3 · A citation previews the paper, not the transcription of it**, cropped to the statement with two lines either side, scrolling when the statement is longer, the page drawn at 115% of the card's width and running to the rounded edge.

**H4 · The place is marked by a translucent amber dot** at the start of the first line, sized from the mark's own height. A box asserts an extent the reader can already see; the dot marks the one thing they cannot infer.

**H5 · Where the place is not known, nothing opens.** A postnote naming a page opens there; a postnote naming a place that cannot be found opens no card at all; a bare citation names the work, so its front page stands.

**H6 · One action, always drawn: `open here`**, which opens the target in the pane the link is in — the other destination from the one a click gives. It is P5's own remedy, and a remedy revealed only once the pointer is inside the card is one most readers never find.

**H7 · A click on the link opens beside.** `open here` and a click differ only in destination pane.

### A proposed text

**T1.** A suggestion's payload opens as **source**: what is proposed is text to be written into a document, so the thing a reader judges is the source, character for character.

**T2 · The control names the destination** — `rendered latex` / `verbatim code` — not the current state.

**T3 · Rendered means rendered.** Math typeset and `\emph`, `\textit`, `\textbf`, `\texttt` turned into markup; anything unrecognised left exactly as written, so a reader who sees a command still in its braces knows to read the verbatim instead. It is not a LaTeX engine and must not pretend to be one.

**T4.** No copy controls. Selecting text is how anyone copies text.

### Settings

**G1.** Every row is one segmented control; separate pills read as separate settings.

**G2.** The rows are Type, Size, Width, Theme, Format, Comments, **Show ids** (default no), **Discussion Pane** (`Left | Right`, in that order, so the control is a picture of the choice).


### Gaps after phases 1–3

What phases 1–3 built, measured against P1–P5 by walking every surface in the running showcase on 2026-09-22: the frame and icon strip, the side panel, the footer and the picker, the global rail and each kind's cluster, panes, tabs, focus and the divider (wide and below 700px), the document, the work, the node, the context, the session's two views, the ledger, the hover card, settings and the popovers. This section records; a phase 4 fixes what human testing confirms. A **violation** breaks a principle as written; a **gap** meets it in part, or only in the common case.

**Running the showcase to check these.** From the repository root: `cp -R demos/showcase "$TMPDIR/showcase"`, then `uv run --project loom loom serve --quilt "$TMPDIR/showcase" --port 4190`, and open `http://localhost:4190/` in a window of 1440×900. Serve a copy, because serving writes into the quilt. The showcase holds four drafts (`main-atomic`, `sketch-atomic`, `sketch`, `talk`), two landmarks (`flows-v1`, `flows-v2`), a node no document reaches (`sh-0402`), works filed and not (`Arden24` and `Bellamy19` with papers and digests, `Bellamy19` with a proposal and open notes; `Cortez07` unfiled; `Stacks` unreadable), two open sessions (`referee-sh-0009`, which wrote a report, and `survey-bellamy`) and two closed ones. A `?beside=` URL below is written with its value unencoded for reading; the browser accepts it as typed.

**G1 · P1, N1 · violation · the node.** The statement does not lead: a title and a meta row (taxon, id chip, badges, version, `not yet numbered`, tags) stand above it, and a missing proof puts `No proof is attached. See this block in Review.` above it too; beneath it, proof states, the comment control, reference notes, `What this rests on` and `On this page` stand between it and its annotations. *Check:* open `/node/sh-0009` — the statement's first line begins about 135px below the rail and the annotations about 490px down; open `/node/sh-0402` — the missing-proof line is above the statement. *Candidate fix:* the statement first at reading size; the id, state and badges into the tab or the rail; the missing proof said where the proof would be; the rest after the annotations or into the context. *Phase 4:* fixed: the node draws its statement and proofs alone, and what is about it stands in its context (DR-242-ikmartin); writing on it is the two tools a paper has (DR-243-ikmartin).

**G2 · P4, C2 · violation · the context.** Absences are reported rather than omitted. A node nothing depends on and that depends on nothing gets `nothing depends on this, and it depends on nothing` under its local graph, and a node no document reaches shows an `unreachable` diagnostic, `no master reaches nodes/sh-0402.tex` — "in no document" said in the system's words. *Check:* `/node/sh-0402?beside=/context/sh-0402` — read the local graph's caption and the diagnostics. *Candidate fix:* the graph draws the lone node and says nothing; the context omits the `unreachable` diagnostic, which is the problems page's business. *Phase 5:* fixed: no isolated-node caption, and the context leaves `unreachable` to the problems page (DR-255-ikmartin).

**G3 · P1, K4 · gap · a work in half a pane.** At the remembered zoom a paper is wider than a half-width pane, so its right side needs a sideways scroll. *Check:* `/master/main-atomic`, click `[1, Proposition 2.1]` — in the right pane the page runs past the pane's edge (857px of page in 563px of column). *Candidate fix:* a work opened into a pane narrower than its page opens fitted to width; the reader's zoom applies once they change it. *Phase 4:* fixed: a paper wider than its pane opens fitted to its text block with the dot in view (DR-246-ikmartin).

**G4 · P3, W3 · violation · tabs.** Names are cut, and one carries source for a name: a work opened at a result is tabbed `Arden24 · Proposition 2.1, p.~1` — the postnote's raw TeX — and shows `Arden24 · Pr` in its 124px; a context shows `context · sh-`; a draft `main-atomic`. A cut name is a plausible name, not the name. *Check:* from `/master/main-atomic`, click `[1, Proposition 2.1]` and open `sh-0009`'s context; read the tabs, and hover one for its full title. *Candidate fix:* a work's tab names the result without the postnote (`Arden24 · Prop 2.1`); the context's tab is the node's own name with a marker rather than a prefix; choose the width against the showcase's real names, as the spec's risks section asked. *Phase 4:* fixed: 148px tabs, the postnote dropped and the taxon abbreviated, a context named by its node after a glyph, and an ellipsis before the controls (DR-247-ikmartin).

**G5 · P4 · gap · the rail and the node's head.** `not yet numbered` stands in the document's cluster and in the node's meta row as text with no action. It answers the build's question — has the document been compiled — and not a reader's. *Check:* `/master/main-atomic`, the rail's middle; `/node/sh-0402`, the meta row. *Candidate fix:* say nothing; where a number is missing, the number's place is simply empty, or a problems-glyph diagnostic carries it. *Phase 4:* fixed, and larger than recorded: the check URL was wrong (`main-atomic` is numbered); an uncompiled document showed the default document's numbers. Each document now shows its own, and the phrase is gone (DR-245-ikmartin).

**G6 · P1, P4 · violation · below 700px.** The global rail's three parts overlap into unreadable text, and the pane switch repeats the tab strip's names directly above it; the local graph float covers most of the document. *Check:* narrow the window to about 660px on `/master/main-atomic?beside=/node/sh-0009` and read the top of the page. *Candidate fix:* below the breakpoint the rail keeps the cluster only (the filter and the discussion into a popover), the switch replaces the tab strip rather than standing over it, and the float does not open. *Phase 4:* fixed: the panel folds below 1200px, the rail puts its edges behind `⋯` when its parts do not fit, and one strip holds both panes' tabs (DR-248-ikmartin).

**G7 · P2, P3 · violation · the footer.** The selected session is saved but never read back, so every load says `no session selected` in the refused tone although one was chosen; and it compounds, since `open session discussion` and every composer are refused after every reload. *Check:* choose `referee-sh-0009` in the picker, reload — the footer says `no session selected`. *Candidate fix:* `sessionView.load()` at start, as `prefs.load()` is; drop a stored selection the manifest no longer has. *Phase 4:* fixed: the selection is read back at load, and a stored session the corpus no longer lists is let go.

**G8 · P2 · gap · the refusal said three times.** With no session selected, the footer (`no session selected`), the rail (`select a session first`) and the composer's sentence all say it at once on a node's page. Each is specified (S6, F5, the composer), but three statements of one fact on one screen is P2's fault. *Check:* `/node/sh-0009` with nothing selected — count the three. *Candidate fix:* the footer's state stays; the rail's control is simply disabled with the sentence as its title; the composer keeps its sentence, since it is the control a reader pressed. *Phase 5:* fixed: the rail's control keeps its label and gives the reason as its title, quiet rather than amber, and a closed session's discussion opens (DR-259-ikmartin).

**G9 · P2 · gap · two local graphs.** With a node's context beside a document, the document's floating local graph and the context's graph can both be showing: one graph twice. *Check:* `/master/main-atomic?beside=/context/sh-0009`, open the document's local graph float — two graph panels. *Candidate fix:* the document's float does not open while a context is showing, or the float is retired in favour of opening the current result's context. *Phase 4:* fixed, and worse than recorded (the two graphs were of different results): the float stands down while a context is on screen (DR-251-ikmartin).

**G10 · P5 · gap · following a link from a context.** A link in a node's context opens in the other pane, which is the node's own pane, so the node the context belongs to goes behind a tab. The context survives; the thing it is the context *of* does not stay on screen. *Check:* `/node/sh-0009`, `context`, then a `depends on` link — the left pane's active tab becomes the linked result and `sh-0009` is a background tab. *Candidate fix:* a context's links open in the context's own pane (replacing it is not a cost: the context is recoverable from the node), or the linked node's context replaces this one. *Phase 4:* fixed: a context's links open in its own pane (DR-250-ikmartin).

**G11 · P2 · gap · a comment count and its mark.** Results carry an `N comment` count beside their label (DR-114's affordance for a comment with no mark) — nine in `main-atomic` — and it needs checking whether those comments also have marks in the text, in which case the count says again what the mark says. *Check:* `/master/main-atomic`, section 2's definitions — click a `1 comment` count, then look for a highlighted mark of the same comment in that result's text. *Candidate fix:* the count only for comments with no mark, as DR-114 intended. *Phase 4:* discarded: none of the nine counts in `main-atomic` has a mark in the text, so the count is the only place each of those comments is drawn, as DR-114 intends.

**G12 · P1 · gap · the discussion's head.** The session's targets lead the transcript as chips, and `referee-sh-0009` has seventeen — three rows before the first word of the conversation. They answer the question the spec gave them (what is this about), but at that length they are the first screen. *Check:* `/session/s-2026-09-16-0001` — how far down the first message starts. *Candidate fix:* the documents it touched as chips and the keys behind a count (`and 15 results`), or the chips on one line that scrolls. *Phase 5:* fixed: the documents, four other targets and `and N more`, unfolding in place (DR-256-ikmartin).

**G13 · P3 · gap · timestamps inside the journal.** The transcript's gutter says the day in words, but the journal's own messages carry their authoring heading in their body — `2026-09-16 11:00 referee-sh-0009` — a raw timestamp the viewer did not write and does not rewrite, next to the author it repeats. *Check:* `/session/s-2026-09-16-0001`, the first message. *Candidate fix:* the publisher strips a message's leading heading when it matches the message's own time and author, or the viewer drops a first line that does. *Phase 5:* fixed in loom: the heading gives the time and stays in the body only where it says more (DR-257-ikmartin).

**G14 · P4 · gap · the digest opens on an explanation.** A work's `Digest` view opens with `Proposed — 1 statement nobody has vouched for` and a paragraph explaining what a proposal is — documentation inside the view, the fault S9 deleted from the panel. *Check:* `/library/Bellamy19?view=digest`, the first screen. *Candidate fix:* the heading alone; the explanation is the book's. *Phase 5:* fixed: `Proposed` and its count; the explanation is the book's, 8.14 (DR-258-ikmartin).

**G15 · P4 · question · the Discussion Pane setting.** `Discussion Pane` now decides only which side a new second pane takes when the rail opens a discussion; the spec expected it to be deprecated. Is it a question a reader asks? *Check:* Settings ▸ Discussion Pane ▸ Left, then with one pane open `open session discussion` — the discussion opens on the left; nothing else changes. *Candidate fix:* retire it, and open the discussion on the right like every other second pane. *Phase 4:* fixed: the setting is retired (DR-252-ikmartin).

**G16 · P4 · gap · what a session touched is named by key.** The session's chips and its `What it touched` name raw keys — `sh-0009#eq:rank`, `Arden24-prop-2.1`, `sh-000C/proof` — where a reader knows the results by their taxon and number. *Check:* `/session/s-2026-09-16-0001?view=did`, `What it touched`. *Candidate fix:* chips named as tabs are (`Theorem 3.1`, `Arden24 · Prop 2.1`), the key in the chip's title. *Phase 5:* fixed: every target named as a reader names it, the key as its title; equations and cited works open their items (DR-256-ikmartin).

**G17 · P1, P4 · gap · the context against its design.** The design image lists `IN`, `USED BY` and `DISCUSSIONS` first and a small graph last; the running context leads with a 200px graph, heads each document by path (`IN DRAFTING/MAIN-ATOMIC.TEX`, then `document › The rank of…`) and names results by raw key (`sh-000A/proof proof`). *Check:* `/node/sh-0009`, `context`. *Candidate fix:* the lists first, each document by its file name, results named as tabs name them, and the graph last and smaller. Found while testing C7, 2026-09-22. *Phase 5:* fixed: lists first, documents by file, results by name, the graph last at 150px (DR-255-ikmartin).

**G18 · P1 · gap · `show all annotations` with floating cards.** With the default floating placement, opening every annotation stands every card over the text and over one another until neither is readable; a node now depends on this control to show all its annotations. *Check:* `/master/main-atomic`, `show all annotations`. *Candidate fix:* opening all lays the cards in the flow, as `inline` does, or in a column beside the text. Found while testing C10, 2026-09-22. *Phase 5:* fixed: `show all` and `e` open every box in the flow; a single click keeps the placement (DR-260-ikmartin).

After phase 4 (2026-09-22): G1, G3–G7, G9, G10 and G15 are fixed, G11 is discarded, and G2, G8, G12–G14, G16, G17 and G18 stay open. After phase 5 (2026-09-22) every gap is fixed or discarded.

Confirmed working, and not gaps: the side panel's single scroll with the pinned footer; the picker's rows and `select none`; the annotation filter alone at the rail's left; the lone tab offering no controls; a panel click never splitting; a link opening beside and keeping its source; an item opened once; the preview's card against the window, its `open here`, and a document link previewing nothing; the ledger's one question per column; the panel's Library carrying only the filed dot; the discarded line appearing only when there is something to show; `What it did` omitting its report where none was written.
---

## Delivery

Five phases: three building the design, a fourth fixing what human testing of the first three confirmed, and a fifth closing the gaps the fourth left. Each gets its own plan-mode plan before implementation; the full code specification belongs there, not here.

### Phase 1 · The side panel (done)

Done 2026-09-22 to its plan (the phase plans, at the end of this document); recorded in DR-229-ikmartin and DR-230-ikmartin.


Self-contained, independently valuable, and it builds the shared surface the rest uses. `Popover` extracted; `SessionFooter` added; `SessionPicker` becomes the popover's body; `IconStrip` loses the session section, the explanatory paragraph and the counts line; contents open by default; the annotation filter moves to the existing `ReadingRail`.

Touches: `sessions/`, `shell/IconStrip.svelte`, `shell/ReadingRail.svelte`, and the six existing popover call sites.

### Phase 2 · The workspace (done)

Done 2026-09-22 to its plan (the phase plans, at the end of this document); recorded in DR-231-ikmartin to DR-237-ikmartin. It also shipped the `node` and `context` kinds, deleted `beside=0` and `Fragment`'s `content-head`, and left `Split` and `Tabs` standing for the thread page alone.


The large one. `Item`, the registry, `Workspace`, `Pane`, `PaneHead`, the global rail with the control cluster, focus and its rules. The five reading routes become item renderers. `Beside`, `Split`, `BesideToggle` and `Tabs` go; the four transitional props go; `ReadingRail` becomes the frame's rail; `?beside` becomes item addresses and the URL takes its new shape.

Ships with: documents, works and sessions as item kinds; tabs; the split; `open here` in the preview.

### Phase 3 · The remaining kinds, and the subtractions (done)

Done 2026-09-22 to its plan (the phase plans, at the end of this document); recorded in DR-238-ikmartin to DR-241-ikmartin. The two tests owed since the spec — a scroll inside the preview, and the preview landing on its mark — are in the reading suite.


The session's two internal views, retiring the thread route's page and with it `Split` and `Tabs`, which only `SplitView` still uses. `/library` leaves the workspace and renders the ledger. `LinkPreview` moves onto the registry. The node's annotation filter row goes (N4). The `node` and `context` kinds, and the deletions of `beside=0`, `rail.beside`, `Fragment`'s `content-head` and the library route's full-height rules, shipped in phase 2.

### Phase 4 · What human testing confirmed (done)

Done 2026-09-22 to its plan (the phase plans, at the end of this document); recorded in DR-242-ikmartin to DR-254-ikmartin. The candidates C1–C10 were tested in the showcase and demoed before and after; the author approved C2–C9, discarded C10, revised C1 (no annotation section on a node at all) and asked for writing to work on nodes and documents as it does on a paper, then added seven changes: the rail matched to its mockup, a work's tools greyed rather than hidden, no focus line, a one-line divider, choosing a session opening its discussion, marks that do not break MathJax, and underlines instead of the block bar.

The node became its statement and proofs, with its provenance, closure, citation suggestions and discarded annotations moved to its context; the select and box tools write on nodes and documents, with loom matching a selection's quote against a projection of the source; loom numbers each document from its own compile and never puts a mark inside a formula; tabs, narrow windows, the session selection, the local graph, a context's links and the Discussion Pane setting as the gaps section records.

### Phase 5 · The remaining gaps (done)

Done 2026-09-22 to its plan (the phase plans, at the end of this document); recorded in DR-255-ikmartin to DR-261-ikmartin. The eight gaps phase 4 left open (G2, G8, G12–G14, G16–G18), and two the author added: the divider drawn at 2px, and a PDF page that showed "Cannot use the same canvas during multiple render() operations" — a page drawn again at a new zoom while its first draw was running, which now cancels the running draw and, where the zoom fits a column not yet measured, waits for it.

## What the book owes

The book records decisions, not truth, so the rules above are only half of the change. Nothing here ships until its record does.

### Decision records

- **Reading mode is items in panes.** A reading view is an addressable item with a renderer, controls and optional internal views; routes stop owning chrome. Carry the reason: five routes each invented a rail, so one question was answered in five layouts and no answer could be reused.
- **The panes are of equal rank.** Neither is primary, because *what I am working on* is a fact about the reader's intent rather than a property of a document, and a layout that encodes it asserts something only the reader knows. Record that ranked panes were built and rejected — the argument is the useful part.
- **A link opens beside; a panel click opens here.** With the corollary that nothing replaces, so there is no trail and no back affordance to maintain.
- **An item is opened once**, and an already-open target is revealed rather than duplicated.
- **The session list leaves the side panel**, with what a session is — a chat in its lifecycle, a unit of recorded work in its content — which is why the list is somewhere you go rather than something kept open.
- **A discussion is scoped by session, not by document.** This changes what the pane shows; it is not a rename.

### Chapters

- **15.8** describes the navigation shell and the panel as holding Sessions; **15.2** describes the shells. Both are rewritten around the frame, the workspace and the pinned footer.
- **15.3.1** describes the read view's rail and gutters; **15.3.2** the node page and its context rail, which becomes an item; **15.3.6** the hover preview, which gains `open here`; **15.5.1** the local graph, which moves into the context item.
- **10.4.1** describes links into a cited work: the locator keys stand, but what a link *does* changes.
- **15.7** gains **Show ids** and the renamed **Discussion Pane** row.
- Wherever the book says the split holds the discussion, it now holds an item.

### Specifications

- `?beside` changes from a boolean to a pair of item addresses; `specs/manifest.md` is unaffected, and the work-link keys of Part C — `page`, `span`, `box`, `annot`, `result` — stand unchanged.

### Deviations

One row each for: the panel's contents; the split's second pane; the node page losing its rail; the thread route losing its page; `/library` leaving reading mode.

---

## Known risks and open questions

Recorded so they are decided rather than discovered.

- **Tab width against real names.** 124px holds `Arden24 · Prop 2.1` and truncates `sketch-atomic.tex` at about twelve characters. The number wants choosing against real filenames and citekeys, and it may argue for a work's tab carrying its citekey rather than its title. Whatever is chosen, tabs stay a **fixed** width: the property that matters is that a target does not move.
- **This design is mouse-first, deliberately.** `⇄` and `×` are roughly 12px targets in a 30px head, under the 44px touch guidance, and the preview is hover-only and absent on touch. Acceptable for the two readers this is being built for; it is a choice, not an oversight, and a tablet reader would need a different answer for both.
- **Narrow windows are unspecified.** The panel already collapses. What two panes do below roughly 760px — collapse to one, or scroll — has not been decided. A work needs ~420px for its control cluster, which sets the floor on the divider but not on the window.
- **The URL's encoding is unresolved.** Two item addresses, each of which may carry its own locator keys, must survive round-tripping, and the arrangement must stay linkable — that property exists today and is worth keeping. Probably one opaque encoded parameter rather than nested query strings. A phase-2 decision, but the constraint belongs here.
- **The annotation filter moves twice.** Phase 1 puts it in the existing `ReadingRail`; phase 2 replaces that rail with the frame's. This is deliberate — it keeps phase 1 independently shippable — and phase 2's planner should expect it rather than treat it as a mistake.
- **`LinkPreview` is already `position: fixed` at `z-index: 60`.** H2 is a constraint to preserve, not a change to make.

---

## Behaviour that must survive

Fine-grained behaviour is mostly unwritten here, and mostly that is fine: it lives in code this plan does not touch. What matters is behaviour that is unwritten **and** owned by a component this plan rewrites or moves — that is what a rebuild loses silently.

Where a test already guards a behaviour, the test is its specification and is cited rather than paraphrased; restating it here would be a second copy to drift. Where nothing guards it, a test is owed.

| behaviour | owned by | moved or rewritten in | guarded by |
| --- | --- | --- | --- |
| the divider drags, snaps at the middle, resets on double-click, nudges by key, and collapses | `split/Split.svelte` | phase 2, deleted | test of that name |
| clicking away closes an open annotation box rather than backgrounding it | `fragments/expand.ts` | phase 2 | `inline, a mark expands its comment…` |
| a mark opens its box on click; hovering never does | `fragments/mount.ts` | phase 2 | `a mark opens a box over the page…` |
| selecting text leaves the selection and offers to annotate it | `pdf/Reading.svelte` | phase 2, becomes an item | `selecting text leaves it selected…` |
| changing where comments stand re-wires without re-typesetting | `fragments/Fragment.svelte` | phase 2 | `changing the placement re-wires…` |
| the session view filters annotations rather than the list | `sessions/` | phase 1 | `one selection governs the page…` |
| the Nodes section folds and narrows by what is typed | `shell/IconStrip.svelte` | phase 1 | `the panel has a Nodes section…` |
| the contents bar follows the reader's scroll, marks exactly one entry, and compares on the published element id | `shell/IconStrip.svelte` | phase 1 | **DR-112 only** |
| a scroll *inside* the preview card does not dismiss it | `components/LinkPreview.svelte` | phase 3 | **nothing** |
| the preview waits for its mark to exist before scrolling to it, rather than landing at the top of the page | `pdf/PdfDoc.svelte` | phase 3 | **nothing** |
| renaming a session is immediate, not a round trip | `sessions/SessionPicker.svelte` | phase 1 | **nothing** |

The last four are the ones to watch. The preview's scroll exception in particular was a long bug hunt: the listener is capturing, so it hears the scroll a PDF preview makes when it scrolls its own column to the page it was asked for, and closed the card in the frame it opened. It is a comment in the file and nothing else.

### Behaviour not specified anywhere, and deliberately

- **No animation.** Tabs, panes and the preview appear and disappear without transition. This is a choice: motion in a reading tool draws the eye away from the text, and nothing here needs to explain itself by moving.
- **Hover opens only the preview.** No other surface in reading mode opens on hover; marks, tabs and panel rows all require a click. A surface a pointer summons cannot be read without holding the pointer still.

---

## Tests

Each principle gets assertions that **fail if it is violated**, with measured thresholds where we have them. Names are the e2e test names.

### P1 · Open on the thing itself

- `a document opens on the paper` — the first line of prose begins **≤ 100px** from the top of the content area, and the prose column is **≥ 45%** of the window's area on the first screen.
- `a work opens on the paper` — the rendered page occupies **≥ 70%** of the window.
- `a pane head carries tabs and nothing else` — the head's children are the tab strip alone.
- `no route draws a rail` — no element with a rail or toolbar role exists inside a pane's content.

### P2 · Say it once, in the place that governs it

- `the write target is stated once` — exactly one element in the document names the selected session.
- `annotations are filtered from one control` — exactly one control sets the session view.
- `a document is opened once` — opening an already-open item yields exactly one tab for it, and focuses the pane holding it.
- `a document's controls are drawn once` — with two panes each holding a work, exactly one zoom control exists.
- `the library list has one home` — the route renders no list of works.

### P3 · Claim only what is known

- `an unlocatable citation previews nothing` — hovering `[1, Lemma 5.9]` opens no card.
- `a citation with a page previews that page` — `Corollary 3.2, p.~2` opens at page 2.
- `a node in no document says nothing about it` — no `IN` section renders.
- `a lone tab offers no controls` — one pane, one tab: neither `⇄` nor `×`.
- `the contents tree is absent for a non-document` — focusing a pane holding a work removes the tree.
- `a refusal names its condition` — with no session selected, the composer is disabled and the sentence `writable()` returns is shown.

### P4 · Name the question. One element, one question

- `the global rail holds three things` — the filter, the cluster, the session discussion; nothing else.
- `every control names its target` — each control in the cluster has an accessible name identifying the item it acts on.
- `a control names the destination, not the state` — the annotations toggle reads `show all annotations` while closed; the payload toggle reads `rendered latex` while showing source.
- `the session picker is not in the column` — the panel's scrolling region contains no session list.

### P5 · Following a connection must not cost the thing you followed it from

- `a link opens beside and leaves its source rendered` — after clicking a citation in a pane, that pane still renders the document it was clicked in.
- `a panel click does not split` — with one pane open, clicking a document in the panel leaves one pane.
- `the preview is not clipped` — with the split open, the card's bounding box is not contained by either pane's.
- `open here and a click differ only in pane` — the same item, the same locator, the other destination.
- `focus follows interaction` — scrolling the unfocused pane, then using the cluster, acts on the pane that was scrolled.

### Behaviour carried over

Tests owed because the behaviour exists only as code and its component moves:

- `a scroll inside the preview does not dismiss it` — with a card open on a PDF, the renderer scrolling its own column to the requested page leaves the card open.
- `the preview lands on its mark` — a card opened at a result shows that result, not the top of its page.
- `the contents bar follows the reader` — scrolling a document moves the marked entry in the panel, and exactly one entry is marked.
- `a session rename shows at once` — the new name appears without waiting for a round trip.

### Regression

- the four existing suites pass: `npm run check`, `vitest`, `playwright test`, `test:write`, `test:reading`;
- `docs/work-queue/check.py` passes;
- no test asserts a behaviour this plan retires.

---

# 0.13.3 — the reading views, made to open on what the reader came for, tabs, design sketches

## Context

A revision session on reading mode: the views a reader is in while reading mathematics, which are `/master/[stem]` and `/canon/[stem]` (one component, two routes), `/node/[...key]`, `/library/[citekey]`, `/library`, `/thread/[id]`, and the `?beside=1` split that is a mode of the first three rather than a route of its own. The review page, the graph, the problems page and the home page are out of scope.

The audit measured each view at 1440×900 against the running showcase rather than reading it off the stylesheet. Every one carried the same 296px of left chrome — a 44px icon rail and a 252px side panel — so a fifth of the window was spent before a view drew anything of its own; and beyond that the views differed by a factor of six in how much of the remainder reached the reader. The library work page gave 73.5% of the window to the paper. The node page gave 12% of its own column to the result the reader had navigated to.

Three principles came out of the work and are what the rest of this plan is measured against.

**P1 · Open on the thing itself.** The first screenful belongs to what the reader came for; what the application has to say *about* that thing goes to an edge or behind a tab.

**P2 · Say it once, in the place that governs it.** Where two surfaces state the same fact, one is deleted rather than restyled, and the survivor is whichever one controls the fact.

**P3 · Claim only what is known.** Where the viewer does not know something it shows nothing rather than something plausible, and a marker says the one thing that cannot be inferred and no more.

---

## Part A — the document view (done)

**Before**, six bands stood between the top of the window and the document's own title, totalling 334px: a full-width band whose only content was one right-aligned link; an "About this document" block; a `Comment` button; the source path in faint monospace; and a row of annotation controls beside a count of them.

![the document view before](images/0.13.3-view1-document-before.png)

**After**, one 36px rail, flush to the top edge, and the paper begins at y=65. Its share of the first screen goes from 32% to 47%.

![the document view now](images/0.13.3-view1-document.png)

- **The document-level annotation block is gone.** An annotation is defined by its anchor; one whose anchor is "the whole document" renders at the top, which reads as a comment on the title. The capability stays — `loom comment` has always taken a master path — and the discussion pane already lists those annotations, so nothing became unreachable. The `Comment` button went with it: nobody wants to comment on a document, they want to comment on a selection, which is what the annotate chip is for.
- **The counts line is gone.** `6 hidden by the session filter` was the only place a reader learned the filter was concealing something, which is why it could not have been deleted six weeks ago. It can be deleted now because the panel's `showing [current | all] session(s)` toggle states it continuously, in the place that controls it (P2).
- **`show all annotations` / `hide all annotations`** stands in the rail with a drawn chevron, present only when the document has annotations. `e` and `h` still work.
- **The PDF link stays**, as a plain blue link with no icon: it is the cheap escape from a manual latexmk, and it had been styled as punctuation appended to a file path.
- **The left gutter stays.** It holds each result's id and state, which is content, not decoration — but it is now off by default behind **Settings ▸ Show ids**, because a reader reading the paper does not need every key beside it and the reader who does is working on the corpus rather than reading it.

Canon takes the same rail, keeping `landmark @3 (flows-v2)` — a landmark's step is its identity rather than a note about it — and dropping the path and the commit message.

## Part B — the library work view (done)

The tabs, the reading controls and the split control are one line of 38px, where three bands took about 92px.

![the library rail](images/0.13.3-view3-rail.png)

- **The page and the zoom are typed into**, as a desktop viewer's are: `+` twelve times is not how a reader reaches page 12. Enter commits, Escape restores, out-of-range clamps.
- **The work's title left the rail.** It is on the paper, and the entry is behind *Info*.
- **`open split`** is the control's name in every view now, rather than one phrasing per page.

Two faults found by exercising it rather than by reading it: typing `150` over `140` yielded `500%`, because a derived `value=` and a focus handler that rewrote the text as it selected it were both writing the field, so the digits appended and clamped to the maximum; and Escape committed, because leaving a box commits it and cancelling dropped focus. Both now have tests naming the symptom.

![the library work view](images/0.13.3-view3-library.png)

## Part C — what a citation opens (done)

`[1, Proposition 2.1]` names a proposition in a paper, so where a copy is filed the link opens **the paper at that result** rather than loom's transcription of it. The digest node's page renders the record — the LaTeX, the provenance, what depends on it — which is a thing to go and look at and not what the citation names.

This needed a `result=` key in the work-link syntax. The result's rectangles are already in the work's sidecar under that id, so the link resolves with no publisher answering and moves when the result is re-anchored. The link ends `beside=0`: a work opened at a page opens split by default, because the link that form was built for is one written *in a discussion* (plan 0.13 item 6), and a citation came from no discussion and asks for no pane.

Where the reader lands, the place is marked by a translucent amber dot at the start of the first line, sized from the mark's own height so it holds at every zoom.

![the landing dot](images/0.13.3-landing-dot.png)

## Part D — the hover preview (done)

Three cases were collapsed into one. A citation with a digest node previews that result, cropped to the statement with two lines either side and scrolling when it is longer. A citation carrying a postnote that names a page — `Corollary 3.2, p.~2` — now opens at page 2, which the viewer had been discarding although the author wrote it down. A citation whose place cannot be found opens **nothing**: `[1, Lemma 5.9]` was opening a card on the front page of Arden24, title and abstract, as though that were the lemma (P3). A bare `\cite{Arden24}` still shows the front page, because that citation names the work rather than a place.

![the hover preview](images/0.13.3-hover-preview.png)

The card is the frame: the page runs to the rounded edge, with no inner border, and the page is drawn at 115% of the card's width, because a paper's margins are not what the reader hovered.

## Part E — a proposed text (done)

A suggestion's payload opens as **source**, because what is proposed is text to be written into a document and the thing a reader judges is the source, character for character. The control names what a click gives — `rendered latex` / `verbatim code` — rather than what is on screen.

![a proposed text, as source](images/0.13.3-payload-verbatim.png)
![the same, rendered](images/0.13.3-payload-rendered.png)

The mangled box had a cause worth recording: `SourceToggle` was mounted *inside* `.payload-head`, which carries `text-transform: uppercase`, so its `<pre>` inherited the uppercasing and its three buttons were squeezed into a flex row with no width, one letter per line. It was also showing the same text the block below already showed, which is why "rendered" was not rendered. The component was the wrong one for the job — the text is already in hand — and the payload now has its own toggle, with `TexProse` typesetting the math and turning `\emph`, `\textit`, `\textbf` and `\texttt` into markup. It is deliberately not a LaTeX engine: anything it does not know is left exactly as written, so a reader who sees a command still in its braces knows to read the verbatim instead.

## Part F — subtractions (done)

- **The annotation tick column**, from both surfaces (DR-227). Beside prose it repeated what the eye already had at the same scroll position, and it was the only layout in the reading view that could not be expressed in CSS — laid out in JavaScript against measured mark positions, recomputed after typesetting, after every write, and from a `ResizeObserver` on every reflow. Beside a page it stood 12px outside the page box and was the stray bar in the left margin of the Library view: a mark on nothing, which the reader took for a rendering fault.
- **`copy` and `copy for chat`**, and `forChat` with them. Selecting the text is how anyone copies text; the second wrapped it in a tag syntax nobody asked for; and three controls where one was wanted is what made the row unreadable when it was narrow.

## The settings panel

Every row is one segmented control rather than a row of pills — the options are the values of a single setting, and separate pills read as separate settings. `Panes` became **Discussion Pane** with `Left | Right` in that order, so the control is a picture of the choice rather than a list of it.

![the settings panel](images/0.13.3-settings.png)

---

## Part G — views 2, 4 and 5 (proposed)

Measured, and unreformed. Demos for each are on the canvas that accompanies this plan; what follows is the recommendation.

### View 2 · the node page

**Today.** Left chrome 296px, content 908px, right rail 236px. Nine stacked bands in the column, of which the statement is one at 856×69 — 12% of the 580px the page uses, with 39% of the column empty below the fold. Five sections in the rail, of which the top one is a 211×241 graph widget with Dot/Box toggles, and two exist to report *absences*: "in no document", "depends on nothing".

**Recommended: a rail that earns its width.** Keep a right rail, narrowed, carrying only facts that exist — what the node is in, what uses it, which discussion touched it — with the local graph behind a disclosure. The statement and its proof take the column; the annotations follow in the flow, without a three-select filter row above a single annotation.

A more radical option was considered and declined: make a node open **the document scrolled to it**, lit with the landing dot, since the node already exists inside the master and rendering it twice in two arrangements is the duplication P2 keeps deleting. It breaks on the case that most needs a page — an atomized node that no live document reaches has no document to open in — so the treatment is worth borrowing when the reader arrives *from* a document, and not worth betting the view on.

### View 4 · the library index

**Today.** A heading and a 238px table in a 900px window: 66% blank, listing the works the side panel is already listing. The page lost its reason to exist when library navigation moved into the panel, and nothing sends a reader here.

**Recommended: retire the page into the panel, and keep a ledger at the route.** The panel's Library section grows a filter, a filed/unfiled dot and a count of results used, and `/library` navigated to deliberately renders a working table: copy filed, digest size, results used here, proposals nobody has vouched for, unresolved findings, with a `needs work` filter. That table earns a page because it answers questions no other surface answers; the *list* does not, because the panel is the list.

### View 5 · the thread

**Today.** 52% of the window blank, a raw ISO timestamp, a `targets:` label with nothing after it, and no indication of what the discussion is about — a discussion is read beside the thing discussed, and on its own page it has no subject.

**Recommended: two arrangements, because there are two errands.** A session read on its own page is being *audited*: what it did in a sentence, the still-open findings as a scannable list carrying kind and target, what it touched, what settled, and the report, with the transcript as a second tab. A session read while working is a *conversation beside its subject*, which is what `open split` already gives. The transcript inside either takes a measured column with relative dates; the ISO timestamp should not survive in any arrangement.

---

## Verification

```
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```

Parts A–F are done and green: 100 unit, 128 e2e, 12 reading, 6 write. Four tests were rewritten rather than deleted where behaviour changed deliberately, and three were added for behaviour that had none — the citation's destination, the typed page and zoom, and the proposed text's two views.

## Documents

- **DR-227** — the margin tick column is retired from both surfaces, with the reason on each.
- **`docs/deviations.md`** — one row for the tick column.
- **Book 10.4** and **15.3.1/15.6** — corrected where they described the ticks as present.
- Still to record when Part G lands: what a node page is for, what `/library` is for, and that a session has two readings.

---

## Part H — the side panel (proposed)

### What is wrong with it

One 252px column, 17.5% of every window in every view, holding six lists — Documents, Contents, Nodes, Library, the Session list with its view toggle and closed section — plus a paragraph explaining what `N new` counts and a line reading `44 nodes · 1 errors · 4 warnings`.

Measured against the three principles it fails mostly on a fourth, which this revision session added:

**P4 · Name the question. One element, one question.** Every element exists because a reader asks something at that moment. If you cannot name the question in one short sentence, the element has no purpose and goes. If you name two, it is two elements. And if two elements answer the same question, that is P2 and one of them goes. Two riders make it usable: **a place is not an element** — a rail or a panel is a place that groups elements, and the test for a place is whether its elements' questions are asked at the same moment — and **the question must be the reader's, not the system's**, which is what condemns "in no document" and "depends on nothing".

The panel's six sections answer questions asked at six different moments. `Which document do I read?` is asked on arrival and rarely after; `where am I in it?` constantly while reading; `which session am I writing into?` when starting or switching work and never while reading a proof. Stacking them in one column guarantees that whichever is wanted is squeezed by five that are not — which is the overlap fault we treated with `flex: 0 0 auto` rather than curing.

One element is in the wrong place outright. `showing [current | all] session(s)` sits in the session list but answers *what am I looking at?*, not *which session am I in?*. It governs the document, so it belongs in the reading rail.

### Sessions are chats, not projects

There is exactly one quilt, so nothing here is a project selector: the project is the application. Sessions are many per quilt, cheap to make, casually named, closed rather than deleted, one selected at a time, and they own no content — the nodes exist without them. That is the chat model, and the navigation should be the chat model.

They are more than chats in one way that matters: a session scopes what is shown, carries a report and a pipeline, and an agent attaches to one independently of the author. That is why a session read on its own page wants view 5C — *what it did* — rather than a transcript.

The consequence is the design. In a chat application the list is primary because the chat *is* the content; here the document is the content and the session is a mode over it. So the session list is not a thing to keep open. It is a thing to go to.

---

## Part I — the reading workspace (proposed)

### What is wrong with it

The split exists so a reader can see a connection with both its ends on screen, and it can hold exactly one kind of second thing: `?beside` is a boolean and the right pane is always `Discussion`. A document cannot be put beside a draft. Two drafts cannot be compared. And following a citation out of a draft consumes the only pane there is, so the draft is gone — which is why the hover preview had to be built so elaborately in the first place: it is a mechanism for avoiding a navigation whose cost is too high.

Those are one fault seen twice. **The viewer has one place to put a text, so every navigation is a replacement, and a replacement destroys one end of the relation the corpus exists to record.**

**P5 · Following a connection must not cost the thing you followed it from.** The viewer's subject is how texts relate; a relation has two terms; a viewer that holds one term can only show a relation by destroying the other.

The rules this argument produced, the files they touch and the tests that hold them are specified at the head of this document.

---

# 0.13.3 — the phase plans

Each phase of the delivery was planned in plan mode before it was built, and each plan is kept here as it was approved, with a note at its head where the build differed from it. The design section above governs; these record how each phase was carried out.

## Phase 1 — the side panel

### Context

Plan 0.13.3 (first section, `docs/plans/0.13.3-reading-ui-improvements.md` §S1–S9, §Delivery Phase 1) redesigns reading mode in three phases. Phase 1 is the side panel: the session list leaves the scrolling column for a pinned footer that states the write target and opens a picker popover; the annotation filter moves out of the list to a rail above the content; the contents open by default; the explanatory paragraph and the counts line go; and one `Popover` component replaces six hand-rolled floating surfaces. It is self-contained and builds the `Popover` phases 2–3 reuse. The annotation filter will move a second time in phase 2 (into the frame's global rail) — expected, per the plan's risks section.

Decisions taken with the author (2026-09-22):
- **Filter home:** the layout draws one `ReadingRail` above the content on every reading route, filter at its left; pages register their own controls into it. No route loses the filter.
- **Row verbs:** ✎ ⏹ ✕ sit over the row's right end behind a gradient in the row's colour, shown on hover or keyboard focus only (the W4 pattern, amended by the author to leave the selected row plain at rest).
- **Leftovers kept quietly:** `display closed sessions` stays inside the picker's CLOSED fold; purpose / participants / attached go into the row's `title`, and `attached` also shows as a small marker.

Decisions I am taking (each small, stated so they can be overruled at review):
- `sessionView.pick` stops toggling; `select none` is the one detach path (`clear()`). A row click selects and closes the picker.
- **S6 + the refused mockup:** `NoSession` shows `writable()`'s sentence inline, permanently, beside the refused control, in warning ink; the control is truly `disabled`. The timed tip goes.
- **P2:** the discussion pane's `writing to <name>` line (`discussion-into`) is deleted; the footer is the one place that names the write target.
- **S9:** the `problems` view's icon moves to the strip's foot above settings, tinted red on errors / amber on warnings, with `N errors · M warnings` as its title and accessible name. The node count goes.
- **S2/S3, approximated until `workspace.current` exists:** the contents tree is open by default and drawn only on a document route (`/master`, `/canon`); elsewhere no tree.
- **Dead code:** `shell/RailSections.svelte` and `shell/DocumentPicker.svelte` are imported by nothing (shell A is retired); deleted, since the former imports `SessionPicker`.

### What the overleaf-inc merge changed (c2d63a5)

Luisa's branch touches none of the files Phase 1 rewrites (`IconStrip`, `sessions/`, `ReadingRail`, the popover sites), so the plan's structure stands. Five points of contact, each folded into the sections below:

1. **The master page gained an incoming comparison.** `?incoming=<key>` sets `hasComparison` and draws an `incoming-comparison` aside beside the document, parallel to the review comparison (`routes/master/[stem]/+page.svelte`). The move to `ReadingActs` must leave both asides, `hasComparison` and the citation-highlight effect untouched; `routes.e2e.ts` "…incoming…" (asserts `incoming-comparison`) is the guard.
2. **The review page was rewritten into three views** (All · Needs review · Incoming) and is the busiest `HelpDot` user (`help-review`, guarded by `routes.e2e.ts`). It is not reading mode, so it gets no `ReadingRail`; `HelpDot`'s move onto `Popover` must keep the panel opening below the title's `?` and keep both testids.
3. **Not every write is a session write.** `review-decision`, `review-finish` and `sync-incorporate` are outside `SESSIONED` (`src/lib/write.ts`), so a reader with no session selected can still record review decisions and incorporate a pull. The footer's refused tone must therefore say what it is about — annotations — rather than read as "nothing can be written": its accessible name is `annotations are written into <name>` / `annotations cannot be written: <writable() sentence>`, and nothing on the review page reads `writable()`.
4. **Decision-record ids** now carry the author (DR-228-ikmartin, committed 8ae8aa3): this phase's records are DR-229-ikmartin and DR-230-ikmartin, and the deviations row cites the full id.
5. **Baselines moved.** After the merge: 100 unit, 129 e2e, 6 write, 12 reading, all green; `home.e2e.ts` and `requests.e2e.ts` were rewritten around the review views but still hold the `counts` and session-selector assertions this plan updates.

### Work

#### 1 · `Popover` — `arras/src/lib/components/Popover.svelte` (new)

One component, two modes, both closing through the existing `dismiss` action (`src/lib/dismiss.ts`), no animation.
- **anchored** (default): props `trigger: Snippet<[{ open: boolean; toggle: () => void }]>`, `children`, `open` (`$bindable`), `placement: 'above' | 'below' | 'right'`, `align: 'start' | 'end'`, `width?: number`, `label`, `testid`. Wrapper holds trigger + body so `dismiss` containment works; the body is `position: fixed`, placed from the trigger's `getBoundingClientRect()` on open and on resize, clamped to the viewport. Fixed positioning is what lets the 320px picker escape the panel's `overflow: hidden`.
- **modal**: `modal` flag, no trigger; renders backdrop + centred dialog through `portal` (`src/lib/portal.ts`), `role="dialog" aria-modal`.

Migrate the six call sites, keeping every `data-testid`: `shell/Settings.svelte`, `shell/DevShelf.svelte`, `components/HelpDot.svelte` (anchored; used by the rewritten review page and `/problems`, placement below-start); `sessions/DeleteSession.svelte`, the expanded dialog in `graph/LocalGraphPanel.svelte`, `components/PdfViewer.svelte` (modal). Their per-site box styles (shadow, radius, border) move into `Popover`; content styles stay.

#### 2 · The rail above the content

- `shell/acts.svelte.ts` (new): registry `{ lead: Snippet | null; acts: Snippet | null }`, the same pattern as `shell/rail.svelte.ts`.
- `shell/ReadingActs.svelte` (new): a page's registrar, the `PageRail.svelte` pattern (set on mount, clear on leave), taking `lead` / `acts` snippets.
- `sessions/SessionFilter.svelte` (new): `showing annotations from [this session | all]`, one segmented control; `show-current` disabled with nothing selected; writes `sessionView.view` + `save()`. Logic lifted from `SessionPicker`'s `.showing` row.
- `shell/ReadingRail.svelte`: loses its props; renders `SessionFilter`, then `acts.lead`, then `acts.acts`.
- `routes/+layout.svelte`: renders `<ReadingRail />` above `children` when `page.route.id` is one of `/master/[stem]`, `/canon/[stem]`, `/node/[...key]`, `/library/[citekey]`, `/thread/[id]`. It stands outside `Beside`, as master's rail already does.
- `routes/master/[stem]/+page.svelte`, `routes/canon/[stem]/+page.svelte`: replace their `<ReadingRail>` with `<ReadingActs>` carrying the same snippets. On master, only the rail block changes: the `review-layout` grid, `hasComparison`, the `incoming-comparison` and `review-comparison` asides and the citation effect stay as merged.
- `routes/library/[citekey]/+page.svelte`: the `.top` band's contents move into `<ReadingActs>` (`Tabs` as lead; `PdfTools` + `BesideToggle` as acts) so a work does not carry two bands; `.page.reading`'s full-height calc subtracts the rail's 36px.

#### 3 · The session footer and the picker

- `sessions/SessionFooter.svelte` (new), pinned at the panel's foot outside `.sections`. A `Popover` (placement above, align start, width 320) whose trigger is one button: state dot, name, fresh count pill (`summary().fresh`), chevron. Tone from `writable()`: `''` → no fill, blue dot, ink name; otherwise amber fill, hollow ring, warning ink, name or `no session selected`. It carries only the state; its accessible name scopes that state to annotations (merge point 3), since review decisions and `sync-incorporate` do not need a session. `data-testid="session-footer"`.
- `sessions/SessionPicker.svelte` becomes the popover body (the `.showing` row, `.foot` paragraph and `DeleteSession` inline usage adjusted):
  - find field (narrows rows by title/purpose) + `+ new`, which swaps the field for the title input and calls `openSession` (`sessions/new.ts`); `session-new` / `session-new-title` testids move here from `IconStrip`.
  - `WRITING INTO`: open sessions via `grouped()` (already recent-first). Row: dot, name, `N open` / `nothing open`, `when(s.opened)` (`sessions/when.ts`). Click → `sessionView.select(id)` and close.
  - `CLOSED (n)`, folded (`show-closed`); inside it the `display closed sessions` control (`closed-no` / `closed-yes`) and the rows, each carrying `⟳` (reopen, then select — S8).
  - verbs ✎ ⏹ ✕ as an overlay (decision above); rename immediate by an optimistic local title shown until the manifest's title matches.
  - `select none` (`session-none`) → `sessionView.clear()`.
  - row `title` = purpose · participants · touched; `attached-{who}` marker kept.
- `sessions/sessions.svelte.ts`: `pick` → `select(id, m)` (no toggle, still admits closed); add `clear()`; header comment updated.
- `sessions/NoSession.svelte`: inline, persistent sentence while refused (`no-session-tip` testid kept); `say()` removed. Call sites `review/Composer.svelte`, `pdf/NoteAt.svelte` set `disabled` from `writable()`.
- `split/Discussion.svelte`: delete the `discussion-into` paragraph.

#### 4 · `IconStrip.svelte`

- Remove the Session list section, the naming state and `sessionsOpen`; render `SessionFooter` after `.sections`.
- Remove the `.counts` line; the `problems` view renders at the foot with the tone and title above (`data-testid="problems-glyph"`). `ShellProps.counts` becomes `{ errors, warnings }`.
- `contentsOpen = $state(true)`; the disclosure and tree appear only when the layout says the route is a document (new `ShellProps.reading: boolean`).
- Delete `RailSections.svelte`, `DocumentPicker.svelte`.

### Tests

Existing tests updated, not deleted, where behaviour moved: the "select a session" helpers in `tests/e2e-reading/reading.e2e.ts`, `tests/e2e-write/write.e2e.ts`, `tests/e2e-write/shots.e2e.ts` open the footer first; `requests.e2e.ts` "one selection governs the page…" and the two `discussion-into` assertions read the footer; `routes.e2e.ts` opens the picker before `show-closed`; `shell.e2e.ts` and `home.e2e.ts` `counts` → `problems-glyph`, and "the side panel scrolls…" asserts the footer is in the viewport instead of `session-list`; `tests/unit/sessions.spec.ts` "deselects when picked again" → `clear()`.

Guards from the merge that must stay green untouched: `routes.e2e.ts` incoming comparison on master, the review views and guided review, and `help-review`.

New e2e, named as in the plan's Tests section:
- `the write target is stated once`
- `annotations are filtered from one control`
- `a refusal names its condition` (and the footer turns amber)
- `the session picker is not in the column`
- `the contents bar follows the reader` (owed, DR-112 only)
- `a session rename shows at once` (owed)
- `⟳ reopens and selects`
- `a review decision needs no session` — with no session selected and a write API mocked, the review page's decision control is enabled while the footer is amber (merge point 3)

### Book

- **DR-229-ikmartin · The session list leaves the side panel** — what a session is (a chat in its lifecycle, a unit of recorded work in its content), so the list is somewhere you go; the footer states the write target; the filter governs documents and so leaves the list; `select none` replaces pick-to-deselect.
- **DR-230-ikmartin · A refusal is stated beside the refused control** — supersedes plan 0.13.1's on-demand tip, with the reason.
- **15.2.3** second paragraph and the **15.3** side-panel paragraph (the one on "Sessions says what loom is writing to…") rewritten around the pinned footer, the picker, contents-open and the warning glyph; **15.8** component list gains `Popover`, `SessionFooter`, `SessionFilter`, `ReadingRail`, `ReadingActs`, loses `DocumentPicker`/`RailSections`. Phase 2 does the full 15.2/15.8 rewrite.
- **`docs/deviations.md`**: one row for the panel's contents, citing DR-229-ikmartin.
- Every record id written in this phase — the book, deviations, comments — uses the `DR-<n>-ikmartin` form (Appendix A format).
- Plan 0.13.3 Delivery: mark Phase 1 done.

### Verification

```
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```

Baseline before the phase: 100 unit, 129 e2e, 6 write, 12 reading. Then drive the showcase at 1440×900 (run skill) and compare against `images/0.13.3-panel-d.png`, `-picker.png`, `-refused.png`, `-loaded.png`; check a node page and a work page carry exactly one band above the content. Finally rebuild and vendor the bundle (`npm run build` + `loom/scripts/vendor_arras.py`), as the tree commits the vendored assets. Stop for review before Phase 2; no commit unless asked.

## Phase 2 — the workspace

### Context

Plan 0.13.3 (`docs/plans/0.13.3-reading-ui-improvements.md`, first section) makes reading mode **items in panes**. Phase 1 (committed fa3d769/5a7e758) rebuilt the side panel and put a shell-drawn rail above reading routes. Phase 2 is the large one: the `Item` abstraction and its registry, a `Workspace` of exactly two equal panes with tabs, focus, the global rail with the control cluster, and the URL. The reading routes stop being pages and become item renderers; `Beside`, `BesideToggle` and the four transitional props go. The result is that following a connection never costs the text it was followed from (P5), and a document's controls are drawn once (P2).

#### Decisions taken with the author (2026-09-22)

- **Nodes move into Phase 2.** The `node` and `context` kinds ship now: a node opens as a tab with no right rail, and its context (what it is in, depends on, is used by, discussions, local graph) is a `context` item opened beside it by the `context` control. This lets `Beside` go cleanly and every margin id, heading link and reference open in a pane. Phase 3 keeps: the session's two internal views, the thread page's retirement, `/library` as ledger, `LinkPreview` onto the registry, N4 (the node's annotation filter row).
- **URL = path + `?beside`.** The path is pane 0's active item in today's URL shapes (`/master/main#sy-0200`, `/library/Kre99?page=4`, `/node/sy-0003`), so every existing link still resolves. `?beside=` carries pane 1's active item as one `encodeURIComponent`'d item path (`beside=%2Flibrary%2FArden24%3Fpage%3D2`). Kinds without a page today get address-only routes: `/session/<id>` and `/context/<key…>` — stubs that exist only to be parsed, never pages (this reverses DR-226's session permalink in part, recorded).
- **The thread page stays a page** until Phase 3: `/thread/<id>` leaves the reading set, renders full width with no rail, and keeps its own `SplitView` — so `Split.svelte` and `Tabs.svelte` survive until Phase 3 for it alone.

#### Decisions I am taking (stated so they can be overruled at review)

- **Inactive tabs unmount.** Each open item keeps a small state object in the workspace (scroll offset, the work's `PdfView`, the node's `verbatim`, the document's `Annotations`), restored on activation. Keeping hidden renderers mounted would leave their document-level listeners live in the background.
- **History:** the workspace writes the URL with `replaceState`. Entering reading mode from another route is an ordinary navigation (back leaves reading mode); nothing inside the workspace replaces, so there is no trail to walk back (the DR on "a link opens beside" records this).
- **Arriving by URL** (load, back into reading mode, the palette, a link on home/graph/review): if the workspace is empty, the path item fills pane 0 and `?beside` pane 1, focus on pane 0; otherwise the path item opens in the focused pane (a choice of what to read, S4) and `?beside`, if present, in the other. The URL is then rewritten to its canonical form.
- **`Discussion Pane` (Left|Right, G2)** decides which side a new second pane stands on when `open session discussion` creates it; every other new pane opens on the right.
- **Narrow windows** keep the existing behaviour: below 700px the two panes become a switch (ported from `Split`), one pane shown at a time.
- **The work's divider floor (K5):** while either active item is a work, the ratio is clamped so each pane keeps ≥ 420px.
- **`beside=0`** (added to citation links in Part C) is deleted now, not in Phase 3: the parameter's meaning changes and a stale `0` would parse as an address.
- **`Fragment`'s `content-head`** goes with its `head` prop (the spec lists the prop in Phase 2, the row in Phase 3; they are one change). `e`/`h` remain the keyboard path.

### Architecture — new files under `arras/src/lib/workspace/`

- `item.ts` — `ItemKind = 'document' | 'work' | 'node' | 'context' | 'session'`; `Item { kind; id; view?; locator? }` (locator: `WorkLink` keys for a work, an anchor for a document or node); `itemKey(i) = kind + ':' + id` (view and locator are not identity, W8); `itemFromPath(m, path)` and `pathFor(m, item)` — the codec between an item and its route URL, reusing `masterUrl`/`canonUrl`/`nodeUrl`/`workUrl`/`keyFromParam` (`src/lib/nav.ts`) and `readKeys`/`placeQuery` (`src/lib/worklink.ts`; `placeQuery` learns to write `result`, which it drops today). A document's id is its path; master vs landmark comes from `m.canon` (as `docUrl` does). A work's internal view travels as `?view=digest|info` (paper omitted).
- `workspace.svelte.ts` — the singleton `Workspace`: `panes: [Pane, Pane?]` of `{ items: Item[]; active: string }`, `focus: 0 | 1`, `state: Map<itemKey, ItemState>`, `ratio` (read/written through `prefs.divider`). Methods `open(item, where: 'here' | 'beside')`, `reveal(item)`, `activate(pane, key)`, `move(pane, key)`, `close(pane, key)`, `setLocator(key, locator)`; getter `current`. `open` reveals an already-open item in whichever pane holds it (W8); moving or closing a pane's last item closes the pane (W6); focus follows a moved tab. `canonical(m)` → path + `?beside`; `apply(m, url)` for arrivals (rules above). Pure logic, unit-tested.
- `registry.ts` — one entry per kind: `{ tab(item, m): string; renderer; controls?; views?: {id,label}[]; state(): ItemState }`. Nothing else in the viewer enumerates kinds.
- `links.ts` — one capture-phase `click` listener on `document`, installed by the frame. Ignores modified/middle clicks, `target=_blank`, downloads, other origins, default-prevented events. A hash-only link inside a pane scrolls within that pane and updates the item's locator. A `cited:` link or a same-origin URL that `itemFromPath` recognises opens **beside** when the anchor is inside a pane (W7: its destination is a fact about the pane it lives in) and **here** (the focused pane) anywhere else — the side panel, the rail, a preview card. Anything else (graph, review, tags) navigates normally. `PdfViewer`'s own document click handler (`components/PdfViewer.svelte:26-47`) folds into this; the modal remains for a work with no local copy.
- Components: `Workspace.svelte` (the frame: one or two panes, the divider ported from `split/Split.svelte` — drag, snap at the middle, double-click reset, arrow-key nudge, fold chevrons, narrow switch, same testids `divider`/`fold-*`/`switch-*`), `Pane.svelte` (`data-pane={i}`; head + the active item's renderer; `pointerdown`/`focusin`/`scroll` in the capture phase set focus, W10; saves and restores scroll per item; the focused-pane mark when two are open — lift, white head, accent line, no dimming, W11), `PaneHead.svelte` (the tab strip alone, W2: 124px fixed tabs, horizontal scroll on hover with `scrollbar-gutter: stable`, W3; `⇄` and `×` overlaid at the tab's right behind a gradient in the tab's colour, always on the active tab and on hover/keyboard focus for the others, W4; neither on the lone tab of a lone pane, W5), `GlobalRail.svelte` (replaces `shell/ReadingRail.svelte`: `SessionFilter` left; the cluster for `workspace.current` — its internal views then its registry controls — middle; `open session discussion` right, gated on `writable()` and reading `select a session first` when refused, F5).

### The item kinds — renderers under `arras/src/lib/workspace/items/`

Each is the body of today's route with its chrome removed; controls move to a `*Controls.svelte` rendered only by the cluster, acting on the item's `ItemState`. Every control's accessible name names its item ("show all annotations in main.tex").

- **document** (`DocumentItem.svelte`, from `routes/master/[stem]` and `routes/canon/[stem]`): the measured column, gutter behind Show ids (D2, `prefs.ids`), the review and incoming comparisons exactly as merged (`?review`, `?cause`, `?incoming` stay pane-0 URL keys, since the review page links to them), the local-graph float. Controls: `annotations` (label names the destination, D3) and `PDF`. Tab: file name; a landmark's `name @step` (D1).
- **work** (`WorkItem.svelte`, from `routes/library/[citekey]`): views `Paper · Digest · Info` (paper first, K1) in the cluster, replacing `Tabs`; paper full bleed (K4). Controls: tool pair, fit, zoom, page, `PDF` (K2) — `PdfTools` bound to the item's `PdfView`. Tab: citekey, `· <result label>` when the locator names a result.
- **node** (`NodeItem.svelte`, from `routes/node/[...key]`): header, statement, proofs, annotations in the flow (N1); no rail (N2). Controls: `context` (opens the context item beside), `source` (the `verbatim` state that `SourceToggle` holds today), `annotations` (N3). Tab: `taxon number` or id.
- **context** (`ContextItem.svelte`, from the node page's `PageRail` block, lines ~156-236): one column — what it is in, depends on, used by, relations, discussions, detached comments, diagnostics, and the local graph (`LocalGraphPanel`) — with **absences omitted** (C2): no `in`/`depends on`/`used by` heading when empty. Tab: `context · id`.
- **session** (`SessionItem.svelte`, from `split/Discussion.svelte`): the discussion **scoped by session** (E4) — the annotations whose `run` is this session, each travelling to its mark in whichever pane holds it; `sessions/Stream.svelte` and `sessions/Composer.svelte` with `session={id}`. Tab: the session's title. (The two internal views are Phase 3.)

### Wiring and global state

- `routes/+layout.svelte`: in reading mode render `GlobalRail` + `Workspace` in place of `children`; call `workspace.apply` on URL changes it did not write; install `links.ts`. `READING` becomes the five item routes plus `/session/[id]` and `/context/[...key]`, minus `/thread/[id]`. `currentDoc` / `onDocument` come from `workspace.current` (S3: the tree follows the focused pane; no tree when `current` is not a document).
- Route files `routes/{master,canon,node,library/[citekey]}/+page.svelte` become empty stubs; new stubs `routes/session/[id]/+page.svelte`, `routes/context/[...key]/+page.svelte`.
- `reading.svelte.ts`: `followReading(root, ids)` and `followNodes(root)` listen to the pane's scroller and look ids up inside it (today they listen to `window`, which is already broken inside a split).
- `fragments/Fragment.svelte`: new `anchor` prop; an item renderer passes its locator and the fragment ignores `page.url.hash` (otherwise pane 1 would scroll to pane 0's hash); `scrollToHash` looks inside `root`. Delete `head` and `content-head`.
- `fragments/mount.ts`: `ann-<id>` lookups scoped to the enclosing `[data-pane]`; `atResult` drops `&beside=0`.
- `components/LinkPreview.svelte`: add `open here` (H6) — opens the target in the pane the hovered link is in, same item and locator a click would give (H7); position stays `fixed`, z 60 (H2).
- `Palette.svelte`: its `goto` becomes `workspace.open(item, 'here')` for item URLs.
- Delete: `split/Beside.svelte`, `split/BesideToggle.svelte`, `split/Discussion.svelte`, `shell/ReadingActs.svelte`, `shell/acts.svelte.ts`, `shell/ReadingRail.svelte` (→ `GlobalRail`), `rail.beside`, `Reading.toolbar`, `PdfDoc.toolbar` (`PdfViewer`'s modal draws `PdfTools` itself), `Fragment.head`. `Split.svelte` and `Tabs.svelte` stay for `SplitView` until Phase 3. The node route's `PageRail` use goes; `PageRail`/`rail.svelte.ts` stay, since `/graph` still registers a right rail. `routes.e2e.ts:590` (asserts no `content-head` on the document) keeps passing trivially once the row is gone.

### Tests

Existing tests updated where behaviour moved deliberately (rewritten, not deleted): the split tests in `requests.e2e.ts` ("the split as a mode of a route", "the divider drags…") become workspace tests on the same testids; `routes.e2e.ts` "a citation opens the cited paper…" loses `&beside=0`; `queue.e2e.ts` "a link in a comment lands in the Library View…, and back returns" becomes "…opens beside, and the node stays"; `panel.e2e.ts` loops over `?beside=` URLs in the new shape; `workbench.e2e.ts` landmark step asserted in the tab; `shots.e2e.ts` `beside-toggle` → `open session discussion`; `tests/e2e-reading` note-with-page assertion reads the session item.

Must stay green unchanged (spec "Behaviour that must survive"): the divider test; `inline, a mark expands its comment…`; `a mark opens a box over the page…`; `selecting text leaves it selected…`; `changing the placement re-wires…`.

New e2e (`tests/e2e/workspace.e2e.ts`), named as in the spec:
- P1 `a document opens on the paper` (first prose line ≤ 100px below the content area's top; prose ≥ 45% of the first screen), `a work opens on the paper` (page ≥ 70%), `a pane head carries tabs and nothing else`, `no route draws a rail`
- P2 `a document is opened once`, `a document's controls are drawn once`
- P3 `a lone tab offers no controls`, `the contents tree is absent for a non-document`, `a node in no document says nothing about it`
- P4 `the global rail holds three things`, `every control names its target`, `a control names the destination, not the state`
- P5 `a link opens beside and leaves its source rendered`, `a panel click does not split`, `the preview is not clipped`, `open here and a click differ only in pane`, `focus follows interaction`
- plus `the URL reproduces the arrangement` (load `/master/main?beside=…` → both panes, reload keeps them) and `⇄ moves a tab and focus follows it`.
- Unit: `item.ts` codec round-trips every kind with locators; `workspace.svelte.ts` open/reveal/move/close/canonical/apply.

### Book

- Decision records, in the `DR-<n>-ikmartin` form from DR-231: **Reading mode is items in panes** (five routes each invented a rail); **The panes are of equal rank** (record that ranked panes were built and rejected, with the argument); **A link opens beside; a panel click opens here** (nothing replaces, so no trail or back affordance); **An item is opened once**; **A discussion is scoped by session, not by document**; **The node and context kinds ship with the workspace** (why they moved from Phase 3); **The URL is the left pane's path plus the right pane's address** (and `/session`, `/context` as addresses, amending DR-226).
- Chapters: **15.2** and **15.8** rewritten around the frame, the workspace and the pinned footer (retiring the two-shells text); **15.3.1** read view, **15.3.2** node page → node item and context item, **15.3.6** `open here`, **15.5.1** the local graph in the context item, **10.4.1** what a link into a cited work does, **15.7** Discussion Pane's new meaning; every "the split holds the discussion" corrected. 15.3.7's stale "comment on a document as a whole" corrected while there.
- Specs: none change — no file under `docs/specs` describes `?beside` (checked), and the work-link keys stand; the URL shape is documented in 15.8.
- `docs/deviations.md`: rows for the split's second pane, the node page losing its rail, and the node/context kinds shipping early.
- Plan 0.13.3 Delivery: Phase 2 done; Phase 3's list amended (node/context moved out, `beside=0` and `content-head` already gone, `Split`/`Tabs` retire with the thread page).

### Order of work

1. `item.ts` + unit tests. 2. `workspace.svelte.ts` + unit tests. 3. Registry and the five renderers with their controls. 4. Frame: `Workspace`, `Pane`, `PaneHead`, `GlobalRail`. 5. Layout wiring, route stubs, URL sync, `links.ts`, palette, preview `open here`. 6. Global-state fixes (reading trackers, `Fragment` anchor, `mount.ts` scoping). 7. Deletions. 8. Test updates and new tests. 9. Book. 10. Screenshots against `images/0.13.3-workspace-{single,split,node,preview}.png`, rebuild and vendor.

### Verification

```
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```

Baseline: 100 unit, 138 e2e, 6 write, 12 reading. Then drive the showcase at 1440×900 against the four workspace mockups (single pane, split with a work, node beside its context, preview over both panes), and measure the P1 thresholds by hand once. Stop for review before Phase 3; no commit unless asked.

## Phase 3 — the remaining kinds, and the subtractions

### Context

Phases 1–2 rebuilt the side panel and made reading mode items in two panes (DR-229…237-ikmartin). Phase 3 finishes the spec (`docs/plans/0.13.3-reading-ui-improvements.md`, E1–E4, H1–H7, N4, View 4/5 of the brainstorm) with what is left after phase 2 pulled the node and context kinds forward: **the session's two internal views**, which retire the thread page and with it `SplitView`, `Split` and `Tabs`; **`/library` as a ledger** with the list's one home in the side panel; **the hover preview asking the registry** instead of keeping its own node and work branches; and **N4**, the node's filter row. The chat overhaul (`docs/plans/0.14-chat-overhaul.md`) is a separate, later plan that builds on the Discussion view made here.

#### Decisions taken with the author (2026-09-22)
- **Chat overhaul: separate, later.** Discussion is built as E2 specifies and nothing more.
- **Discarded annotations (N4):** the kind and author selects go; beneath a node's annotations a quiet `N discarded — show` line appears only when there are any, and reveals them in place.
- **The run page's extras:** the journal's messages join the Discussion transcript; the notation panel goes under the report in *What it did*; the run picker (and the `.other-run` dimming it drove, whose CSS never matched) is dropped.

#### Decisions I am taking
- **`/thread/<id>` stays a URL, as a session address.** `itemFromPath` reads `thread/<id>` as `{kind:'session', id}`; `threadUrl` returns the session address; the route becomes a stub in the reading set. Old links, the palette, the review page's run list and the context's "discussions" all land on the session item. A thread with no session row (a `run` or `comments` thread) is a session item too: its views read the thread.
- **Findings point across panes.** A finding in *What it did* opens its target beside — the run's document (`documentOf`, `src/lib/review/run.ts`) at the finding's key, else the node — and a mark's double-click already travels to `ann-<id>` (`mount.ts` `card()`), so the finding rows carry `id="ann-<id>"`: the two panes point at each other as the run page's did, through the workspace instead of a private split.
- **The ledger takes its filters into its own header** and registers no `PagePanel`, so on `/library` the side panel keeps its Documents, Nodes and Library sections. That is what makes the panel the list's one home (P2 `the library list has one home`: the route renders a ledger table, never a list of works).
- **Preview kinds:** `node` and `work` get small renders; a link to a document, context or session previews nothing (P3: no card beats a made-up one). The hover resolver goes through `itemFromPath`, which strips the base path, closing the gap where `resolve()` tested raw `/node/` and `/library/` paths.

### Principles review (the first draft of this plan, measured against P1–P5)

Each finding names the principle, the fault in the first draft, and the revision now written into the Work below.

| # | P | Fault in the first draft | Revision |
| --- | --- | --- | --- |
| 1 | P2 | Discussion was to open with a **title** and the targets under it (E2's wording), but the tab already names the session — phase 2 removed the title for exactly this reason. | No title. The target chips lead the column; the tab is the one place the session is named. P2 governs over E2's phrasing, recorded in the DR. |
| 2 | P2 | The side panel's Library rows were to carry a filed dot, a count of results used and `Np` — the same facts as the ledger's columns, twice. | The panel keeps the **filed dot only**: it is the one thing that predicts what a click gives (a paper or not) and cannot be inferred (P3's marker rule). Every count lives in the ledger, which governs them. The mockup's `5`/`2p` are dropped, recorded. |
| 3 | P2, P4 | The ledger kept all five `?show=` filters and added `needs-work`: `digested`, `held` and `unread` restate columns the table already shows, and `needs-work` contains `proposed`. | Filters are `all · needs work · proposed` (the last is DR-206's backlog). A column answers "which are filed / digested"; a filter answers "which need me". |
| 4 | P4 | *What it did* was to list the **run log's commands** under "what it touched". Which commands ran is the system's record, not a reader's question (P4's second rider). | "What it touched" is the keys and documents the session wrote to or annotated, as chips. The run log leaves the viewer; `loom session` keeps it. |
| 5 | P4 | The ledger kept the current page's **cited-by** list alongside **used here** — two answers to "does my text lean on this?". | One column, `used here`, a count; which results cite it is the work's own Digest view. |
| 6 | P3 | "What happened in a sentence" risked a sentence the record does not support — a verb the viewer would be inventing. | The sentence is composed only from recorded fields: the session's `purpose` when it has one; otherwise `{modes} on {targets}` from the pipeline, then `{n} findings, {k} open, {r} settled`, and `{rounds} rounds`. No other words are made up. |
| 7 | P3 | A thread with no session row (a `run` or `comments` record) would have shown a composer and a state it does not have. | It shows what the record holds: no composer, no state dot, and the views drop the sections with nothing to say (C2's rule, applied here). |
| 8 | P2 | `Stream` shows live `changed-` events for annotations, which the flow would also show as findings once the manifest refreshes — one event, twice. | Inside Discussion, `Stream` contributes **messages only**; annotation changes arrive as findings in the flow. |
| 9 | P5 | A Discussion row whose mark is not on screen did nothing (`travel` to an element that does not exist). | Travel when the mark is in a pane; otherwise open the finding's target **beside**, at its key. Same for *What it did*. |
| 10 | P4, P2 | Phase 2's preview footer carries `clicking the link opens it beside` beside `open here` — documentation inside a control, the fault S9 deleted from the panel. | The hint goes; `open here` alone. (A phase 2 surface; fixed here since phase 3 rewrites the preview.) |
| 11 | P2 | *What it did* lists open findings and then the report, which carries the same findings in its blocks. | Kept, as E3 specifies both — the list is the scannable index and the report the account — but a report block's findings are not re-listed beneath it; the list's rows link into the report's place. |

Where the plan is already in line with the principles and was not changed: the two views answer the two errands (P4, E1); the ledger's first screen is the table itself with its header on one line (P1); kinds with no small render preview nothing (P3); findings and targets open beside, never replacing the session (P5); the discarded line exists only when there is something to show (P3). The ledger itself sits outside reading mode (the spec's boundary), so following a work from it is navigation rather than a connection between two texts; the workspace keeps its state, and back returns to the ledger.

### Work

#### 1 · The session item — `arras/src/lib/workspace/items/`
Registry `views` for `session`: `Discussion · What it did` (E1), `?view=did` in the URL.
- `SessionDiscussion.svelte` (E2): measured column (`--measure`), **no title** (review 1): the targets lead, as chips, each a link and so opening beside. A **date gutter** with relative dates (`when()`, `src/lib/sessions/when.ts`) — no ISO timestamp anywhere. One flow sorted by time: `thread.messages` (the journal), the session's annotations (findings marked by a left edge in their kind's colour, `beside-<id>` testids kept; a row travels to its mark when it is in a pane, else opens its target beside — review 9), attachments as an entry, then `Stream` restricted to **messages** (review 8), with `when(e.when)`. `Composer` at the foot, only for a session that can be written to (review 7). Replaces today's `SessionItem.svelte` body.
- `SessionDid.svelte` (E3): **what happened**, one sentence composed only from recorded fields (review 6); **still open** — findings with `status==='open'`, kind, severity, target, via `findingsOf` (`run.ts`, filtered by status), each row `id="ann-<id>"`, opening its target beside and linking to its place in the report; **what it touched** — the keys and documents it wrote to or annotated, as chips, and no run log (review 4); **what settled** — resolved findings; **the report** — `pipeline[].fragment` via `<Fragment standalone>` with `wireReport`'s `[data-annotation-id]` click-to-open, its findings not re-listed (review 11), and the notation panel (`notationOf`/`ambiguous`) beneath it. A section with nothing in it is omitted. Every date through `when()`.
- `SessionItem.svelte` becomes the switch between the two by `item.view`. Tab name stays the session's title.
- Delete: `routes/thread/[id]` page body (stub instead), `lib/review/SplitView.svelte`, `lib/split/Split.svelte`, `lib/split/Tabs.svelte`; move `split.spec.ts`'s divider arithmetic check beside the workspace (`src/lib/workspace/divider.spec.ts`) or drop it if it only re-tests `prefs.coerce` (`prefs.spec.ts` covers it). `nav.ts` `threadUrl` → session address; `item.ts` reads `thread/` as a session.

#### 2 · `/library` as the ledger — `arras/src/routes/library/+page.svelte`
- Columns, one question each: work (title, citekey) — *which*; **filed** (dot; `artifacts.pdf`) — *can I read it here*; **digest** (results extracted, `—` when none) — *how much has been read off it*; **used here** (digest results in `reachedExternal(m)`) — *does my text lean on it*, replacing the cited-by list (review 5); **unvouched** (`proposed.nodes` with a record, as `WorkItem` counts them) — *what awaits my judgment*; **open findings** (`reading.open` for notes on its pages + open annotations on its digest nodes) — *what is unanswered on it*; links (`WorkLinks`) — *where else is it*.
- Filters in the page header, one line: **`all · needs work · proposed`** (review 3); `needs-work` is unvouched > 0 or open findings > 0; `show-proposed` kept. `?show=digested|held|unread` fall back to `all`. No `PagePanel`. Sorted: needs work first, then citekey.
- Side panel Library section (`shell/IconStrip.svelte`): every work (capped like Nodes, with a narrowing field `library-filter`), each row the title and a **filed dot** (filled/hollow) and nothing else (review 2); the `all N works` link becomes `ledger`. Counting lives in a new `src/lib/library.ts` (`ledgerRow(m, ref)`), used by the ledger and unit-tested.

#### 3 · The preview asks the registry — `arras/src/lib/components/LinkPreview.svelte`
- `Kind` (`workspace/registry.ts`) gains optional `preview?: Component<{ item: Item; onresize: () => void }>`; `NodePreview.svelte` (head, badge, fragment body with `wire`/`typeset`, section count) and `WorkPreview.svelte` (sidecar statement crop at `CARD_SCALE`, `locate` for span/quote, `PdfDoc` with `focus`, the amber dot H4) are lifted out of LinkPreview unchanged in behaviour.
- `LinkPreview` keeps the chrome: timings (H1), placement against the viewport (H2), dismissal including the scroll exception, `open here` (H6). Its `resolve()` becomes `hoverItem(a)` → `Item | null`: `data-preview-key` and `a.ref`/`span.cite` targets by key (the digest rule — a digest node with a filed paper and a located page becomes `{kind:'work', place:{page, result}}` — stays here, it is a hover decision), `cited:` via `itemForHref`, the postnote page fallback (H5), everything else via `itemFromPath`. No card when the item's kind has no `preview`, or when it is the active item of the link's own pane (replacing the pathname check).
- Remove the dead `ref` target, `bibText` and `WorkLinks` imports, and the footer's `clicking the link opens it beside` hint (review 10).

#### 4 · N4 — `arras/src/lib/components/AnnotationPanel.svelte`
Delete the filter row and `ui.kindFilter` / `ui.authorFilter` (only reader); `ui.showDiscarded` becomes the panel's own state behind `N discarded — show` (testid `show-discarded`), drawn only when N > 0.

### Tests
Rewritten where behaviour moved (not deleted): `routes.e2e.ts` "a run is read as the document beside the report" → the session's *What it did* with the report and notation; "the panes point at each other" → a finding opens the document beside at its mark, and the mark's double-click travels back to the finding; "a session that wrote no report keeps the list shape" → its *What it did* says so; "a run lists the notation…" → under the report; "marks and boxes… discarded hidden by default" → `show-discarded`; `queue.e2e.ts` backlog and library tests and `requests.e2e.ts` "the strip panel never repeats the strip" → filters in the ledger header; `routes.e2e.ts` "…the Library counts them" → the ledger's used-here column; `tests/shots/report.spec.ts` (already stale) → the session views; `store.spec.ts` `/thread/s-1` → a session item.

New e2e:
- P2 `the library list has one home` — `/library` renders no list of works (`main ul a[href*="/library/"]` = 0) and the panel's Library section is present beside it.
- `the ledger says what needs work` — `needs-work` narrows to works with unvouched results or open findings; the filter row holds exactly `all`, `needs work`, `proposed`.
- `a work's counts are stated once` — P2: the panel's Library rows carry no counts, only the filed dot.
- `a session reads as its discussion and as what it did` — the two views, the transcript's relative dates, no ISO timestamp in either (`/\d{4}-\d{2}-\d{2}T/` absent), no title repeating the tab, no run log.
- `what it did claims only what is recorded` — a thread with no report and no session row shows no report section, no composer and no state.
- `a finding opens its target beside` — P5 for the session, from both views, and a row whose mark is on screen travels instead.
- Carried over (owed since the spec): `a scroll inside the preview does not dismiss it`; `the preview lands on its mark` (`[data-testid="preview-page"] [data-testid="mark-_stmt"]` in view, not the page's top).
- `a link to a kind with no preview opens no card` — a document link.
- Unit: `library.ts` ledger rows; `item.ts` `thread/` → session; `LinkPreview`'s `hoverItem` rules where extractable.

### Book
- Decision records from DR-238-ikmartin: **A session has two readings** (E1–E4; the thread page retired into them; the run picker dropped with its reason; the journal into the transcript; no title since the tab names it; the run log leaves the viewer, a system's record rather than a reader's question); **`/library` is a ledger, and the list's home is the panel** (View 4; filters reduced to the three that answer "which need me"; the panel's rows carry the filed dot and no counts, which the ledger governs); **A hover card renders an item** (H1–H7 through the registry; unsupported kinds show nothing; the base-path gap closed; the footer's hint removed); **A node shows its annotations without a filter row** (N4; discarded behind a quiet line). Each record names the principle that decided it where the spec's wording and a principle disagreed.
- Chapters: **15.3.6 Reviewing a run** → the session's two views (and fix the duplicated 15.3.6/15.3.7 numbering in 15.3); **15.3.5** threads; **15.3.6 Hover previews**; **10.2.1** node annotations; **10.2.3** the Library and the ledger; **15.2.3** the panel's Library rows; **15.4** rails-by-view table (library row; drop the stale `references` row).
- `docs/deviations.md`: rows for the thread route losing its page and `/library` becoming a ledger.
- Plan 0.13.3 Delivery: Phase 3 done; the plan's owed-test list closed.

### Final step · Principles review of phases 1–3, recorded for a phase 4

After phase 3 is implemented and green, every surface the three phases built is measured against P1–P5, and the gaps and violations are written into **plan 0.13.3 itself, at the end of its design section** — a new `### Gaps after phases 1–3` after `### Settings` and before `## Delivery`. That section is the input to a phase 4, which will fix what human testing confirms; it records, it does not fix.

**Method.** Walk each surface against each principle, in the running showcase, not from memory: the frame and icon strip; the side panel (Documents, Nodes, Library, contents); the footer and the session picker; the global rail and each kind's cluster; panes, tabs, focus and the divider (wide, and below 700px); the document; the work (paper, digest, info); the node; the context; the session's two views; the ledger; the hover preview; settings and the popovers. Confirm or discard each candidate below, and add what the walk finds.

**Entry format**, one per gap: an id (`G1`…), the principle(s), the surface, what is wrong in one sentence and why it breaks the principle, **how to verify it in the showcase** (the URL, the action, what the tester should see if the gap is real), and a candidate fix for phase 4. Severity: *violation* (the principle is broken as written) or *gap* (the principle is met in part, or only in the common case).

**How a tester runs the showcase** (to be written at the head of the section): `cp -R demos/showcase "$TMPDIR/showcase"` and `uv run --project loom loom serve --quilt "$TMPDIR/showcase" --port 4190`, then `http://localhost:4190/` at a 1440×900 window. A copy, because serving writes into the quilt. The showcase holds four drafts (`main-atomic`, `sketch-atomic`, `sketch`, `talk`), two landmarks (`flows-v1`, `flows-v2`), a node no document reaches (`sh-0402`), works filed and not (`Arden24` and `Bellamy19` with papers and digests, `Bellamy19` with a proposal and three open notes; `Cortez07` unfiled; `Stacks` unreadable), two open sessions (`referee-sh-0009`, which has a report, and `survey-bellamy`) and two closed ones.

**Candidates already seen in phases 1–2**, to be confirmed or discarded by the walk:

| # | P | Surface | Candidate | Where to look in the showcase |
| --- | --- | --- | --- | --- |
| C1 | P1, N1 | node | The statement does not lead: a title, a meta row (taxon, id chip, badges, version, "not yet numbered", tags) stand above it, and proof states, the comment control, reference notes, "what this rests on" and "on this page" stand between it and its annotations. | `/node/sh-0009` at 1440×900: how far down the statement's first line starts, and whether the annotations are on the first screen. |
| C2 | P1, K4 | work in a half pane | At the remembered zoom a paper is wider than a half-width pane, so its right side needs a sideways scroll. | `/master/main-atomic`, click `[Arden24, Proposition 2.1]`: does the page fit its pane, or run past its right edge? |
| C3 | P3, W3 | tabs | 124px tabs cut identities short (`context · sh-`, `Arden24 · Pr`, `main-atomi`). A cut name is a plausible name, not the name. | Open a node's context and a citation beside `main-atomic`; read the tabs. |
| C4 | P4 | document cluster | `not yet numbered` stands in the rail as text with no action: it answers the build's question, not the reader's. | `/master/main-atomic`, the rail's middle. |
| C5 | P1, P4 | global rail, narrow window | Below about 1000px the rail clips its overflow, so `open session discussion` and the cluster's tail disappear without saying so. | Narrow the window to 900px on `/library/Arden24`: are the page field and `PDF` still reachable? |
| C6 | P2, P3 | footer | The selected session is saved but never read back, so a reload says `no session selected` although one was chosen. | Choose `referee-sh-0009` in the picker, reload: what does the footer say? |
| C7 | P2 | document + context | With a node's context beside a document, the document's floating local graph and the context's graph can both be showing: one graph twice. | `/master/main-atomic`, open the local graph float, then open `sh-0009`'s context beside it. |
| C8 | P5 | context | A link in a node's context opens in the other pane, which is the node's own pane, so the node the context belongs to goes behind a tab. | `/node/sh-0009`, `context`, then a `depends on` link: is `sh-0009` still on screen? |
| C9 | P4 | settings | `Discussion Pane` now decides only which side a new second pane takes when the rail opens a discussion; the spec expected it to be deprecated. Is it a question a reader asks? | Settings ▸ Discussion Pane ▸ Left, then `open session discussion` with one pane open. |
| C10 | P2 | document | Each annotated result carries an `N comment` count beside its label and the marks in its text carry the same annotations. | `/master/main-atomic`, section 2's definitions. |

### Verification
```
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```
Baseline (phase 2, uncommitted): 114 unit, 158 e2e, 6 write, 12 reading. Then drive the showcase at 1440×900: a session's two views beside a document, a finding opening its document, the ledger with `needs work`, the panel's Library rows, and a preview over a citation and over a node reference. Rebuild and vendor. Then the final step: the principles walk, and the `### Gaps after phases 1–3` section written into plan 0.13.3, every entry carrying its showcase check. `check.py` again after writing it. Stop for review; no commit unless asked.

## Phase 4: the fixes human testing confirmed, and the author's additions

**Done 2026-09-22**, recorded in DR-242-ikmartin to DR-254-ikmartin. Where the build differed from this plan:

- **The quote builder's math substitution** is tested end to end in the write suite, not as a unit test, because the unit project runs without a DOM.
- **Found while building, and fixed:**
  - A labelled proof's own page carried none of its marks: loom put them only under its statement.
  - Opening a second pane re-created the first, which lost a note half written there.
  - A comment count ignored the session filter once the list that used to follow it was gone.
  - A quote carrying `\ref{}` rendered as `???`, because MathJax reads `\ref` in text.
- **Outside reading mode**, choosing a session only selects it: opening its discussion would have navigated away from the review page.

### Context

Phase 3 ended with a principles review. The ten candidates C1–C10 were then tested in the showcase, and before/after demos were published (artifact "0.13.3 phase 4 candidates"). The author approved C2–C9 as demoed, accepted C10's discard, and revised two things:

- **C1:** a node shows no annotation section at all. Clicking a mark opens its card; an annotation section always open is noise.
- **Writing:** annotations are made on nodes and drafting documents the way they are on PDFs. The PDF pane's two tools (select and box) appear for node and document panes too.

Answers taken with the author:
- Discarded annotations and citation suggestions move **into the context** item.

The author then added seven changes to this phase:
- **A1:** the global rail matches the mockup (`~/Desktop/better-bar.png`), in particular the `this session | all` control.
- **A2:** a work's tools grey out on Digest and Info instead of disappearing.
- **A3:** no orange focused-pane line; the focused pane gets a slightly stronger shadow.
- **A4:** the split divider becomes one thin line, with no arrow buttons and no grip.
- **A5:** selecting a session opens its discussion.
- **A6:** annotations anchored to LaTeX no longer mangle MathJax.
- **A7:** no vertical-bar annotation style; everything is underlined (`~/Desktop/vertical-line-annotation.png`).

#### Decisions I am taking (stated so they can be corrected)
- **A5 does not take focus.** Selecting opens the session beside the focused pane, or reveals it, and focus stays where the reader was. The rail keeps showing the tools of what is being read.
  - `open session discussion` stays in the rail. It reopens the discussion once its tab is closed.
- **A7 covers fragments only.** The vertical bar exists only on HTML `.annotation-block`. PDF marks keep their tinted rectangles.
- **Numbers (C4) are fixed in the publisher**, which already renders one fragment per master and is the only layer holding per-master numbers and citation labels.
  - Node pages and hover previews keep the default document's numbers, as the book specifies.
- **Loom refuses text a reader selected across math or `\emph{}`**, because it matches quotes against the TeX source. It gains a projection fallback so a selection that reads naturally anchors (§3). Without this, "the same as PDFs" would fail on almost every mathematical sentence.

### Principles check of this plan

| # | P | Risk in this plan | How the plan meets it |
|---|---|---|---|
| 1 | P2 | Selecting a session opens its discussion, and the rail also offers `open session discussion`: two routes to one place. | They answer different moments: choosing where to write, and returning to a closed tab. The rail's control becomes a reveal when the tab is open. It is kept, and recorded as such. |
| 2 | P1 | A5 could push the reader's text aside on every selection. | It opens **beside** and never takes focus. With two panes open, it becomes a tab in the other pane. |
| 3 | P4 | Greyed tools (A2) are elements with no question on Digest and Info. | The author chose stability over absence. A disabled control says "this acts on the paper". P4 is met in the author's reading; recorded in the DR. |
| 4 | P3 | A selection that cannot be anchored would be written as if it could. | NoteAt shows loom's refusal verbatim and offers "note on the whole {name}". Nothing is written silently unanchored. |
| 5 | P2 | Tools in two places, rail and PDF modal. | One `ToolPair` component. The modal keeps its own PdfTools, as it is not reading mode. |
| 6 | P5 | Context links opening in the context's pane replace the context. | The context goes behind a tab in the same pane, and the node stays in view. |
| 7 | P3 | Loom could print a guessed number in an unnumbered document. | Numbers are per master; an unnumbered master prints none. A reference falls back to the target's title. |

### Work

#### 1 · Loom: numbers per document (C4)
- `loom/src/loom/render/fragments.py`: `master_fragment` records the master being rendered in the renderer's per-thread state (`self._local`, the same pattern as `_expanding`/`_sink`). `_numbers_for` and `_cite_labels_for` then return that master's tables (`plan.numbers[m]`, `plan.cite_labels[m]`) whenever it is set. Node fragments keep the default-master rule.
  - Tags, `data-number`, `.number` spans and reference text then follow the document being rendered. An uncompiled master shows none.
- `loom/src/loom/render/convert.py` `ref_html` (1057–1081): a node reference with no number prints the target's title when the build knows it, and the key otherwise. Equation references keep `(label)`.
- Tests in `loom/tests/unit/render/`: a second master numbered differently shows its own numbers; an unnumbered master shows none; a reference with no number shows the title.

#### 2 · Loom: marks that do not break math (A6), and marks for boxed equations
- `loom/src/loom/render/marks.py` `place_marks`:
  - Text inside `.math` elements is never a search piece.
  - When the one-piece search fails, match against a projection of the block. Each `span.math.inline` reads as `$tex$`, and other tags read as nothing.
  - On a hit, emit one `<mark>` around the whole range when its ends sit at the same depth. Otherwise emit one `<mark>` per text run, all with the same id.
  - A match touching inline math takes the whole `span.math` inside the mark.
  - A match inside a display formula marks the `div.math` as a block.
  - `annotation-block` stays only as a rare fallback.
- `loom/src/loom/render/build.py` `_marks_by_node`: an annotation whose target is a region key (`key#label`) and which has no quote gets a block mark on that region's display, from `RegionRec`'s offset. A boxed equation is then marked and clickable.
- Tests: extend `test_marks.py` (update its `annotation-block` crossing case, which now becomes marks) and `test_build.py`. Update `test_review_commands.py:695`'s mark count if it changes.

#### 3 · Loom: quotes a reader can select
- `loom/src/loom/records/selectors.py` `find_quote`: when no exact match is found, match against a projection of the node's TeX. In the projection:
  - `\(…\)`, `\[…\]`, `$…$` and `$$…$$` are normalised to one delimiter;
  - `\emph{…}`, `\textit{…}`, `\textbf{…}` and `\texttt{…}` are unwrapped;
  - whitespace is collapsed.
  
  An offset map sends the match back to the source, and the recorded `exact` is the source slice. Tests in `loom/tests/unit/records/`.

#### 4 · The node (C1)
- `arras/src/lib/workspace/items/NodeItem.svelte` renders only three things:
  - The fragment: statement and proofs.
    - It is wrapped in `gutters-host > gutters > column` with `margins`, so **Settings ▸ Show ids** puts the id and state in the gutter, as in documents.
    - `No proof is attached.` follows the fragment.
    - The `conflicted` notice stays.
  - `On this page` for a section node.
  - The verbatim source, when toggled.
- It no longer renders:
  - the header (h1, taxon, `IdChip`, badges, version, `not yet numbered`, tags, digest line);
  - `.proof-states`;
  - `review/Composer.svelte`;
  - `ReferenceNotes`;
  - the closure details;
  - `AnnotationPanel`.
- `arras/src/lib/workspace/items/ContextItem.svelte` gains:
  - **Provenance:** the digest source ("from Arden24, Prop 2.1, p. 1" via `Locator`), `versionLabel` for the node and its proofs, and tags.
  - **Rests on:** `ClosurePanel` behind a disclosure, beside `depends on`.
  - **Suggested citations:** `ReferenceNotes`, moved.
  - **`N discarded — show`:** discarded annotations as `AnnotationBox`es, drawn only when there are any.
  
  Absences stay omitted (C2).
- `arras/src/lib/fragments/Fragment.svelte`: annotations are counted as `ready` when there are marks **or** comment-count buttons. Otherwise a node whose only comments are unmarked would lose the rail's `show all annotations`.
- Delete, since each is now dead:
  - `IdChip.svelte`
  - `AnnotationPanel.svelte`
  - `review/Composer.svelte`, replaced by §5
  - `showDiscarded` remnants

#### 5 · Writing on nodes and documents: the select and box tools
- Split the tool pair out of `arras/src/lib/pdf/PdfTools.svelte:62-81` into `pdf/ToolPair.svelte` (`holder: { tool: Tool }`, `of`). PdfTools uses it, so the test ids are unchanged.
- `NodeState` and `DocumentState` (`workspace/state.svelte.ts`) gain `tool`. `NodeControls` and `DocumentControls` render `ToolPair` when `can('comment')`.
- New `arras/src/lib/fragments/FragmentNotes.svelte` wraps the Fragment in `NodeItem` and `DocumentItem`, following `pdf/Reading.svelte:48-67, 206-241`:
  - **Select tool:**
    - On mouseup inside the fragment, the offer chip appears (`annotate-offer`, the PDF chip's markup).
    - The target is the closest `div.env[data-key]`, `details.env-proof[data-key]`, `section[data-key]` or `section[data-id]`. It falls back to the node key or the master path.
    - The quote is built from the selection. Each `.math` element it touches contributes `$tex$`, from a `data-tex` attribute recorded in `Fragment` before typesetting.
  - **Box tool, or Alt-drag:**
    - An overlay rectangle is drawn.
    - The target is a labelled display under the rectangle (region key `closestKey#label`, no quote). Failing that, the smallest `[data-src]` block under it, with its projected text as the quote. Failing that, the enclosing key.
- `arras/src/lib/pdf/NoteAt.svelte` gains a key mode: an optional `target` prop, with `citekey` and `page` optional.
  - It skips `locate`.
  - `where` names the key: the tab name of the node, or "equation (n) in …".
  - It writes `write('comment', {target, quote?, message, kind, severity})`.
  - A refusal is shown in `note-said`, with `note on the whole {name}`, which retries without the quote.
  - Its no-session refusal is the same as for PDFs.
- `AnnotationBox.svelte:46`, `SessionDiscussion.svelte:74` and `ContextItem.svelte:129` render the quote with `TexProse`, so `$c$` typesets.

#### 6 · Underlines, not bars (A7)
- `arras/src/lib/theme.css:980-983`: `.annotation-block.k-*` loses its inset left bar and gains `text-decoration: underline 2px <kind colour>; text-underline-offset: .2em; text-decoration-skip-ink: none`, with the kind's wash.
- A display-math block (`.math.display.annotation-block`) takes an inset bottom rule instead.
- `expanded` and `active` keep their outlines.
- Book 15.3.1 (line 88) and DR-114 are updated.

#### 7 · The work (C2, A2)
- `arras/src/lib/pdf/view.svelte.ts`: `PdfView` gains `fitText = true`, which `zoomTo` clears as it clears `fitWidth`.
- `arras/src/lib/pdf/PdfDoc.svelte:59-67`:
  - When `fitText` holds and the page at the reader's zoom is wider than the column, `drawAt` is the zoom that fits the page's **text block** into `columnWidth − 30` (an 18px landing margin plus a gutter).
  - The text block is `min x0`/`max x1` over `byPage` spans; with no spans, fit the page.
  - After focusing, the column scrolls to `text left − 18`.
- `arras/src/lib/pdf/PdfTools.svelte` gains `disabled` for every button and input.
- `WorkControls.svelte` always renders the tools for a known work, disabled off Paper or when the work is unreadable. `PDF` becomes a disabled span when there is no copy.
- The view check matches `WorkItem`'s own (`workViews`).

#### 8 · Tabs (C3)
- `PaneHead.svelte`:
  - `.tab` is 148px wide.
  - The label reserves the controls' width while they show (`.on`, `:hover` and `:focus-within` → `padding-right: 40px`), so a cut always ends in an ellipsis.
- `registry.ts`:
  - `shortLocator(locator)` drops the postnote page (`, p.~N`) and abbreviates the leading taxon (Thm, Lem, Prop, Cor, Def, Rem, Ex, Conj, Constr: loom's `ABBREV`, capitalised).
  - A work tab reads `Arden24 · Prop 2.1`, and a digest node's tab reads the same way.
  - The context kind gains `marker: { icon: 'graph', says: 'context of' }`. PaneHead draws the icon, and `title` and `aria` read "context of Theorem 3.1". `tab()` returns the node's name.

#### 9 · Narrow windows (C5)
- **Panel:** a `squeezed` value (window below 1200px) collapses the panel for display, without writing `prefs.panel`, in `IconStrip.svelte` via `class:away`. The fold button still opens it for the visit (`peek`).
- **Rail:** `GlobalRail.svelte` measures its width against its parts. When they do not fit, the filter and `open session discussion` move into a `⋯` Popover, and the cluster keeps the middle.
- **Workspace:** below the two-pane floor, `Workspace.svelte` drops `.switch`. One strip holds both panes' tabs (the unfocused pane's after a rule, muted), and a tab in it focuses its pane. `Pane` gains `head={false}`.

#### 10 · Rail style (A1)
- `theme.css` gains an `--accent` token (`#d97757`, and a dark value). `--annotation` fallbacks in chrome use it.
- `SessionFilter.svelte`:
  - The pill has a 10px radius.
  - The selected segment is `--link-wash` with `--link` text; the other is `--leaf`.
  - A hover rule is added, and the padding is `1px 9px`.
- `GlobalRail.svelte`:
  - The views get an accent underline and 12px gaps.
  - A 1px × 14px separator follows the views, as in the mockup.
  - The font size is 12.5px.
- `PdfTools.svelte`: `.of` uses `--ink-faint`, separators are 14px tall, `.tool.on` uses the accent tint, and the fields are filled as now.

#### 11 · Panes and the divider (A3, A4, C7, C8)
- `Pane.svelte:81-89`: the orange inset line on the focused pane's head is deleted. The focused shadow becomes `0 0 0 1px var(--rule), 0 6px 26px rgb(0 0 0 / 11%)`.
- `Workspace.svelte`:
  - The divider is a 7px transparent hit area drawing one 1px `--rule` line, darker on hover and drag.
  - `fold-0`/`fold-1`, `.grip` and `folded` are removed.
  - Drag, double-click reset and keys stay.
- **C7:** in `DocumentItem.svelte`, the float and its toggle are not drawn while a context is the active item of either pane. The float's stored preference is untouched.
- **C8:** in `links.ts` `interceptLinks`, when `workspace.active(from)?.kind === 'context'`, a link does `follow(item, from, 'here')`. A `clickWhere(from)` helper is exported. `LinkPreview`'s action becomes the destination a click does *not* give (`open here` or `open beside`), labelled to match.

#### 12 · Sessions (C6, C9, A5)
- **C6:** `+layout.svelte` `onMount` calls `sessionView.load()` beside `prefs.load()`. When the first manifest arrives, a stored selection that the manifest does not list is cleared. This check is not repeated, so a session just made by `+ new` survives.
- **C9:** remove `prefs.swap` in these places:
  - `DEFAULTS`, `coerce`, `data-swap`, the state, `current`, `load`;
  - the Settings row;
  - `links.ts`'s side argument;
  - `store.beside`'s `side` parameter and its left branch;
  - `prefs.spec.ts`.
- **A5:** `SessionPicker.svelte`'s `choose`, `start` and `reopen` call `openDiscussion(id)`, a new helper in `workspace/links.ts`. It opens the session beside the focused pane, or reveals it, and restores `workspace.focus`.
  - `GlobalRail`'s `discussion()` uses the same helper.
  - The hook stays out of `sessionView.select`, because of the unit tests and the import cycle noted in `new.ts`.

### Tests
- **Rewritten where behaviour moved:**
  - Node tests that assert the annotation list, `show-discarded`, closure, version, missing-proof and ReferenceNotes (`routes.e2e.ts`, `requests.e2e.ts:573`, `workbench.e2e.ts:73`, `queue.e2e.ts:37–76`) move to the context item, or open the mark first.
  - Tests waiting on `main h1` for a node wait on the node's fragment instead.
  - `write.e2e.ts` Composer tests become select-tool tests on `/node/sy-0003` and `/master/main`.
  - `panel.e2e.ts` "a refusal names its condition" moves to the select tool's refusal.
  - `panel.e2e.ts:33` counts the write target in the footer only, since the session tab is a different question.
  - `requests.e2e.ts:649` (divider) drops the fold assertions; its narrow part asserts the merged strip.
  - Tests of the switch and the swap setting are removed.
- **New e2e:**
  - a node draws no annotation section, its gutter shows id and state under Show ids, and its context carries provenance, rests-on, suggested citations and discarded;
  - the select and box tools on a node and on a document write a comment (write suite): a selection across `$c$`, a box on a labelled equation to its region key;
  - a mark around math typesets (no raw `\(`);
  - an annotation block is underlined, not barred;
  - the tools show disabled on Digest and Info;
  - a work in a half pane fits its text, and its dot is inside the pane (reading suite);
  - tabs name `Arden24 · Prop 2.1` and a context by glyph;
  - `talk.tex` shows no borrowed numbers (loom unit test, plus a reading e2e);
  - at 1100px the panel is collapsed and the rail's parts do not overlap (bounding boxes disjoint);
  - the selection survives a reload;
  - one local graph with a context open;
  - a context link keeps its node on screen;
  - no Discussion Pane row;
  - selecting a session opens it beside without moving focus;
  - the divider has no buttons;
  - the focused head has no inset line.
- **Unit tests:** `shortLocator`, per-master numbering, the marks projection, the `find_quote` projection, and the quote builder's math substitution (a pure function over a DOM fixture).

### Book and plan
- **Decision records from DR-242-ikmartin:**
  - A node draws its statement and proofs only; its annotations are reached by their marks; what it is about moves to its context (amends DR-236, DR-241, DR-160's placement).
  - Annotations are written with the same two tools on every reading item (retires the node Composer).
  - Marks never split math; blocks are underlined (amends DR-114).
  - Each document shows its own numbers.
  - A work opens fitted to its text in a narrow pane.
  - Tabs are 148px and name results by short taxon.
  - A narrow window collapses the panel first and compacts the rail.
  - The focused pane is marked by its shadow alone; the divider is a line.
  - A context's links open in its own pane (amends DR-233's W7).
  - One local graph on screen.
  - The Discussion Pane setting is retired.
  - Selecting a session opens its discussion.
  - A work's tools stay, disabled, off the paper.
  
  C6 is a bug and gets no record.
- **Chapters:**
  - 15: 15.2 frame, rail and settings; 15.3.1 the document and marks; 15.3.2 the node and context; 15.3.x tabs, divider and focus; 15.8 the component list without IdChip and Composer.
  - 10.2.1 and 10.4.
  - 09 numbering.
- **`docs/deviations.md`:** rows for the retired Composer and the moved node sections.
- **Plan 0.13.3:** Phase 4 under Delivery. The Gaps section marks G1, G3–G7, G9, G10 and G15 fixed and G11 discarded, and adds the two findings from the demos (the context's layout against the design; `show all` with floating cards). G2, G8, G12–G14 and G16 stay open.
- **This plan** is kept with the others at the end of plan 0.13.3.

### Verification
```
cd loom  && uv run pytest -q tests/unit/render tests/unit/records
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```
Baseline: 118 unit, 164 e2e, 6 write, 14 reading.

Then rebuild the showcase copy and walk each of the C1–C9 and A1–A7 checks at 1440×900, and at 1100 and 660 for C5, re-taking the demo's before/after shots as a record. Rebuild and vendor (`npm run build`, `uv run python scripts/vendor_arras.py ../arras/build`). Stop for review; no commit unless asked.

## Phase 5: the remaining gaps, a thicker divider, and the PDF render error

**Done 2026-09-22**, recorded in DR-255-ikmartin to DR-261-ikmartin. Where the build differed from this plan:

- **G13:** the heading's remainder is kept whenever it is not the session's own name, rather than trimmed of the agent's name word by word.
- **The render error** reproduced only with the CPU slowed. The reading-suite test slows it eightfold, and the phase 4 bundle showed the error there where this one does not.
- **Choosing a session now re-fits a paper** whose pane it narrows. The reading suite's note tests wait for that redraw before selecting.


### Context

Phase 4 left eight gaps open in plan 0.13.3's gaps section (G2, G8, G12–G14, G16–G18). The author asked for all of them to be fixed, and added two more changes:

- **The divider** should be twice as thick.
- **A render error** — "Cannot use the same canvas during multiple render() operations…" — shows at the top of a viewed PDF, for example `Cycle spaces of finite quivers`.

**The cause of the render error.** `PdfPage.svelte` `draw()` runs again whenever `scale` changes, and it never cancels the render already running on that canvas.

Phase 4's fit-to-text change made this common. `PdfDoc` first draws at the reader's zoom while the column's width is still 0, then redraws at the fitted zoom a frame later. pdf.js refuses the second render, and `draw()` shows its message as the page's problem.

### Principles check

| # | P | Risk | How the plan meets it |
|---|---|---|---|
| 1 | P2 | The rail's disabled control must still say something when it is refused | Its label stays `open session discussion`. The reason is the control's title, and the footer carries the state, which leaves one statement of the refusal on screen. |
| 2 | P3 | Folding the session's targets behind `and N more` could hide what the session was about | The documents always show. The count names what is folded, and it unfolds in place. |
| 3 | P3 | A shortened name could hide which key it is | Every name keeps the raw key as its title. |
| 4 | P1 | Moving the context's graph last makes it harder to find | The design puts it last. It keeps its expand control, and the document's float still answers "what is around here" while reading. |

### Work

#### G2 · The context reports absences
- `arras/src/lib/graph/LocalGraph.svelte:245`: remove the isolated-node caption, "nothing depends on this, and it depends on nothing". The lone node is drawn and nothing is said about it. `nothing to draw` stays.
- `arras/src/lib/workspace/items/ContextItem.svelte:48`: diagnostics leave out code `unreachable` (the codes are published bare, e.g. `unreachable`, and namespaced, e.g. `loom:missing-proof`, so match on the suffix). "In no document" belongs to the problems page. `missing-proof` and other diagnostics stay.

#### G17 · The context against its design
All in `ContextItem.svelte`:

- **Order:** `in` → `from` → `depends on` + `what this rests on` → `used by` → relations → discussions → detached comments → suggested citations → discarded → diagnostics → **the graph last**, at `height={150}` (`LocalGraphPanel`'s `height` prop exists; its default is 200).
- **`in`:** one `RailList label="in"`. Each master that reaches the node gets one row: its file name linked to the document at the node (`masterUrl` + the node's anchor), then `›` and the section crumbs. This replaces `in drafting/main-atomic.tex` and `document ›`.
- **Names:**
  - Results in every list use a new `nodeName(m, key)`, e.g. `Theorem 3.1`, `Arden24 · Prop 2.1`, `Lemma · Integral vertices…` when there is no number.
  - Proof keys are collapsed onto their owner in `collapse()` through `m.keys[k]?.node`, and a dependency that is only on a proof reads `proof of Lemma 1.2`.
  - The raw key is each link's title.
  - The faint kind after each row (`statement`, `proof`, `prose`) stays, since it says where in the node the use is.

#### G16 · A session's targets are named by key
- New `arras/src/lib/workspace/names.ts` exports `nodeName(m, id)` and `keyName(m, key)`, beside `shortLocator`:
  - a document path gives its file name;
  - a node gives `nodeName`;
  - a proof key or proof node gives `proof of <owner>`;
  - a region key (`m.regions`) gives `(3) in Theorem 3.1`, or `eq:rank in …` when unnumbered;
  - a cited work's source identifier gives its citekey (matched against `references[*].work` and `works`);
  - anything else stays the key.
- `items/session.ts`:
  - `targetName` becomes `keyName`.
  - `itemAt` opens a region key as its container node at the display's anchor, and a work identifier as the work item. Both land on "Unknown key" today.
- `SessionDiscussion.svelte` and `SessionDid.svelte`: chips, rows and step headers use `keyName`, with the key as the title.

#### G12 · The discussion's head
In `SessionDiscussion.svelte`, the target chips show:
1. the documents it touched;
2. the first four other targets;
3. an `and N more` link (`session-targets-more`) that unfolds the rest in place.

`What it did` keeps its full `What it touched` list, since that view is the audit.

#### G13 · Timestamps inside the journal
- `loom/src/loom/render/threads.py` `_messages` (lines 56–81): the message body no longer starts with `**{heading}**`.
  - What the heading says beyond its date and time and the session's agent name is kept as the first line, and only when there is something left (e.g. `referee of sy-0003` minus `referee` leaves `of sy-0003`, which is dropped as a fragment; a heading with a real title keeps it).
  - The time is still parsed from the heading.
- Refresh the vendored fixture with `docs/specs/tools/refresh-fixture.sh`.
- Loom test: a journal message's `body_html` does not contain its heading's date.

#### G14 · The digest opens on an explanation
- `arras/src/lib/workspace/items/WorkItem.svelte:105-116`:
  - The heading is `Proposed` with its count, muted.
  - The explanatory paragraph goes; each proposal already carries its `proposed` flag, and what a proposal is belongs in the book (10.2.3).
- The Library's title attribute keeps its phrase: it is a tooltip, not the first screen.

#### G8 · The refusal said three times
- `GlobalRail.svelte`:
  - `open session discussion` keeps its label.
  - It is disabled only when nothing is selected, and then styled quiet rather than amber, with `writable()`'s sentence (`Select a session first`) as its title.
  - A **closed** selected session opens: reading a closed session is not writing to it. This fixes the bug where a closed selection read `select a session first`.
- The footer keeps the state; `NoteAt` keeps its sentence, since it is the control the reader pressed.

#### G18 · `show all annotations` with floating cards
- `arras/src/lib/fragments/expand.ts`:
  - `openAt(trigger, ids, float = floating)` takes its placement per call, stored on each opened box.
  - `expandAll` opens every box **in the flow**, under its block or label, as `inline` does. Boxes already floating are closed and reopened in the flow.
  - `again()` repositions only floating boxes.
  - A single mark's click keeps the reader's placement.
- This applies to every fragment, a digest's included.

#### The divider, twice as thick
`Workspace.svelte`: the drawn rule becomes 2px (`.divider::before { width: 2px; left: 2.5px }`), inside the same 7px target. Amends DR-249-ikmartin's "1px".

#### The PDF render error
- `arras/src/lib/pdf/PdfPage.svelte` `draw()`:
  - Keep the running `RenderTask` and a draw counter.
  - A new draw cancels the running task (and the text layer's) and waits for it to settle before touching the canvas.
  - After each `await`, a superseded draw returns.
  - pdf.js's `RenderingCancelledException` is not a problem and is never shown.
  - Cancel on destroy.
- `arras/src/lib/pdf/PdfDoc.svelte`: while `fitText` holds, pages wait to render until the column's width is known, so the first draw is already at the fitted zoom and nothing is drawn twice.

### Tests
- **Updated:**
  - `requests.e2e.ts:626-627`: the rail's label while refused.
  - Context tests that click the crumb `document` (`workspace.e2e.ts:84`, `phase3.e2e.ts:87`) click the file name.
  - Hover tests that take the context's first node link (`requests.e2e.ts:359, 370`) name their link.
  - The report screenshot of the context.
- **New, in `tests/e2e/phase5.e2e.ts`:**
  - `sy-0009`'s context has no isolated-node caption and no `unreachable` diagnostic, and still shows a `missing-proof` diagnostic when there is one.
  - The context's lists come before its graph; `in` names `main.tex`; no raw `/proof` key is shown.
  - The session's chips name `Lemma 1.2`-style results with the key as title, fold past four with `and N more`, and a region or work target opens its item.
  - The rail's `open session discussion` keeps its label while refused, and a closed session's discussion opens.
  - `show all annotations` under floating opens every box in the flow (no `.floating`), and a single click still floats.
  - The digest's proposals carry no explanatory paragraph.
  - The divider's rule is 2px.
- **Reading suite:** opening `Arden24` (`Cycle spaces of finite quivers`) in half a pane and alone shows no `Cannot use the same canvas` problem on any page.
- **Loom unit test:** a journal message's body carries no heading date.

### Book and plan
- **Decision records from DR-255-ikmartin:**
  - A context lists first and names results as readers do; absences, including `unreachable`, are omitted (G2, G17).
  - A session names its targets as readers do and folds them past a few (G12, G16).
  - A journal message's heading is its date and author, not its first line (G13).
  - A digest's proposals carry no explanation (G14).
  - The rail's discussion control keeps its label, and reading a closed session is not refused (G8).
  - `show all` opens every box in the flow (G18).
  - The divider is a 2px rule (amends DR-249-ikmartin).
  
  The render error is a bug and gets no record.
- **Chapters:**
  - 15.3.2 (the context), 15.2.5 (the rail's control), 15.3.9 (the session views), 15.3.1 and 10.4 (show all), 15.2.4 (the divider).
  - 10.2.3 (the digest's proposals).
  - The thread-rendering section on journal messages (find with `grep -rn "thread.md\|## heading" docs/book`).
- **Plan 0.13.3:** G2, G8, G12–G14 and G16–G18 marked fixed, and Phase 5 under Delivery.
- **This plan** is kept with the others at the end of plan 0.13.3.

### Verification
```
cd loom  && uv run pytest -q
cd arras && npm run check && npx vitest --run && npx playwright test
cd arras && npm run test:write && npm run test:reading
cd ..    && uv run --project loom python docs/work-queue/check.py
```
Baseline: 120 unit, 177 e2e, 8 write, 17 reading.

Then serve a copy of the showcase and look at:
- `/node/sh-0402` with its context;
- `/node/sh-0009` with its context;
- `/session/s-2026-09-16-0001` in both views;
- `/library/Bellamy19?view=digest`;
- `show all annotations` on `/master/main-atomic`;
- `Cycle spaces of finite quivers` alone and in half a pane.

Rebuild and vendor. Stop for review; no commit unless asked.
