# Appendix A. Decision records

## Format

**[decided]** One record per decision, appended to this file, never edited afterwards (a reversal is a new record citing the old one). Fields: number, date, title, the principle(s) concerned, the decision in one or two sentences, the reason in one or two sentences, what it replaced if anything, and status (`active`, `superseded by N`). Records from the design conversations are dated by the day the conversation settled them; implementation-time records will be dated by merge.

## Records

DR-1 · 2026-09-15 · Adopt the block-based workflow of arXiv 2609.05669 §1.2 · P1, P5 · Decompose a paper into atomic units with tracked dependencies and review status, under a master recording order. · The source paper reports a threefold acceleration from exactly this. · active

DR-2 · 2026-09-15 · Source is LaTeX; no outer markup · P1 · Nodes are ordinary LaTeX; forester, Markdown, MyST, org, and Typst are rejected as source languages. · Mathematicians know LaTeX; the submitted `.tex` must be the written `.tex`; models are fluent in LaTeX. · active

DR-3 · 2026-09-15 · Forester as source considered and set aside · P1 · Forester's structure (addresses, transclusion, scoped macro modules) is better for a corpus; the per-paper tool is built first, in LaTeX. · No forester-to-LaTeX export exists; opam-only install; the corpus is deferred. · active

DR-4 · 2026-09-15 · The graph is defined by labels and environments, never by paths · P5 · One-file-per-node and regions-in-files are one specification. · Removes the need for a file format and lets the Stacks-style author keep one file. · active

DR-5 · 2026-09-15 · Transclusion is `\input`; no `\transclude` macro · P4 · The master compiles with plain `pdflatex`. · LaTeX already has transclusion. · superseded in part by DR-27 (`\nest` added for level shifting)

DR-6 · 2026-09-15 · Ids are `<prefix>-<local>`, prefix chosen by the author, sequential base-36 · P1 · Replaces random tags. · Forester and the Stacks project set the convention; the author chooses the prefix scheme. · active

DR-7 · 2026-09-15 · The ledger records acceptances only · P2, P3 · Reviews are files; states are computed; agents never write the ledger. · Prevents pollution by garbage runs; keeps decisions human. · active

DR-8 · 2026-09-15 · "Reviewed" is not a state · P3 · Reviews appear as facts and counts. · The word did two jobs and failed at both. · active

DR-9 · 2026-09-15 · Snapshots stored with acceptance rows · P2 · Content-addressed text of accepted keys and closures. · A stale acceptance must be explainable with a diff without git. · active

DR-10 · 2026-09-15 · One write path for annotations: `loom comment` · P4 · Agents and humans both use it; record files are never hand-written. · Loom owns the format end to end and computes anchors. · active

DR-11 · 2026-09-15 · Text-quote selectors relative to a region · P5 · Not file and line. · Anchors survive atomize, inline, and edits elsewhere. · active

DR-12 · 2026-09-15 · Runs are directories per chat thread, mode-free · P2 · Modes are applied inside runs; no run lifecycle. · A chat may cover several modes; restricting runs to one mode was prohibitive. · active

DR-13 · 2026-09-15 · Loom contains no model client; local models only · P8, P9 · Runner is an external local command; deferred. · No credentials, no hosted APIs. · active

DR-14 · 2026-09-15 · Digests in LaTeX as external nodes · P1, P4 · Replaces `map.toml` and `map.md`. · Reuses scanner, viewer, bundles; `\cite[postnote]` becomes an edge with no new syntax. · active

DR-15 · 2026-09-15 · Digest statements verbatim by default, macro-free or scoped · P1 · Expansion where mechanical, macro block otherwise; proofs never copied; sharing subject to licenses. · Verbatim is mathematically safer; paraphrase was solving licensing and notation, which are handled otherwise. · active

DR-16 · 2026-09-15 · `\uses` adopted from leanblueprint · P4 · No-op macro recording dependencies the text does not name; lint reconciles with `\ref`. · Some dependencies are only in prose; digests need internal trees. · active

DR-17 · 2026-09-15 · `\incomplete` rather than `\block` · P1 · Beamer defines `block`. · A talk in `drafts/` is a plausible master. · active

DR-18 · 2026-09-15 · Arras is a static Svelte bundle reading a build directory; loom is Python · P11 · No code shared; no server in the viewer; the write API is loom's. · Enforces separation by language and by artefact. · active

DR-19 · 2026-09-15 · Watching, not notification · P11 · Arras's only trigger is the manifest changing. · The viewer must not learn event kinds. · active

DR-20 · 2026-09-15 · Sections are nodes; nodes include nodes · P5 · Sectioning units are nodes; `sections/` is dropped; inclusion trees per master; own-text ownership. · Unifies masters, spines, sections; matches forester's model; the master view becomes "render expanded". · active

DR-21 · 2026-09-15 · Loom never modifies an author file · P7 · Destinations required; patches printed; `loom delete` refuses. · In-place rewriting destroys work. · active

DR-22 · 2026-09-15 · Directives use the `% !TEX` magic-comment family: `% !LOOM key: value` · P10 · Declarations only; never change output; regions as `begin`/`end`. · Established TeX convention; editors recognise it; `% !TEX program` and `root` honoured. · active

DR-23 · 2026-09-15 · Masters live in the masters directory; compile from the root · P12 · Every file with `\documentclass` there is a master; `config.toml` names the directory and the default. · Multiple masters without clutter; Overleaf-compatible. · active

DR-24 · 2026-09-15 · No loose-files directory · P5 · Loose is computed; `% !LOOM ignore` for archived drafts. · Naming the directory revealed it was a property. · active

DR-25 · 2026-09-15 · "id" for identity, "tag" for theme · vocabulary · Replaces "tag" as identifier. · Matches forester and sitegen; supports the zettelkasten use. · active

DR-26 · 2026-09-15 · Digest ids are `<citekey>-<paper's label>` · P1 · Distinct shape from author ids. · Cleanly separates provenance of nodes. · active

DR-27 · 2026-09-15 · `\nest{file}` for level shifting; no config flag · P4, P10 · Per inclusion site; `\input` or `\nest` as written. · A comment cannot change the PDF; a config default had nothing to decide. · active

DR-28 · 2026-09-16 · `loom.sty` vendored; `\usepackage{loom}` per master · P12 · Replaces preamble blocks. · Three macros in every master's preamble was duplication. · active

DR-29 · 2026-09-16 · Author name in user config, not quilt config · P2 · `~/.config/loom/config.toml`; fallback to git; refusal with instructions. · A committed config would attribute one person's acceptances to another. · active

DR-30 · 2026-09-16 · Diagnostics: reserved codes in the interface, publisher codes namespaced · P11 · Arras renders unknown codes generically. · Arras must not know what a proof is. · active

DR-31 · 2026-09-16 · Names: distribution `loomtex`, command `loom`, viewer `arras` · naming · `loom` on PyPI is abandoned; PEP 541 request in parallel. · active

DR-32 · 2026-09-16 · Licenses: GPL-3.0-or-later (loom), AGPL-3.0-or-later (arras), MIT (`loom.sty`, demo) · policy · Copyleft prevents proprietary appropriation without forbidding use; the package travels with papers. · active

DR-33 · 2026-09-16 · Command vocabulary consolidated · P4 · `deps`/`unravel` for the graph; `ai init|orient|start|discard|promote`; `digest extract|fetch|import`; `id` replaces `tag`; `delete` refuses. · Six graph commands were one question. · active

DR-34 · 2026-09-16 · Workspace repository tracks docs and specs; tool repositories separate; fixtures uncommitted · P11 · `loom-arras/` is a git repository ignoring `loom/`, `arras/`, `tests/fixtures/`. · The book and the interface need history; arXiv sources must not be redistributed. · active

DR-35 · 2026-09-16 · Overleaf compatibility is a contract · P12 · Portability rules; manual test per release; CI proxy. · The quilt is an ordinary LaTeX project. · active

DR-36 · 2026-09-16 · Vendor wrappers are generated pointer-only stubs · P4, 11.10 · Skill stubs and slash commands may be generated per mode but contain no mode content; the layer works with every stub deleted; `upgrade` never overwrites an edited mode file. · Two copies of a procedure drift; Codex has no stub mechanism. · active

DR-37 · 2026-09-16 · `bundle --with` and `bundle --draft` · P7, 11.5 · A bundle may be built with a proposed diff or a substitute file in place of a key's text, or for a not-yet-promoted node file, without touching the quilt. · Simplify, referee, and draft outputs must be compilable before a person promotes or applies them; otherwise verification happens after the only cheap moment for it. · active

DR-38 · 2026-09-16 · Mode files derived from the chat rules by an explicit mapping · 11.5 · `docs/source/global-rules.md` holds the chat rules verbatim and `global-rules-mapping.md` maps each part to its loom destination; the four environmental differences (inputs by command, outputs as files, findings as annotations, code and compilation available) drive every adaptation. · Provenance: an edit to a mode can be traced to the rule it came from. · active


DR-39 · 2026-09-15 · The 10.8 forbidden-word list exempts "digest" and "proof" · P11, 10.8 · Arras's source may use the manifest's own field names (`proofs`, `digest`, `kind: proof`) and the spec's route `/digest/`; the guard test forbids "quilt", "atomize", "unravel", and every "loom <command>" phrase, and generic rendering of unknown labels, codes, and taxa stays enforced by tests. · The manifest specification and the route list name those words, and the Web Crypto API has `digest()`, so the rule as written could not be satisfied. · active

DR-40 · 2026-09-15 · The scanner reads by character offset; line anchoring is enforced only where regions are moved · P1, 5.1.4, 5.2.3, 6.2 · Environments whose `\begin` or `\end` shares a line with body text are read correctly; `atomize` still refuses them and `import` gains `--fix-anchoring` to rewrite the copy it makes. · 43 % of the nodes in one fixture paper violate anchoring; refusing to read them would make lint useless on real papers (user decision). · active

DR-41 · 2026-09-15 · Proofs attach by enclosure; statements nested in proofs are nodes · 5.6.1, 5.7.1 · A proof directly inside a theorem-like node with no statement before it attaches to that node; a theorem-like environment inside a proof is a node in its own right, and the enclosing proof gets an implicit proof-edge to it (`via: nested`). · Five proofs inside `example` environments in Manolache and four lemma-plus-proof pairs inside one proof in ACGS. · active

DR-42 · 2026-09-15 · A `\cite` as the first token of the body marks an external node · 5.5.2 · External node = plain style, no proof, and a citation in the title or as the first body token. · Every real instance in the fixtures writes `\begin{definition}\cite[...]{...}` with no brackets. · active

DR-43 · 2026-09-15 · A heading's label is never taken from a line that opens an environment or another heading · 5.4.2 · The "same line or next non-blank line" rule stops at such a line. · Three label-theft cases in the fixtures. · active

DR-44 · 2026-09-15 · Inclusions resolve as TeX does · 5.9.1 · The path as written first, then with `.tex`; the braceless `\input name` form is accepted; a name kpsewhich finds is a system file and ignored; a non-.tex file is an opaque inclusion never scanned for nodes. · `\input{fig.pspdftex}` eleven times and `\input xy` in the fixtures. · active

DR-45 · 2026-09-15 · Digest id prefixes are citekey slugs; labels are whitespace-normalised · 5.3.1, 5.4 · The prefix is the citekey with everything but letters and digits removed; two citekeys with one slug are `loom:citekey-slug-collision`; the `digest:` directive keeps the verbatim key; runs of whitespace in any label collapse to one space. · Real citekeys contain hyphens, colons, and spaces, and real labels wrap across lines. · active

DR-46 · 2026-09-15 · The preamble closure is transitive through local style files; macro display names and unknown styles are resolved · 5.5.1, 5.5.3 · `\usepackage` lists, multi-line lists, and `\usepackage` inside a local `.sty` are followed; a display name that is a zero-argument macro is expanded, otherwise the environment name is capitalised with `loom:taxon-name-macro`; a `\theoremstyle` outside plain, definition, remark maps to plain with `loom:unknown-theoremstyle` unless `\newtheoremstyle` declared it. · All 26 declarations of the acceptance paper are two hops away in a local `.sty`. · active

DR-47 · 2026-09-15 · Non-UTF-8 sources are decoded with a warning · 5.1 · UTF-8 first, then Mac Roman, then Latin-1, with `loom:non-utf8-source`. · A 2011 arXiv source carries Mac Roman en dashes. · active

DR-48 · 2026-09-15 · Comments are blanked before every stage, not only before hashing · 5.13 · Comments are replaced by spaces of equal length so offsets are stable and no stage reads them; directives are parsed from the raw text. · A commented-out environment duplicated a live label. · active

DR-49 · 2026-09-15 · Ownership is per file; hierarchy is per master · 5.9.2, 5.9.3 · A section's own text runs from its heading to the next heading of equal or higher level in the same file (or the file's end), minus nested claimants; its parent and children come from the expanded master. Every character of a file belongs to exactly one claimant, the file (or master, which owns the preamble) being the outermost. · Hashes must not depend on which master reached a file. · active

DR-50 · 2026-09-15 · A master's preamble yields no edges or regions; a node never has an edge to itself; single-label reference commands are not split on commas · 5.7.1, 5.8 · Edges and region labels are read from `\begin{document}` on; `\ref` and `\eqref` take one label, `\cref`, `\Cref`, and `\uses` take lists. · Macro bodies in a preamble contain `\ref{#1;#2}`; labels contain commas. · active

DR-51 · 2026-09-15 · `unreachable` is emitted once per loose file and never for digest files · specs/diagnostics.md, 8.1.2 · One diagnostic per file no master reaches, listing its node keys; digests are loose by construction and are not reported. · Per-node reports would drown the problems page. · active

DR-52 · 2026-09-15 · Five diagnostic codes added · specs/diagnostics.md · `loom:non-utf8-source` (warning), `loom:unknown-theoremstyle` (warning), `loom:taxon-name-macro` (info), `loom:citekey-slug-collision` (error), `loom:main-not-found` (warning), plus the reserved `loom:foreign-annotations` and `loom:agent-wrote-outside-run` the book names in prose. · Each names a condition the fixtures produce. · active

DR-53 · 2026-09-15 · Unknown theorem-like environments are detected by a list of common names · 5.5.1 · An undeclared environment is reported as `loom:unknown-environment` only when its name is one of the usual theorem-like names (theorem, lemma, prop, defn, and so on); any other undeclared environment is prose. · Without a declaration nothing else says an environment is theorem-like. · active

DR-54 · 2026-09-15 · The static deployment is a shell prerender · 10.1.1, 10.7 · `arras build --prerender` writes one `index.html` per route named by the manifest plus the build directory, so pretty URLs work on any static host; pages are the single-page shell and content loads in the browser from `build/manifest.json`. · Server-rendered content needs a second data path (filesystem loads at build time) that the MVP does not need; crawlable pages are deferred. · active

DR-55 · 2026-09-15 · MathJax 3 with SVG output · 9.4.3, 10.1 · The viewer bundles MathJax's `tex-svg` component; no font files are shipped and a deployed site works offline. · The CommonHTML output needs a font directory the bundle would have to carry and reference. · active

DR-56 · 2026-09-15 · Per-fragment macro sets are applied inside the fragment · 8.3.5, 10.1 · A fragment naming a macro set gets `\renewcommand` lines for that set prepended to its first math element before typesetting; the default set is global. · MathJax's macro table is global; isolating a set per fragment would need a second MathJax instance. Node pages show one node at a time, so the approximation is invisible in practice. · active

DR-57 · 2026-09-15 · `file://` is unsupported; `loom serve` provides the SPA fallback · 10.1.2 · Routing is path-based; `loom serve` answers any extensionless path with `index.html`; opening the bundle from `file://` is not supported in the MVP (any static server, such as `python -m http.server`, works). · SvelteKit's router type is a build-time choice, so it cannot switch to hash routing when a page is opened from a file. · active

DR-58 · 2026-09-15 · The synthetic quilt compiles: missing include inside `\iffalse`, beamer talk declares `proposition` · specs/fixture.md §1, 14.3 · The `missing-include` case sits inside `\iffalse … \fi`, which LaTeX skips and the scanner does not interpret; the beamer master declares `proposition` in definition style for the taxon conflict instead of redeclaring `lemma`, which beamer predefines. · Both masters must compile for numbering, and a master with a missing `\input` cannot. · active

DR-59 · 2026-09-15 · Nodes that owe no proof are proved when their statement is accepted · 7.6.3 · `proved` requires a fresh accepted statement and, only for plain-style non-external nodes, a fresh accepted proof; definition- and remark-style nodes are proved by acceptance alone, so they can be settled and so can what depends on them. · The book's rule made every definition unprovable, hence nothing settled. · active

DR-60 · 2026-09-15 · Annotation ids are unique across the quilt · 7.4.2 · `a-<date>-<nnnn>` counts over every review record in the quilt rather than per file. · The book asked for quilt-wide uniqueness while assuming a per-file counter, which collides. · active

DR-61 · 2026-09-15 · `lint` and `check` include record-derived diagnostics · 5.14, 7.9, 7.10 · Detached annotations, retired ledger keys, positional-key matches, and foreign annotation files are reported by `loom lint` and `loom check`, not only in the manifest. · The book lists these codes under lint; they need the records, which the scanner does not read. · active

DR-62 · 2026-09-15 · Files no master reaches are sectioned per file · 5.9.2.3, 8.3.1 · A `.tex` file that no master reaches and that is not itself a master is sectioned on its own, the file path standing in for the master, so its headings are section nodes; a digest's headings are therefore the `<citekey>-sec-<n>` nodes of 8.3.1 and `loom id` labels headings in loose files. · The book stated the rule but the first implementation computed sections only on master expansions, which left digests without section nodes and `loom id` unable to label a heading in a file no master included.

DR-63 · 2026-09-15 · A proof naming only unknown labels attaches by position · 5.6.1 · When every label in a proof's optional argument is unknown, the proof attaches as it would without the argument (adjacency, then enclosure) and the unknown label is reported as a dangling link; `loom:unattached-proof` is reserved for proofs no rule places. · A deferred proof whose `\ref` has a typo still sits beside a statement; refusing to attach it lost information the position carried, and the dangling link already names the mistake.

DR-64 · 2026-09-15 · A heading's id label goes directly after the heading's arguments · 6.2.4 · `loom id` and `loom import` insert `\label{<id>}` immediately after the closing brace of a sectioning command, ahead of any label the author already placed there, so the id is the first label under the same rule that governs environments. · Appending the id after an existing label made the author's label the first one and the heading keyless.

DR-65 · 2026-09-15 · Identity through the reaching master for non-master files · 6.3, 6.4, 6.6 · When `atomize` or `inline` rewrites a file that is not a master, the identity test compiles the first master reaching it twice: as it is, and in a scratch copy of the quilt where DEST's text stands at SRC's path. With no reaching master the test is reported skipped rather than passed. · A section file has no PDF of its own, and the book specified the test only for masters.

DR-66 · 2026-09-16 · A digest's own locator citations are not postnote edges · 8.7 · A `\cite[LOCATOR]{citekey}` inside a node of the digest of that same citekey (every digest node's title is one) creates no edge and no `loom:unmatched-postnote`. · Matching the digest's own titles made the setup node depend on its section and flagged every title whose locator named only itself.

DR-67 · 2026-09-16 · Unnumbered results get star ids · 8.5.3 · A result of a `\newtheorem*` environment is extracted with id `<slug>-<abbrev>-star-<n>` (`n` counting unnumbered results in document order) and the locator `<Name> (unnumbered)`. · The book's id grammar assumed every result has a number.

DR-68 · 2026-09-16 · The counter emulator always runs · 8.5.2 · Results are numbered by the amsthm emulation in document order; whenever the paper's `.aux` numbers a labelled result, that number replaces the emulated one and resynchronises the counters, so unlabelled results between labelled ones are numbered correctly. `numbering: emulated` is written only when no `.aux` was produced. · An `.aux` file names only labelled results, so the emulator is needed even when the paper compiles.

DR-69 · 2026-09-16 · Display names map to the numbered environment · 8.1.7 · When the quilt declares a display name twice (`thm` and `thm*` both display `Theorem`), the extractor and `digest import` choose the numbered one, then the one whose environment name is the lowercased display name, then the first declared. · relloc's `math-env.sty` declares starred twins for every taxon.

DR-70 · 2026-09-16 · Run directories and fetched sources are not scanned · 5.1, 8.9, 11.2 · File discovery skips `ai/`, `refs/src/`, `refs/pdf/`, and `.claude/` in addition to `build/`; a bundle or draft copied into a run is never a master or a duplicate id. · Runs hold complete documents (`bundle-*.tex`) and drafts carrying ids that exist in the quilt; scanning them produced duplicate-id errors and phantom masters.

DR-71 · 2026-09-16 · Skills carry the target; commands are wrappers · 11.12 · `loom ai init --skills` writes a skill stub per mode under `.claude/skills/loom-<mode>/SKILL.md` whose body names the mode file and, for target-taking modes, `$ARGUMENTS`; it also writes `.claude/commands/<mode>.md` for those modes as one-line wrappers, since Claude Code documents commands as a legacy form of skills. `.claude/settings.json` denies Edit and Write per protected directory and the author-only loom commands, because deny rules take precedence over allow rules and an allow-only whitelist cannot be expressed. · Verified against Claude Code's current documentation at implementation time, as 11.12 deferred.

DR-72 · 2026-09-16 · The agent launch passes a pointer, not the orientation · 11.4.1 · `loom ai start` runs `[ai] agent` in the quilt root with `LOOM_RUN` set and a one-sentence prompt naming `loom ai orient --run RUN` and the run directory; the agent then reads the orientation and the live state through that command. `--no-launch` creates the run without launching. A run's `run.toml` records the agent's name when one is configured. · The orientation is a long document and `loom ai orient` is what keeps it current; passing it as an argument would freeze it at launch time.

DR-73 · 2026-09-16 · Definition aliases are resolved before macros are parsed · 5.5.1, 8.3.1 · A control word declared as an alias of `\newcommand`, `\renewcommand`, or `\providecommand` (by `\newcommand{\nc}{\newcommand}`, `\let\nc\newcommand`, or `\def`), transitively, is read as that command wherever it is used, in the scanner's macro table and in the extractor's macro block. · The virtual-localization paper defines its 130 macros through `\nc` and `\renc`; without the aliases none was expanded and its digest did not compile in a bundle.

DR-74 · 2026-09-16 · The macro block carries environments too; presentation packages are not required · 8.3.2, 8.5.6 · The extractor puts into the macro block every `\newenvironment` and every enumitem `\newlist` (with its `\setlist` lines) the statements use, adds `enumitem` to `requires:` when a `\newlist` is copied, drops `\xspace` from expanded macro bodies, and leaves page, font, hyperlink, and bibliography packages (`inputenc`, `geometry`, `hyperref`, `cleveref`, `natbib`, ...) out of `requires:`. · Statements use the paper's list environments; `\xspace` before `^` breaks math; a statement never needs the paper's page layout.

DR-75 · 2026-09-16 · Alias ids name a digest result too · 8.7 · Postnote matching derives `<taxon> <number>` from every id-shaped label of a digest node, not only its first, so a node labelled `ro-thm-1.0.1` and `ro-thm-1.2.1` answers citations under both numberings. · The relative localization paper cites Romagny under the numbering of an earlier arXiv version.

DR-76 · 2026-09-16 · Digest files are exempt from the loose-reference check · 5.11, 8.1.2 · `loom:reference-to-loose` is not reported when the target lives in a digest file, whether it is a result or one of the digest's section nodes; digests are loose by construction. · `\cite[Section 4.1]{...}` resolving to a digest's section node was reported as an error.

DR-77 · 2026-09-16 · Fetching respects the eprint type and retries · 8.9 · `digest fetch` uses the bib entry's `eprint` only when `eprinttype` or `archiveprefix` is absent or `arXiv`; a request answered 406, 429, or 5xx is retried twice after a pause; when the source cannot be fetched but `--pdf` was asked, the PDF is still fetched and the source failure reported. · Bibliographies carry JSTOR eprints, and arXiv answers a burst of requests with 406.

DR-78 · 2026-09-16 · Every non-asset path is a viewer route · 10.1.2, 12.5 · `loom serve` answers with the bundle's `index.html` for any path that is not a file of the bundle, unless the path lies under `_app/` or ends in an asset suffix (`.js`, `.css`, `.svg`, `.png`, `.json`, fonts), which stay 404. · Node ids with dots (`ro-thm-1.0.1`, `Kre99-thm-2.1`) were mistaken for files and their pages did not load.

DR-79 · 2026-09-16 · How a fallback document is built · 9.4.2 · A block that falls back to SVG is compiled in `standalone` from a scratch directory with the quilt root on `TEXINPUTS`, so `loom.sty`, `preamble.tex`, and the author's `.sty` files load as the master loads them; the preamble is the master's own text before `\begin{document}` (never the closure's concatenation, which would define every macro twice), wrapped in `\makeatletter`/`\makeatother`, with `geometry` passed `pass` and `microtype` told not to protrude, and with page-layout packages removed; the body sits in a 16 cm `minipage` so lists and displays are allowed and dvisvgm crops to the ink. A digest statement's document also carries the digest's macro block and the packages its header requires that the preamble lacks. Three attempts are made: as described, then without the added packages, then with a minimal preamble; the diagnostic names the node and every attempt's first error, and `LOOM_SVG_KEEP=DIR` keeps failing documents for inspection. · The first implementation ignored `TEXINPUTS`, so every first attempt failed on `loom.sty` and only the minimal retry ever rendered; on the migrated paper 33 blocks failed until each of these was fixed, and none fail now.

DR-80 · 2026-09-16 · The macro block is loaded, never shown · 8.3.2, 8.12 · A digest's document fragment starts after its `% !LOOM end macros` line; the block's definitions surround every statement that needs them and are not rendered as text. · Rendered as text, the block's `\newlist` lines were compiled a second time as a fallback figure and failed as already defined.

DR-81 · 2026-09-16 · `digest import` does not remap environments · 8.1.7, 8.10 · `loom digest import` copies a digest as written, rewriting only its citekey, prefixed labels, `\uses` entries, and `\cite` keys when `--as` renames it; environments the quilt does not declare are reported for the author to declare, since a digest file carries no display names to map by. The clause of DR-69 saying that `digest import` chooses the numbered environment described the extractor only. · Noticed while revising chapter 8 against the code (M7); the record is appended rather than DR-69 edited, since records are never edited.

DR-82 · 2026-09-16 · Derived region files are not written · 9.1, 9.2.1, 9.7 · `loom build` does not write `build/derived/<id>.statement.tex` and `<id>.proof.tex`; bundles and fragments take a key's region from the scan. The directory stays reserved for a consumer that needs the regions as files. · No command or test read the files; writing them would have doubled the publish for nothing (found while revising chapter 9 at M7).

DR-83 · 2026-09-16 · Mechanisms that differ from the book without changing a decision · 9.2.2, 9.4.1, 9.8, 10.2, 10.5, 10.7 · Atomic publish writes each file beside its target as `.tmp` and renames, the manifest last, rather than staging a directory; `loom serve` recompiles the default master after any `.tex`, `.sty`, `.cls`, or `.bib` change, not only files that master reaches; `\footnote` renders as `span.footnote` per the dialect, not as a sidenote; the review panel's filters are stale, draft, incomplete, loose, previous-key matches, master, author, and tag, with undigested citekeys listed below; the blockers page lists the downstream set of each incomplete key; the live reload empties the fragment cache wholesale; arras's npm package is private and its pip wrapper exposes only `bundle_path()`. · Each is the simpler mechanism found at implementation; the decisions they serve (publish atomically, re-render on save, render footnotes, filter the review panel, show what an incomplete proof blocks, reload on manifest change, ship the viewer inside loom) stand.

DR-84 · 2026-09-16 · The fallback preamble gobbles with its own macro and restores what the class provided · 9.4.2 · The `\title`/`\author`/`\date` substitution writes `\loomgobble{`, defined in the guard as `\newcommand{\loomgobble}[1]{}`, not `\@gobble{`; and because stripping `\documentclass` also strips what the class loaded, the guard supplies `amsmath` and `amsthm` when the preamble does not load them itself, and `\providecommand`s `\bibname`, `\refname`, `\abstractname`, `\figurename`, and `\tablename`. · A preamble that loads xypic with `\input xy` leaves `@` an ordinary character, so `\@gobble` tokenised as `\@` followed by `gobble` and every fallback compile in the Manolache quilt died in vertical mode: 299 of 303 blocks, each emitting its source as verbatim text and costing four LaTeX runs. Two further blocks failed on `\numberwithin` and `\bibname`, both provided by `amsart` in the real document.

DR-85 · 2026-09-16 · One LaTeX pass for a fallback, and failures are remembered · 9.4.2 · A fallback block is compiled once, and a second time only when the log matches `Rerun to get`, `There were undefined references`, or `Label(s) may have changed`; `-halt-on-error` is passed so a doomed document stops at the first error; a block that cannot be compiled writes `<hash>.failed` beside `<hash>.svg` in the cache and is not attempted again until the cache is cleared. · The second pass was unconditional and changed nothing: across all 24 fallback blocks of the relative localization quilt the rendered content is identical after one pass, and the apparent byte differences are dvisvgm emitting glyph paths in a different order, which varies between two identical runs too. Failures were not cached, so every cold build paid for them again.

DR-86 · 2026-09-16 · An environment written inline in a container renders as HTML · 9.4.1, 9.4.2 · A theorem-like environment or proof that sits in a master or section fragment without being a claimed child is converted to `div.env` (or `details.env.env-proof`) with the same markup a node fragment carries, minus the node identity, rather than being compiled to an SVG picture of itself. The SVG fallback keeps the meaning the chapter gives it: a block the converter genuinely cannot translate. · The chapter listed a nested theorem-like environment among the fallback's cases. In practice the branch fired for every environment the ownership pass had not claimed, so a quilt with duplicate ids rendered its whole text as pictures, or, when those pictures failed to compile, as verbatim LaTeX.

DR-87 · 2026-09-16 · `loom serve` prints its URL before the first build · 9.8 · The command binds the port and starts serving first, prints the URL, then runs the initial build, reporting `building the quilt ...`, then the fragment count, the elapsed time, and the error count with `run loom lint` when the quilt has errors. `/build/manifest.json` is a 404 until the first publish, which arras already reports as `arras:manifest-missing` and recovers from on its next poll. · The build ran inside `start()` before anything was printed, so a first serve on a large quilt looked like a hang for minutes.

DR-88 · 2026-09-16 · `loom atomize --ignore-src` · 6.4, 12.3 · With the flag, atomize writes `% !LOOM ignore` above the source file it atomized, so the quilt keeps one definition of each node; without it the printed note says the quilt now holds two copies and names the flag. · Atomize only advised the author to delete or ignore the source. The advice was missed when the demonstration quilts were built, leaving three definitions of every node in `demos/man12` and a quilt whose every id was duplicated.

DR-90 · 2026-09-16 · The serif stack, the dark palette, and where preferences live · 15.6, 15.7 · The body serif is the stack the author's site generator uses, `'Palatino Linotype', Palatino, 'Book Antiqua', Georgia, serif`, so a node page and a note page read as the same publication; chrome is Inter and ids are the existing monospace stack. The dark palette is the same vocabulary a second time, `--paper: #17171a`, inks inverted, and every state wash expressed as a low-alpha overlay of its state colour rather than a second hand-picked hue; it is declared both under `@media (prefers-color-scheme: dark) :root:not([data-theme='light'])` and under `:root[data-theme='dark']`, so the system setting and the explicit choice use one block. The five preferences — shell, typeface, size, line width, theme — are the viewer's, stored in `localStorage` under `arras.prefs` inside try/catch, applied as `data-*` attributes on `<html>`, and read once by a blocking script in `app.html` before the first paint. `?shell=` overrides the stored shell for one visit so two arrangements can be compared by sending a link. · Chapter 15 left the serif stack and the dark values open and the earlier request had asked for the preferences to live in `config.toml`; the chapter is ground truth and says preferences are arras's and never the corpus's, so the configuration key was dropped.

DR-91 · 2026-09-16 · Section nodes publish their sectioning level · specs/manifest.md §3, 15.2 · A node entry of kind `section` carries `"level"`, the sectioning depth, 1 for `\section`. The change is additive and does not raise `interface_version`. · The contents tree is built from `inclusion`, which gives true document order but nests everything a section contains beneath it, so tree depth is not outline depth and a `\paragraph` unit is indistinguishable from a `\section`. Without the level a viewer cannot indent the rail correctly, nor stop it at a chosen depth, and the ACGS paper's contents fills with paragraph units.

DR-92 · 2026-09-16 · Every node element carries a real `id` · specs/dialect.md §2.2, §2.4, §2.5 · `section`, `div.env` and `details.env-proof` each carry `id`, their `data-key` slugged by the rule display equations already use. The viewer's `anchorId` applies the same rule, so a contents entry, a heading link and a reference all land on the element. · The publisher emitted `data-id` and `data-key` but no `id`, so no fragment had a single anchor target and every link into a document dead-ended; the viewer compounded it by rebuilding an equation's hash from the bare label, producing `#eq-fix` where the element was `#sy-0001-eq-fix`.

DR-93 · 2026-09-16 · The graph's two layout libraries · 15.5 · Force, the default, is `d3-force`: a simulation stepped to completion rather than animated, seeded from the previous drawing so a filter change moves nodes instead of reshuffling them. Layered keeps `elkjs`. Both share one page, one selection, one filter set, and one pan-zoom-drag surface, and the toggle swaps only the geometry. Above a few hundred nodes the page is scoped by selecting a node and choosing a depth; no automatic threshold is set, because the useful scope depends on the question, not on the count. · Chapter 15.5 deferred the library choice and the node count at which the page scopes itself by default. The first is settled by use: `d3-force` is the implementation org-roam-ui's behaviour is described from, and ELK's layered algorithm is already in the viewer and produces the better hierarchical drawing. The second is left unset deliberately.

DR-94 · 2026-09-16 · A key with no page is not linked · 10.3, 15.3.2 · A viewer resolves a key to a page through the node that owns it, so an unlabelled proof links to its statement. A key that resolves to nothing — a file container such as `nodes/sy-0002.tex`, which diagnostics name — is rendered as plain text rather than as a link. · Linking every key a diagnostic names produced "Unknown key" pages from the problems page, and giving file containers pages of their own would put an object in the viewer that the interface does not describe.

DR-95 · 2026-09-16 · TeX conditionals in a macro body are resolved before publication · 9.4.3, specs/manifest.md §14 · A macro whose body holds `\ifinner A\else B\fi` is published as `\mathchoice{B}{A}{A}{A}`, and `\ifmmode A\else B\fi` as `A`. Any other conditional is published unchanged. · MathJax implements no TeX conditionals at all, so the ACGS paper's `\newcommand\arr{\ifinner\to\else\longrightarrow\fi}` reached every page as the words `\ifinner`, `\else` and `\fi` set in error red beside two arrows, dozens of times per section. `\ifinner` asks whether the formula is inline, which is exactly the distinction `\mathchoice` selects on, so the rewrite is faithful; `\ifmmode` is always true inside a formula. Guessing a branch of any other test would silently change the mathematics, so those are left alone.

DR-96 · 2026-09-16 · A title keeps its mathematics · specs/manifest.md §3 · The plain text of a title preserves `$…$` and `\(…\)` spans as written and cleans only the prose around them; a viewer typesets what it can and shows the source otherwise. · Stripping control sequences from titles destroyed them: the ACGS paper's contents rail read "Structure of $ $" twice, and five of its sections had lost the object they were about. Nothing in the manifest gains a field; the same `title` string simply stops discarding half its content.

DR-89 · 2026-09-16 · Brainstorming and granularity with one new directive · P4, P5, P10 · Brainstorming uses runs, candidate taxa, loose nodes, and an outline master; same-statement granularity uses multiple proofs; different-statement granularity uses a node-level `see:` directive recorded as a relation that is never a dependency. A `brainstorm` mode with four blocks is added. Rejected: an `expands:` directive (a second key for one idea), a `refuted` convention with tool support (a state in a tag), an `expands-statement-differs` diagnostic (fires on the intended case), a graph toggle (post-MVP). · The requirement was a link the viewer shows and the paper never sees; directives are the mechanism reserved for that, and everything else already existed. Numbered DR-39 in `docs/plans/0.2-brainstorm-and-relations.md`, which was written before DR-39 to DR-88 were assigned; the plan's own numbering is superseded by this record.

DR-97 · 2026-09-16 · Four changes that let a client scan an editor's buffers · 5.9.1, 5.7.3, 9.1 · `kpsewhich` is memoised per name, since it is a subprocess run once per unresolved inclusion and a paper's unresolved names repeat: it was 86 ms of a 166 ms scan on the Manolache import, and a warm scan of that quilt is now 74 ms. `\ref`, `\eqref`, `\cref` and `\uses` sites carry the character offset and column that were computed and thrown away, so `EdgeRec` has `offset` and `column` and a `dangling-link` points at the command rather than at the line. A node records `label_offsets`, the offset of each of its `\label`s, so a definition site is a range and not a line; a section's labels all point at its sectioning command, which is where a reader wants to land. `scan(quilt, overlay)` takes a map of quilt-relative paths to the text of unsaved buffers, which stand in for what is on disk, and a path that exists only in a buffer resolves as an inclusion, so a file written and not yet saved is scanned. · Each is small and improves loom alone; together they are what a language server needs, and without the last one an editor's diagnostics would follow the last save rather than the keystrokes.

DR-98 · 2026-09-16 · The read view is a measured column between two gutters · 15.3.1, 15.6, 15.7 · The document sits in a column of `--measure` plus a third of the slack around it, with a gutter either side of `(available − measure)/3`, which is the site generator's own algebra, so that a corpus page and a note page are laid out alike. The gutter is a container-relative length (`100cqw`), not a percentage, because the things standing in it are positioned against a node and a percentage would resolve against that node. The margin annotation — the key and its state — moves out of the text column into the left gutter, ending exactly at the environment's taxon accent, which is now the boundary between the two rather than a rule 96px inside the text. A node's comments stand in the right gutter beside it; one whose rendered text runs past 220 characters stays in the text as a box instead, because a paragraph squeezed into a gutter is unreadable. Below 1100px there are no gutters and both rejoin the flow. The width scale becomes 36, 44 and 52rem so that the middle setting is the site generator's 44rem. · Chapter 15.3.1 gave the read view an 86px margin column inside the text and put comments in the shell's right rail. Built, the margin column took its width from the text and the rail put a comment a third of a screen away from the sentence it was about; the author asked for both to move into gutters, and for a comment with something to say to stay in the text where it can be read.

DR-99 · 2026-09-16 · The shells fit the window, and the rails are half again as wide · 15.2, 15.6, 15.8 · Padding and borders count inside a declared size throughout the viewer. The contents-bearing rails are 252px in shell C, 267px in shell A and 240px in shell B, half again their design widths; the icon strip and the right rail are unchanged. A rail's scrollbar is `thin` with a transparent thumb until the pointer or the keyboard is in the rail, and the space it needs is reserved either way, so nothing shifts when it appears. · Every rail is `height: 100vh` with 12px of padding, and under the default box model that is 24px taller than the window: the settings control at the foot of the icon strip and the counts line at the foot of the panel were both below the fold, and the whole page scrolled by exactly that much. At the design widths a contents entry of a real paper wrapped onto three lines — the ACGS import's "Stable logarithmic maps and their moduli" among many — which the extra width fixes at the cost of gutters some 30px narrower. And a rail that always showed its scrollbar put a grey stripe down the side of every page of a long document.

DR-100 · 2026-09-16 · A display block that is a picture goes to the fallback · 9.4.1, 9.4.2 · A display environment or `\[…\]` block whose content names an environment outside a small allowlist of the ones a mathematics renderer handles, or uses a drawing command such as `\xymatrix`, `\tikz` or `\includegraphics`, is compiled by LaTeX as a figure rather than handed to the viewer's renderer. The allowlist decides, not a list of known offenders, so an environment nobody anticipated goes to LaTeX, which can always render it. A numbered display is compiled in its starred form with its `\label` removed, and the number the document gave it travels on the figure, where the viewer prints it; a standalone compile would otherwise begin counting again and print (1). `CD` is in the allowlist and `amscd` is enabled in the viewer's renderer, because an amscd diagram is a diagram but not a picture. · The rule that sent a diagram to the fallback fired only for a bare `\begin{tikzcd}`. A diagram written inside `\begin{equation}` — the ordinary way to number one — reached the renderer as `\[\tag{2}\begin{tikzcd}…\]` and set the whole block in error colour; the relative localization quilt had three such blocks and the ACGS import sixteen, all of them `\xymatrix`.

DR-101 · 2026-09-16 · An author's macros are published in a form a renderer can draw · 9.4.3 · An alphabet declared with `\DeclareMathAlphabet` is published as the nearest alphabet the renderer has, chosen by font family, falling back to upright roman. Commands a renderer does not implement but that a macro body may use — `\ensuremath`, `\scalebox`, `\resizebox`, `\raisebox`, `\mbox`, `\hbox` — are published as definitions that keep the content and give up only the presentation, and only when a published body actually uses one. · The relative localization paper defines `\sHom` as `\scalebox{1.2}{\ensuremath{\mathpzc{Hom}}}`. None of those three is something MathJax implements, so every use of the author's sheaf-Hom reached the page as error text. Giving up a 1.2 scale and an unavailable script font is a trade that loses presentation; leaving it is a trade that loses the mathematics.

DR-102 · 2026-09-16 · A macro is mathematics by its definition, not by its argument · 9.4.1 · Whether an expanded macro is set as mathematics is decided from the macro's body, with its `#n` placeholders, not from the expansion with the author's text substituted in. · `\red{…}` is `\textcolor{Mahogany}{#1}`, plainly text. Applied to a sentence containing `\cite{aranha-khan-latyntsev-etal_VirtualLocalizationRevisited2025}`, the expansion held an underscore, the heuristic read that as mathematics, and a paragraph of English prose was set as a formula in the renderer's error colour.

DR-104 · 2026-09-16 · A display block never scrolls vertically, and the glyph cache is the page's · 15.3.1 · A display block scrolls sideways and not up and down: naming one axis of `overflow` makes a browser compute the other to `auto`, and the block is then a scroll container for everything inside it. The viewer's mathematics renderer keeps its glyph cache once for the page rather than once per expression. · Two invisible things were being counted as content and each grew a scrollbar beside a formula that fitted the column. A per-expression glyph cache puts a `<defs>` block inside every container whose paths sit some four thousand pixels above it in the SVG's coordinate space; and the hidden MathML copy the renderer emits for screen readers is as tall as the formula is in its own units. Ten of the eighty-eight display blocks in the relative localization paper had a vertical scrollbar, at every type size and every line width, and none of them had anything to scroll to. The author sent a screenshot of two of them.

DR-103 · 2026-09-16 · `\textcolor` keeps its colour · specs/dialect.md §2.6 · `\textcolor{NAME}{text}` in prose becomes `<span class="tex-color" data-color="NAME">`, carrying the colour's LaTeX name as written; the viewer knows the base names and the dvipsnames an author is most likely to reach for, and anything else inherits the surrounding colour. · The converter read the colour and threw it away. An author who writes `\red{…}` to mark text as unverified means it to be visible, and the marking was silently lost.

DR-105 · 2026-09-16 · A quilt is not a repository unless the author asks · 4.7, 6.1, 12.3 · `loom init` writes `.gitignore` always and creates a repository only when `--git` is given; the flag `--no-git` is withdrawn. Both acts are reported, the ignore file with what it ignores and why. · Loom uses version control for nothing. It never stages, commits, branches, tags, or reads history; no command requires a quilt to be a repository; and the snapshots under `.loom/` exist precisely because it cannot assume one. Running `git init` was therefore the single place where the tool reached outside its own files, it was on by default, and it was silent: `_git_init` captured its output and said nothing, so an author who did not think to pass `--no-git` never learned a repository had been made. The ignore file is a different case and stays unconditional, because it costs nothing and is right the day the quilt becomes a repository. What remains of the relationship is deliberate and small: the ignore file, the opt-in flag, `loom doctor` noticing whether git is installed, and the ledger written as one table per key so that two people accepting different keys merge cleanly.

DR-106 · 2026-09-16 · `init --from` is one transaction: a refused import leaves nothing behind · 4.7, 6.1 · `loom init --from FILE` writes the quilt's skeleton before the import can read it, and removes it again if the import refuses; a directory `init` created goes whole, and one that was already there keeps everything `init` did not write. The quilt, the ignore file and the repository are announced only once the import has completed. · The import refuses a paper whose theorem-like environments are not line-anchored, and its own message names the remedy: re-run with `--fix-anchoring`. That is what an author does next, and it failed, because the skeleton written a moment earlier made the directory a quilt and the retry was refused as `already inside a quilt` -- so the remedy the tool had just recommended could not be taken without deleting a directory by hand. The same command announcing `created quilt ...` and then `Nothing was written` in the same breath was the other half of the fault: an announcement of something that did not outlive the command is the plan-versus-executed confusion again, in the one place the author is already being told that nothing happened.

DR-107 · 2026-09-16 · The book describes what is implemented; unfinished work moves to a work queue with triggers · 00-front-matter, Appendix B, every chapter · The `[assumed]` and `[deferred]` markers are withdrawn and every statement carrying one is either rewritten as what loom does or moved to `docs/work-queue/` as an item with an observable trigger. The per-chapter "Open questions" sections are gone and Appendix B, which collected them, becomes a pointer. `[decided]` survives with one job: marking a statement as normative. `docs/work-queue/check.py` fails if either withdrawn marker or an "Open questions" heading reappears. · The book had two roles and they had begun to disagree. Twenty-three `[deferred]` and thirty-five `[assumed]` statements were scattered over the chapters and specifications, and reading them showed the rot: several were stale, marked as open on a sentence that *was* the decision and had been implemented for a day; several were not deferrals at all but cross-references, or conditionals inside a decided statement; and the largest group were questions already settled, sitting under a heading that said they were not. Nothing anywhere recorded *when* a genuinely deferred thing should be picked up, so the deferral was a wish with no way to become work. Meanwhile the only "what is next" pointer was the demonstrations status table, every row of which reads `demonstrated`, so an agent following `AGENTS.md` reached a dead end on the second line. The split fixes both ends: the book states what is, and the queue states what is not, each item with a condition you can check in thirty seconds. Several of those conditions are numbers loom already prints, so a port is discovered ready by running `loom doctor` rather than by remembering to read a file.

DR-108 · 2026-09-16 · Digests are authored and tracked; nothing under `refs/` is · 4.2, 4.7, 8.9 · A digest lives at `digests/<citekey>.tex`, beside `nodes/` and `drafts/`. `refs/` holds only what was fetched, one directory per work named by its global identifier as `<scheme>/<sanitised value>`, and is ignored wholesale: `SKIP_PREFIXES` falls from `("ai/", "refs/src/", "refs/pdf/")` to `("ai/", "refs/")` and the ignore file from two lines to one. · The reference directory was half authored and half downloaded, which is why the skip list and the ignore file each had to name two sub-paths, and why `refs/` read as ad hoc: digests sat loose beside two subdirectories. One rule replaces three — nothing under `refs/` is ever authored — and the tracked/ignored split now coincides exactly with the directory split. Keying `refs/` on the global identifier rather than the citekey is forced by the crawl this prepares for: at depth 1 a work has a citekey, and past it a work reached through someone else's bibliography has none at all, so citekey-keying structurally cannot hold a corpus. It also makes deduplication between two quilts a hardlink rather than a mapping layer. Nobody navigates there by hand — `loom refs path` prints it and the viewer surfaces it — so the identifier is mangled for path-safety rather than for looks. The scanner did not change: a digest is recognised by its `% !LOOM digest:` directive, never by its path.

DR-109 · 2026-09-16 · A digest declares the prefix its ids carry, and records what it was extracted from separately from what the bibliography cites · 8.4, 8.5 · The header gains `prefix:`, defaulting to the citekey's slug, and `source:` splits into `extracted-from:` and `published-as:`. `source:` is still read, so a quilt that has not been upgraded is understood. `loom:unresolved-work` (info) reports a cited work stating no identifier; `loom:unverified-locators` (warning) reports a digest whose statements came from a preprint while the bibliography cites a published version, or which does not say what it came from at all. · Three identifiers were doing two jobs. The citekey is forced by BibTeX and cannot be chosen; the id prefix was derived from it by slugging, so a Zotero key produced `\uses{manolacheVirtualPullbacks2012-constr-3.13}` — eighty characters of which twelve carry information — and re-exporting a bibliography with a different key template would have broken every `\uses` in the quilt at once. Declaring the prefix separates the name you type from the name BibTeX needs. The provenance split is the same principle applied to the work: the artifact whose `.aux` gave the numbers and the article a reader will open are different documents, and in `demos/relloc` they already differed — the Manolache digest recorded the published DOI for statements extracted from the arXiv source, so `\cite[Definition 2.1, p.~4]` sends a reader to page 4 of an article that begins at page 201. Nothing could have detected that before, because one field held both facts. Local identifiers are for authoring; global identifiers are for joining.

DR-110 · 2026-09-16 · `loom serve` offers a work's fetched artifacts · 9.8, 10.5 · A `/refs/` route serves what has been fetched for a cited work — its unpacked source and its PDF — confined to that directory by the same prefix check the build tree gets. The manifest's reference entries say which artifacts are present, so the viewer can offer one or say it has not been fetched. · This is the first thing the server offers that loom did not generate, which is why it is recorded rather than slipped in. It is justified by what it unlocks: a comment that points at a specific place in a reference, followable in the viewer, which is the shape the author wants for citing evidence rather than paraphrasing it. Without the route a fetched PDF is invisible to arras — there is no URL for it — and without the manifest flags the viewer cannot even say whether one exists. The exposure is small and bounded: localhost, read only, one directory, and nothing under it is authored or secret, being a copy of a public preprint.

DR-111 · 2026-09-16 · The `[ai] runner` config key is withdrawn · 4.2, 11.9, specs/runner.md · `loom init` no longer writes it, `QuiltConfig` no longer carries it, and `loom upgrade` deletes its line from an existing `config.toml`. It stays in the accepted-key set so that a quilt loom itself wrote it into does not now report it as unknown. · The key was parsed into `cfg.ai_runner` and read by nothing, and it pointed at a specification for a subsystem that has since been declined (WQ-15). An inert key advertising a feature that is not coming is worse than no key: it is the config-file form of the same fault the work queue's own rule was written against, a promise nobody intends to keep. Tolerating it rather than erroring on it is deliberate — loom put it there, so an author who has not upgraded should not be told their file is wrong — and `upgrade` removes the line rather than rewriting the file through a TOML round trip, which would lose the comments the template ships with.

DR-112 · 2026-09-16 · The strip's icons are drawn, and the contents bar follows the reader · 15.2.1, 15.2.3 · The six view icons and the search affordance are stroked paths on a 24-unit grid at one stroke width, inline in the bundle rather than from an icon package. The contents rail's position bar is driven by the reader's scroll position, always marks exactly one entry, and compares on the published element id rather than on a node key. · The icons were unicode marks — `◇ ▤ ◈ ✓ ! ¶` — which a font renders at whatever weight, size and baseline it chooses, so the column sat unevenly and read as punctuation rather than as a set of controls. Drawing them is what makes them look like one family; keeping them inline rather than adding a package matters because arras ships vendored inside loom's wheel, so a dependency is weight in every install and six paths are a few hundred bytes. The position bar had two faults with one cause, `location.hash`: it appeared only after someone clicked an entry and then never moved as they read, and an entry whose key is not already slug-shaped — an unlabelled section, keyed `drafts/main.tex#section:3` — never matched the hash at all, so its bar never lit even when clicked. Following the scroll fixes the first and comparing on `anchorId(key)`, which is the id the publisher actually writes and the hash actually carries, fixes the second.

DR-113 · 2026-09-16 · The tabs shell is retired · 15.2, 15.2.2, 15.7, 15.8, 15.9 · Two shells remain, the rail (A) and the strip (C, the default). A stored or linked `b` is not a valid shell, so it falls back to the default like any unknown value; the settings panel offers the two; `TopBarTabs.svelte`, its tokens `--rail-b` and `--topbar`, and its reference figure are gone. The contract that every shell holds the same elements is unchanged. · The author asked for it after using all three: the tabs shell was the one nobody chose, which is the outcome 15.2's deletion rule was written to act on. Keeping the strip and the rail keeps the comparison that was still being made.

DR-114 · 2026-09-16 · Comments can stand in place, and a mark says what kind of comment it is · 10.4, 15.3.1, 15.7, 15.10 · A display preference, `comments`, is `margin` (the default and the earlier behaviour: short comments in the right gutter, long ones as boxes below their node) or `inline`. Inline, a mark on the text expands its comment beneath the paragraph, item or display it sits in, one at a time, and selecting outside or pressing Escape closes it; a comment with no mark — no quote, or an anchor that no longer resolves — is reached from a small count beside its node's label. In both placements a mark is coloured by the kind of its leading comment: objection, suggestion, question or confirmation. Switching the preference rebuilds the fragment, because marks and slots are wired once per element. · A comment in a gutter is a third of a screen from its sentence on a wide window and in the flow on a narrow one; the author wanted it where the sentence is, opened only when wanted. Colouring by kind lets a reader see that a paragraph carries an objection before opening anything, which a uniform highlight did not.

DR-115 · 2026-09-16 · The layered graph draws in one coordinate frame, dependencies above, and its nodes stay where it put them · 10.2.7, 15.5 · ELK is asked for every coordinate in the root's frame (`elk.json.shapeCoords` and `elk.json.edgeCoords`). Edges are laid out from the dependency to what uses it, so upstream sits above as 15.5 already said. A section that holds drawn nodes is drawn once, as their group, and an edge from a node into the section containing it is left out of the layered drawing. Dragging a node belongs to force alone; in layered a drag pans. Both layouts zoom about the pointer, and the page draws the shapes of the layout that has arrived rather than the one the toggle names. · By default ELK places an edge relative to the lowest common ancestor of its endpoints while still listing it at the root, and reading it as a root edge is what drew grouped edges away from their nodes — the broken drawing the author reported on the ACGS paper. The edges also ran from user to dependency, which put upstream below. A section was drawn both as a group and as a node inside the same layout, and ELK cannot route an edge into its own ancestor. A layered drawing's positions are its meaning, so moving one by hand only makes it wrong; and until ELK answers, boxes were being drawn at the force positions.

DR-116 · 2026-09-16 · A local graph follows the reader · 15.3.1, 15.3.2, 15.4, 15.5.1 · One node's neighbourhood, one or two dependency steps out in either direction with `see` relations drawn but not followed, laid out live with `d3-force` in the manner of Quartz's graph view: the centre pinned in the middle, nodes sized by degree and coloured by state, hover dimming everything but a node and its neighbours, each node a link that opens it and previews on hover, the drawing fitted to its box until the reader zooms or pans. It heads a node page's right rail. In the read view it is a floating panel the reader opens, whose centre is the last statement or proof to pass a third of the way down the window, and whether it is open is remembered in this browser. Either expands to a dialog that closes on an outside select or Escape. · The author asked for a graph that changes with where they are in a draft, like Quartz's. Written for arras rather than ported: Quartz draws with PixiJS, arras ships inside loom's wheel, and a neighbourhood is tens of nodes, which SVG draws without help. Following the last result passed rather than the element under the line keeps the centre from flickering between a result and its section on every paragraph of prose.

DR-117 · 2026-09-16 · Links to nodes and references preview on hover · 10.2.1, 15.3.6 · Resting on a link to a node — a reference in the text, a rail list, a table row, a node in a graph — or to a cited work shows a card: for a node its label, number, state and statement, typeset, clamped by height with a fade; for a work its title, authors, outward links and digest. The behaviour is the author's site generator's: a 300ms delay, a 200ms grace to move into the card, a card that stays while the pointer is in it so its links work, Escape, a scroll or a press elsewhere to dismiss, and nothing on a device that cannot hover. A section previews its title and state only, and a link to the page already open previews nothing. · The author asked for the site generator's preview in the viewer, so that what a result depends on and what uses it can be checked without leaving it. A section's fragment is the whole section, which is too much to fetch and typeset for a glance.

DR-118 · 2026-09-16 · The counts open filtered tables, and the blockers page is the review table's incomplete view · 10.2.4, 10.2.5, 10.2.6, 10.2.10, 15.2.3, 15.3.4, 15.3.5, 15.4 · Filters live in the URL. The home cards open `/review?show=accepted`, `?show=stale`, `?show=incomplete` and `/problems?severity=error`; the review table gains an `accepted` filter, and its header counts toggle their filter, as the problems page's do. With `show=incomplete` the table gains a column saying what each gap blocks, expandable to the list. `/blockers` redirects there and leaves the indexes. The problems page's filters move to the left panel, as 15.4 already listed. A column no row fills is not drawn. The strip's panel no longer lists the views beside the strip: a page with nothing of its own there shows the document and its contents. · The author found these pages bizarre, and they were: three of the four cards opened the same unfiltered table, the incomplete card opened a differently shaped page, and that page carried a panel repeating the strip it sat next to. A filtered view that is a link is also one that can be sent.

DR-119 · 2026-09-16 · A cited work links out, and a citation with no digest result links to its reference · 10.2.3, 10.2.9 · The references index and each reference page link a work by its identifiers — `doi:` to doi.org, `arXiv:` to arxiv.org, `mr:` to MathSciNet, `zbl:` to zbMATH — and by its bibliography `url`, each in a new tab, and to its fetched PDF under `/refs/` only when the manifest says one is there. A synthetic `work:` identifier links nowhere. A citation that names a digest result still opens that result; one that does not now opens its reference instead of rendering as text. Displayed bibliography fields lose BibTeX's protective braces and have accent commands made into letters, leaving `$…$` for the typesetter. The index lists who cites a work without the digest's own results, which cite their own paper, and folds a long list. · 26 of the 58 citations in the relative-localization paper's read view had no link at all, and the references page showed `{{Gromov}}--{{Witten}}` and offered no way to reach a paper. The identifiers were already in the manifest (DR-108).

DR-120 · 2026-09-16 · A long document typesets what the reader sees first · 10.1, 10.5 · A fragment with more than 160 formulas is typeset in batches: everything up to the element the URL points at, or the first screen and a half, before the page is revealed and scrolled, and the rest in batches that yield to the page, stopping if the fragment leaves the document. Every typesetting call goes through one queue. MathJax is loaded while the browser is idle once the manifest's macros are known. The manifest poll is skipped while the tab is hidden and runs at once when it is shown again. · Measured on the ACGS paper's read view (2,744 formulas, 92,000 elements once typeset): the first screen took 1,058ms cold and 1,156ms on return, and now takes 367ms and 174ms. Everything above the reader is typeset before the reveal, so nothing above them changes height afterwards; MathJax's own lazy mode was rejected because it leaves unrendered formulas at zero size, which moves the page under a reader who scrolls up or follows a link into the middle of a document. Holding the manifest as raw state rather than a deep proxy was measured and made no difference, so it was not kept.

DR-121 · 2026-09-16 · A floating box closes when the reader selects outside it · 15.2.3, 15.3.5, 15.3.6, 15.5.1 · The settings panel, a help panel, an expanded comment, a preview card and the expanded local graph all close on a press outside them or on Escape, through one shared action (`src/lib/dismiss.ts`) attached to the element that holds both the box and its opener. · The author asked for it; the settings panel stayed open until its own control was pressed again. One action rather than a handler per box keeps the behaviour identical everywhere, and listening for `pointerdown` in the capture phase means nothing inside a page can swallow the press first.

DR-122 · 2026-09-17 · A work that states no identifier gets looked-up candidates, never a looked-up identity · 4.2, 4.8, 8.9, 8.9.1, specs/manifest.md §13 · `loom refs resolve [CITEKEY...]`, allowed by `[refs] resolve = true`, asks zbMATH Open and then Crossref for the cited works whose entry states no identifier, scores every record itself, pools one work's records into one candidate led by its most useful identifier, and records the candidates in the work's own `refs/work/<hash>/resolved.json`. `loom lint` names a recorded candidate in `loom:unresolved-work` without touching the network, and the manifest gains an additive `candidates` list per reference. `[refs] contact`, when set, is sent to Crossref as `mailto`. Nothing is changed: a candidate becomes the work's identity only when the author's own bibliography entry states it. · WQ-04 planned a resolver whose strong matches bind with provenance `resolved` and whose identifiers are offered as a diff to `refs.bib`. On 2026-09-16 the author ruled the bibliography out of loom's scope, and whether an identity the author confirms is recorded anywhere but the bibliography is an open question of WQ-23; automatic binding would also move a work's `refs/` directory. Candidates deliver the half that needs neither decision — finding the identifier — and on `demos/relloc` found DOIs for two of its three unresolved cited works (Edidin–Graham 1998 with its arXiv preprint and Zbl number; Silverman's book, pooled from both services) and correctly no match for the Stacks project. zbMATH Open comes first because it covers mathematics and gives a Zbl number to works with no DOI; loom scores records itself because the two services' scores are on different scales and Crossref ranked Silverman's chapters above the book. Its own consent key, rather than reusing `fetch`, because it sends bibliography text to outside services where fetching retrieves a declared paper. Terms were checked on 2026-09-17: zbMATH Open needs no key, licenses its data CC-BY-SA 4.0 and asks for a reasonable rate; Crossref needs no key and reported a public limit of one request per second.

DR-123 · 2026-09-17 · Links into cited works open the fetched paper, and only the artifact they name · 10.2.3, 10.4.1, 15.3.7, specs/dialect.md §2.13 · A link `cited:SCHEME:VALUE#page=N` or `#quote=TEXT` in a comment, message or report opens a viewer dialog showing the fetched PDF in the browser's own renderer at that page. A copy on file is used only when it is filed under the identifier the link names; a copy of another artifact of the same work is offered with a warning that pages may not match; with nothing fetched the dialog says so and links to the identifier's own service, arXiv's PDF at the page where possible. A digest result's page in its locator opens the version the digest was extracted from, and only that version. The link form enters the dialect, since a viewer must recognise it. · The motivating case, from WQ-21, is a comment pointing at the evidence in a reference rather than paraphrasing it. Naming the global identifier rather than the citekey keeps a link valid across a bibliography re-export and in a collaborator's corpus. Refusing to open another version at the same page is the point of the extracted-from and published-as split (DR-109): a preprint and its article are paginated differently, so page 9 of the wrong one is a confident wrong answer. Browser-native rendering rather than PDF.js keeps arras's size inside loom's wheel; `#quote=` is accepted now so no link needs rewriting when a text layer arrives, and is shown as text to look for until then. Safari's `#page=` handling could not be checked, since no WebKit build was installed; opening in a tab is the fallback.

DR-124 · 2026-09-17 · The graph can draw each cited work as one node · 10.2.7, 15.4, 15.5.2 · The graph's "cited results" control gains "as papers": the corpus's own results stay expanded and every cited work becomes one node, joined to each own result that depends on any of its digested results (weighted by how many) or cites it, and to another paper when one digest's result depends on another's. Undigested works appear. A paper node has its own shape, inspector and double-click target. · WQ-05's trigger was met: `demos/relloc` holds 200 external nodes against 185 of its own, so the expanded graph is mostly other people's theorems and `reached` makes it legible but not navigable. The item described three views, the third being the quotient by source; taken over the corpus's own results too, that quotient is a single node with a star around it for one corpus, which answers nothing, so the contraction is applied to external nodes only and the full quotient waits for a corpus spanning several quilts (WQ-03). No manifest change was needed, since a node's source is its `digest`, and no control is named depth, which the item warned would collide with the crawl's.

DR-124 · 2026-09-17 · A parent's hash reads an inclusion of a child as that child, so atomizing moves no state · 5.9.3, 5.13, 6.4.2 · Own text replaced a child claimant with `% !LOOM child: <key>` but kept an inclusion line as written, so a node hashed differently once a child moved into `nodes/<id>.tex`. An inclusion of a file whose nodes are the parent's children now leaves the same markers, one per node the file provides. · Atomizing one node is meant to be a move, not an edit: its own text is untouched and its state must not move, and the enclosing section's state should not move either, since nothing it says changed. Existing quilts see such a parent go stale once, which is the whole cost; loom's synthetic quilt carries the migrated ledger and the vendored fixture is unchanged by it.

DR-125 · 2026-09-17 · A library is built by depth and subject, planned from metadata and fetched under a cap · 4.2, 4.8, 8.9, 8.13, 8.13.5, 12.2 · `loom refs crawl plan` identifies the cited works, follows their references to `[crawl] depth` within `[crawl] subjects`, writes a record per work and `refs/crawl/plan.json`, and prints what a fetch would download; `loom refs crawl fetch` downloads it in order until `[crawl] cap` downloads are on disk; `loom refs crawl status` counts the plan's works. Planning needs `[refs] resolve = true` and fetching also `[refs] fetch = true`; the `[crawl]` table is new. A plan records a fingerprint of `[crawl]` and the bibliography's files and fetch refuses a plan made from others. · WQ-02 described a crawl bounded by references per paper. The author, redesigning it on 2026-09-17, made the controls subjects and depth, asked for a hard download cap raised only in the quilt's config, and for the number to be downloaded to be known before anything downloads; that splits planning, which touches only metadata, from fetching, and a plan that no longer describes the quilt must not be fetched. The existing consent keys are reused because the plan sends what `refs resolve` sends and fetch retrieves what `digest fetch` retrieves. The cap counts downloads on disk rather than downloads made by one run, so running fetch twice never exceeds it.

DR-126 · 2026-09-17 · The author names the subjects and categories a crawl keeps; loom surveys, and infers neither · 4.2, 8.13, 8.13.1 · `[crawl] subjects` lists MSC families and keeps a work when any of its codes is in one; `[crawl] categories` lists arXiv categories and keeps a work with no MSC code when its primary category is one; a work with neither is excluded, and every exclusion is listed and counted by family and category. Loom holds no MSC-to-arXiv table and no default for either list. Deeper than depth 1 and with no subjects, `plan` surveys: it classifies the works the cited works cite and counts them by primary family, by any code, and by category, and writes no plan. A work zbMATH does not know under the identifier cited is classified through a search by title and first author. `scripts/msc_arxiv_categories.py` counts the categories papers in given families are filed under. · Plan 0.8 took the default subjects from the cited works' MSC codes and admitted unclassified works through a hand-written table from MSC areas to arXiv categories. On relloc the primary-code default left out algebraic groups (`14L`), the primary family of 20 of the works the cited works cite and a code of 45; measured on 1,675 arXiv records from June 2025, the table held the category authors chose for 93% of algebraic-geometry papers but 75% of PDE and 38% of ODE papers. The author ruled that the default must not be fixed but computed from a source or specified by the user, starting with configuration, and asked what an analyst or topologist would lose to a table tuned on algebraic geometry; the survey is the computed part, informing a choice the configuration makes. Matching on any code rather than the primary one, excluding works with neither, and surveying only the cited works' references were the author's choices. The title search is kept because without it zbMATH's classification of Fulton's *Intersection Theory*, Kontsevich's torus-actions paper and Laumon–Moret-Bailly was unreachable from the DOIs other papers cite.

DR-127 · 2026-09-17 · A work's references are the union of three sources, and one work is one record under every identifier · 8.13.2, 8.13.3, 8.13.4, 12.10 · A work's references are merged from zbMATH Open, OpenAlex and the `\bibitem`s of its own source once downloaded. Identifiers are compared normalised and every identifier of every work is indexed, so a reference under another scheme joins the known work and records a source links are merged; strong lookups are followed and possible ones listed. Records are `refs/<scheme>/<identifier>/work.json`, written atomically. An OpenAlex key is read only from `LOOM_OPENALEX_KEY` and sent as a header; a service's refusals are counted and named after the plan. `.bib` files under `refs/` and `ai/` are not the quilt's bibliography. · Each source misses what another has: zbMATH lists no references for Graber–Pandharipande 1999 and OpenAlex 19; OpenAlex lists none for the Aranha et al. preprint and zbMATH 46. The first relloc plan keyed works by their first identifier only, so a work gaining a DOI changed key mid-walk and EGA I appeared twice; indexing every identifier removed all five duplicates. OpenAlex references without an identifier were first looked up by bare title and so scored only possible (Behrend–Fantechi at 0.75); passing their authors and year made them strong. The key stays out of tracked files at the author's instruction. A fetched source's own `.bib` would otherwise have been read as part of the quilt's bibliography.

DR-128 · 2026-09-17 · Fetch counts what is on disk first, skips failures past the cap, and stops when arXiv refuses · 8.13.6 · Before downloading, fetch counts every download already on disk against the cap; a failure is recorded and takes no place under it, and a later run retries it only if a place is free. Three arXiv refusals in a row end the fetch with a message, as does an interruption, keeping everything already fetched. · Counting on-disk downloads as fetch reached them let a retried failure early in the order be fetched while later works already on disk were still uncounted, exceeding the cap on the second run. On relloc arXiv answered 406 to every e-print request for more than fifty minutes after a burst of other arXiv requests from the same machine, and a fetch that went on would have recorded a failure against every selected work.

DR-129 · 2026-09-17 · The graph page offers four drawings, and the layered one drops its section groups · 10.2.7, 15.5, 15.5.1, 15.5.2, 15.4 · The graph page's control names four drawings of one filtered graph: Dots (force), Box (results in layers), Sections (a card per section, its results inside, one line per pair carrying every dependency between them) and Reading Order (the document top to bottom with dependency arcs in the left margin). Box draws no section groups and leaves out the references that run from a section; a row of text fades only halfway where a shape is dimmed to a fifth. The local graph offers the same two laid-out drawings at neighbourhood scale, under a bar reading `Local Graph`, depth, drawing, expand, close, and its expanded dialog is moved to the end of the document so no panel can cover it. · The layered drawing that DR-115 repaired was correct and unreadable: on `demos/acgs`, 71 results inside 74 sections, with 89 of 172 edges touching a section, filled a canvas that only fits on screen at a zoom where no label can be read. Groups are the cause — a group spanning several layers reserves the whole column, and an edge leaving one is routed around the others — and the author, shown the current drawing beside three candidates on the same graph, kept all three: Box and Sections as replacements for the layered drawing, Reading Order as an addition. The fixture's figures were of a quilt too small to show the fault, which is why it survived a by-eye check.

DR-130 · 2026-09-17 · A macro inside `\\text{…}` is published between dollars · 9.4.1, specs/dialect.md §5 · Math is published as the author wrote it apart from three rewrites: `\\label` removed, `\\ref` replaced by its number, and now a macro of the author's whose body only math mode accepts, used inside a `\\text{…}`, written between dollars with its arguments. A macro whose body is words is left alone, as is one already inside math. · `\\text{nodes of \\ul C}` compiles, because LaTeX's `\\underline` opens math itself, and MathJax's text mode has no `\\underline`: it refuses the whole formula, which is how the ACGS proof came to show its own source in the viewer. No MathJax configuration reaches this — text mode takes its macros from its own package and neither `tex.macros` nor a `textmacros` block overrides them — and `\\text{nodes of $\\ul C$}` is the same document to LaTeX. The alternative, leaving it to the reader to write the dollars, asks an author to write for a typesetter they never see.

DR-131 · 2026-09-17 · A checkout's build cache notices loom's own code · 9.1, 12.2 · The fragment hash carries loom's version, as before, and where that version says `dev` a fingerprint of the `.py` files of the installed loom as well, read once per process. `loom build --force` renders every fragment regardless. · The version is what tells an upgraded loom to re-render, and it works for a release; in a checkout it stays `0.1.0.dev0` through every edit, so a quilt built before a converter fix kept serving the text the fix was meant to repair, and only deleting `build/cache` recovered it. The author hit exactly that after two converter fixes. Hashing the code costs one pass over loom's sources per process, which a served quilt pays once however many times it rebuilds. `--force` stays because a cache is a guess about inputs and a person should be able to overrule it without deleting a directory.

DR-132 · 2026-09-17 · The workbench layout: `drafting/`, `canon/`, the project's name, and the record's own directory · 1.3, 3.2, 3.9, 3.10, 4.1, 4.2, 4.3, 4.4, 4.6, 4.7, 4.8, 4.9, 4.10, 12.1, 14.5, 17.1, 17.2, specs/fixture.md · `[quilt]` gains `name` (the project's name, published as `corpus.name`), `drafting` (the working documents, every one live) and `canon` (the landmarks: flat, self-contained, never scanned), and documents a `history` key that `init` never writes. `drafts` is read as `drafting` in an older quilt with `loom:deprecated-config-key` and nothing moved; `drafting` wins when both are present; `loom upgrade` renames the key in place and moves `.loom/snapshots/` into `<history>/texts/`. A person's user config may set the four, and the quilt's value wins. The vocabulary gains atomic and linear format, live and superseded, and the verbs `draft`, `canonize`, `stamp`, `fork`, `revert`, `live`, `linearize`; `canonicalize` and `canonise` print `canonicalize → canonize` rather than run silently. · "Drafts" was the wrong word for the directory an author works in: a draft is a version of the paper, which is what the canon holds. Placement rather than a list is the only honest declaration of which documents are live, because a file cannot say whether it is being worked on and a list is a second source of truth (1.3, sign 4); naming the directory for it is not naming a directory for a property loom can compute (sign 10), for the same reason the masters directory never was. The snapshots move because they are already content-addressed, so every acceptance row still resolves, and because one store for snapshots, versions and anchors is one thing to reason about; it is the only deletion loom performs outside `build/`, and it is inside loom's own directory. A silent alias teaches the wrong name, so the aliases print.

DR-133 · 2026-09-17 · P13: loom verifies what it can, reports what diverges, repairs nothing, and never blocks work · 1.2, 1.5, 17.1 · The thirteenth principle. The head is always the files on disk; the history is a log of what loom was told, never a claim about the filesystem. · Every check this round added had to answer "and then what?", and the answer was always the same: say so, name the command that would resolve it, keep building. The principle was the author's, stated in the design conversation, and writing it down is what keeps the next check from being tempted into repairing something.

DR-134 · 2026-09-17 · The history: steps and stamps, quilt-wide numbering, versions per key · 3.3, 3.5, 7.1, 7.2.2, 7.9, 17.2–17.6, 17.17, WQ-25, WQ-29, WQ-30 · `.loom/history/` holds `ledger.jsonl` (one JSON object per line, appended only, written after the files it describes), a directory per step holding one file per key whose text moved plus the preamble and the document, and `texts/`, the content-addressed store shared with acceptance snapshots. Steps are numbered once over the whole quilt; a version is addressed `rl-0001@3` or `rl-0001@paper-v2`; a proof's version records `of: rl-0001@2`. A step's `parent` is declared, inferred by key-hash overlap, or unknown, and says which. · A per-node counter cannot place two nodes on one time axis, and the question a history answers is about a moment. Versions are per key because acceptance, hashing and annotations already use that unit. The three cases of a proof and its statement changing — one, the other, both — are only distinguishable if a proof's version records which statement version it stood against. The ledger line is written last so that an interrupted step leaves a directory nothing refers to, rather than a line pointing at files that do not exist.

DR-135 · 2026-09-17 · A node is defined once and included many times; a second definition is conflicted, not resolved · 4.4, 4.9, 4.10, 5.1.1, 5.3.2, 5.3.3, 5.3.5, 6.5, 17.12, 17.14, specs/diagnostics.md §2, specs/manifest.md §3 · Liveness is a property of documents: a document is live when it sits in the drafting directory and no conversion recorded that its output superseded it. Only live documents define what they hold inline; every other scanned file defines as before. Two files defining one id leave it conflicted — no text, no winner — with `duplicate-id` naming every file and carrying a fix per file. `atomize` and `linearize` record supersession instead of writing an ignore line into the author's file, and `loom live` reverses it. The id allocator consults the history as well as the source, so an id it has recorded is never allocated again; a retired id written under again is recovery or reuse, told apart by whether the text is one the history recorded. · Choosing between two definitions by walk order is the one outcome nobody can debug: the manifest publishes one, a bundle inlines the other's closure, every hash asks for "the text" of something that has two, and an agent edits one copy while the other silently disagrees. Supersession replaces `--ignore-src` because a conversion knows with certainty what it replaced, and two mechanisms for one fact is one too many. A stored maximum goes stale the day someone writes a label by hand, so the allocator recomputes and now reads the record too.

DR-136 · 2026-09-17 · Diagnostics carry structured fixes and say what they are about · 5.14, 10.2.5, 10.6, 15.3.5, 17.15, specs/diagnostics.md §1, §3, specs/manifest.md §11 · A diagnostic may carry `fixes`, a list of `{label, command}`, and `subject`, `source` (the default) or `record`. Loom emits nine new codes: `loom:superseded-file`, `loom:canon-edited`, `loom:history-missing`, `loom:history-edited`, `loom:history-corrupt`, `loom:dangling-ancestry`, `loom:id-reused`, `loom:node-recovered`, `loom:no-live-document`, and `loom:deprecated-config-key`. `loom:undefined-node` and `loom:orphan-node` were considered and not added: `dangling-link` and `unreachable` say exactly those two things, and a synonym is the mistake that made `duplicate-id` ambiguous in the first place. A "text of @N" badge is metadata on the key, never a diagnostic. · The panel that reports a conflicted id has to offer three different resolutions, and a sentence of prose cannot be clicked. A fault in the source and a fault in loom's own record are different kinds of work — one is mathematics, the other is housekeeping — so the viewer groups them apart. The fixes are data because a viewer that ran a command would be a viewer with a write path, which the interface does not have.

DR-137 · 2026-09-17 · `import` writes one flat landmark; `draft` is where a paper becomes source · 4.5.5, 4.7, 6.1, 6.2, 6.3, 6.7, 6.8, 6.9, 12.11, 17.7 · `loom import` linearizes the paper's master into one canon document, copies the non-`.tex` closure to the root, inserts nothing, runs the identity test, and records step 0001. `loom draft` copies a landmark into the drafting directory, inserting `\usepackage{loom}` and an id on every node, with `--fix-anchoring`, the report, and the identity test; the anchoring refusal moves there with the label insertion. `init --from` is init then import and remains one transaction (DR-106). · A landmark must be the paper as it arrived, and a working document must be loom's own file, which P7 has always allowed it to edit; both are true only if they are two files. One flat file rather than a copied tree was the author's choice: every canon document then has the same shape whoever wrote it, and a landmark that is one file is a landmark that can be sent to a coauthor. Anchoring matters only where labels are inserted, so it belongs to `draft` and not to a copy that inserts nothing.

DR-138 · 2026-09-17 · `atomize` records what it superseded; `--retire` moves the file; `--ignore-src` is withdrawn · 4.8.1, 4.8.2, 4.9, 6.5, 12.11, 17.12 · Supersedes DR-88. The ledger line names the file the spine replaced, which makes it inert; `loom live` reverses it; `--retire` moves it into `retired/` instead. Loom no longer writes `% !LOOM ignore` into an author's file. · Writing a directive into the author's file was the one place loom edited source, and it existed only to record a fact loom already knew. Moving the file is the author's request rather than loom's initiative, which is why it is a flag; `retired/` is not scanned, so the fact is recorded by the move.

DR-139 · 2026-09-17 · `assemble` is dropped; `linearize` replaces it; a landmark carries loom.sty's macros inline · 3.10, 4.4.3, 6.6, 11.5, 12.11, 17.13 · `loom linearize SPINE --to FILE` flattens a document with the `\nest` level shift, refuses when it would inline a node another live document includes, and offers `--fork` or `--keep-shared`, which writes its reason beside each inclusion it left. `loom canonize` replaces `\usepackage{loom}` with the package's three `\providecommand`s in a marked block, and `loom draft` swaps the block back. `loom assemble` is deleted; `loom inline` stays as the surgical reverse of one atomization. · Nothing else lived in `assemble`, and what it did needed to know the identity rule: flattening a shared node file is exactly how a node comes to be defined twice. A landmark must compile in ten years in a directory holding nothing else, and `\uses` and `\incomplete` are part of the text; `\providecommand` makes the inline block harmless where the package is also loaded.

DR-140 · 2026-09-17 · `ai promote` keeps the digest case only · 3.8, 11.7, 11.8, 12.11 · A drafted node is not promoted. The author reads it in arras, copies it, and pastes it where they decide, taking an id from `loom id --next` or letting `loom id FILE` write one. `loom ai promote` of a node file refuses and says so. · The author's decision: pasting is more intentional, it forces the reader to decide where the node goes, and it is the same act as accepting any other suggestion. It also removes the only command that filed a stranger's lemma into `nodes/` with an id allocated on its behalf.

DR-141 · 2026-09-17 · Landmarks, the project's name, the conflicted state and structured fixes reach the viewer · 9.1, 9.2.1, 9.3, 9.8, 10.1, 10.2.2, 10.2.5, 10.3, 10.6, 10.8, 15.2, 15.3.1, 15.3.5, specs/manifest.md §1, §2, §3, §4, §8, §11, §15, specs/dialect.md · The manifest gains `canon`, `corpus.name` as the project's name, a `conflicted` state label, a placeholder node and key for a doubly-defined id, `version` on a key, and `subject` and `fixes` on a diagnostic; fragments gain `data-fragment="canon"`. Arras gains `/canon/<stem>`, a two-group document picker, an empty state for a corpus with nothing being worked on, the conflicted display wherever a node appears, the version badge, and copyable fixes on the problems page. Everything is additive and the interface version stays 1. · A landmark nobody can read is a landmark nobody trusts, and the viewer is where an author reads. A canon theorem has no identity by design, so the graph, the review table and the annotation surfaces know nothing about it and need no change; the one thing it needs is a page. `corpus.name` was named separately from `root_label` because a corpus's name and its default document's title are different things and conflating them showed the paper's title where the project's name belonged.

DR-142 · 2026-09-17 · One generator writes the demo, the synthetic quilt and the conformance fixture · 13.4, 14.3, 14.6, specs/fixture.md, demos/README.md · `loom/scripts/gen_quilts.py` writes `tests/quilts/demo/`, `src/loom/assets/demo/` and `tests/quilts/synthetic/` from `tests/quilts/sources/`, by running loom's own commands under a fixed clock and applying the patches loom prints as an author would; `--check` regenerates and compares byte for byte, and a test runs it for both. `demos/build.py` drives it in the workspace and rebuilds the paper quilts in an isolated environment; `demos/refresh.sh` and `demos/hermetic.sh` are deleted. Nothing a fixture holds may depend on the machine that wrote it. · The fixture quilts' records were already made by running loom's commands, but only a note in a README said so, and nothing checked it; an edit by hand would have been invisible. Generating them makes that claim a test, and it makes the demo and the fixture one thing rather than two that have to be kept equal. The author's instruction to delete and rebuild the demos is what made it cheap to do now.

DR-143 · 2026-09-17 · Where the app is served and where the corpus is served are two settings · 10.1, 10.7, 10.8.1, 15.6 · The base path is build-time (`ARRAS_BASE`, empty by default), because SvelteKit resolves routes and asset URLs against it; the data root — the build directory holding `manifest.json` and the fragments — is settable at run time and defaults to `build/` beside the app. Both live in `src/lib/paths.ts`, and every URL the core emits is composed there rather than written. `tests/unit/host-neutrality.spec.ts` fails on an origin-absolute literal, a write to `:root` or `documentElement`, or a publisher's scheme in `src/lib/`, with a named list of the sites that predate it. · Six call sites hard-coded `/build/` and nine functions built absolute routes, so arras could only ever be served at an origin root with the corpus beside it. Making the base build-time and the data root run-time is the smallest split that serves both futures: a site under a prefix needs the first, and a page that is not this application — a hand-written page embedding one node of a corpus served elsewhere — needs the second without rebuilding anything. Defaults are byte-identical to the previous behaviour, which the unchanged end-to-end suite and screenshots are the evidence for. The guard is a boundary test in the spirit of the forbidden-words guard: its value is that a boundary already decided is not eroded one plan at a time, which it demonstrated by rejecting a publisher's command in a comment written while it was being added.

DR-144 · 2026-09-17 · The reference crawl leaves loom for weft · 4.2, 4.8, 8.9, 8.13, 12.2, 12.10, 12.11 · `src/loom/refs/crawl/` (ten modules), the three `loom refs crawl` commands, the `[crawl]` table and its validation, the two research scripts and the 31 crawl tests are deleted. `[crawl]` stays in `CONFIG_KEYS` as accepted-and-ignored and `loom upgrade` removes the table, as `[ai] runner` is handled. loom keeps everything a single project needs: the bibliography and its identifiers, `refs resolve`, `refs add`, `refs path`, `digest fetch` at depth 1, extraction, digests, bundles and the review ledger. weft copies the code from **loom d053b09**, the commit before this removal, and maintains it separately; the boundary is `docs/plans/weft-and-loom.md`. · A crawl is corpus machinery living in a paper's tool, and the invariants pull in opposite directions: loom promises no database, no service clients beyond the two lookups, and no rate budget, while a corpus tool needs all three. Deleting rather than deprecating is right because nothing outside `refs/crawl/` read its storage, the table was never in the config template, and git holds the code weft ports. The removal is subtraction only: `refs/resolve.py` and `refs/identity.py` survive byte for byte, because every symbol the crawl borrowed has other callers in `refs resolve`, `scan/lint.py` and `render/manifest.py`. DR-125 to DR-128 stay in this appendix as the history of a decision that stood and has now been retired.

DR-145 · 2026-09-17 · A config table that is not a table no longer crashes loom · 4.2 · `QuiltConfig.from_dict` warned that `[refs]` was not a table and then read it as one, raising `AttributeError` on any `config.toml` containing a line like `refs = "yes"`. Every table read now goes through one accessor that yields an empty table for a scalar, so the warning is the whole consequence. · Found by the test written to replace the crawl's, which was the only exercise of the config warning machinery; the crash had been reachable since the warnings were added and nothing covered it.

DR-146 · 2026-09-17 · An absent manifest section means an empty one · 10.8.1, specs/manifest.md §1, specs/fixture.md §5 · Seventeen top-level sections were required, and an absent one threw at the first render of any route, with no `+error.svelte` to catch it, so a publisher that omitted a section it had nothing to say about blanked the app. `parseManifest` now ends in `normalise()`, which fills every absent top-level section with its empty value before the cast; `interface_version` and `publisher` stay required, `interface_version` stays 1, and a malformed value is untouched and still the publisher's error. `scripts/build-prerender.mjs` reads the manifest outside the app and guards its six sections itself. `docs/specs/fixture-minimal/` holds the floor, omitting eleven sections outright rather than writing them empty, and `arras/tests/e2e-minimal/` renders every route against it in CI. · Two publishers are coming — weft and the author's site generator — and neither writes the sections a paper's tool writes, so this stopped being debt and became a prerequisite. Doing it in the loader rather than at the forty-odd access sites keeps every consumer's type total and puts the guarantee where the boundary is: the publisher may omit, the viewer normalises. Making the sections optional in `types.ts` instead would have pushed a null check into every view and every future view, which is the same work paid forever.

DR-147 · 2026-09-17 · Two base-path sites R1 missed, and the guard rule that missed them · 10.8.1 · `/blockers` redirected to an origin-absolute `/review?show=incomplete`, and both shells linked home with `<a href="/">`. The host-neutrality guard's route rule required a lowercase letter after the slash, so a bare `href="/"` was invisible to it; the rule now matches any `/` not followed by a second slash or a glob star. · Found while mapping the boot path for DR-146 rather than by the guard, which is the argument for widening the rule rather than only fixing the three sites.

DR-147 · 2026-09-18 · Annotations are an append-only log · 7.4, 7.5, 7.8, 11.6, 3, specs/manifest.md §9, specs/diagnostics.md · `annotations/log.jsonl` at the quilt root replaces `comments/<author>/<date>.json` and `ai/runs/<run>/annotations.json`. Events `created`, `replied`, `edited`, `resolved`, `discarded`; current state by replay; `run` and `author` separate columns; annotations gain `severity`, `payload` and `placement`, and `citation` joins the kinds. A person's annotations still group by day. · A record was a file rewritten whole whenever anything in it changed, which is why the book had to say `status` was "the one field loom rewrites in place" and why a re-check could only reply. Three passes over a finding that still stood left it wearing three restatements of itself. An `edited` event supersedes a body with the history kept, so one finding stays one finding and a reply goes back to meaning dialogue. The rewrite also killed a live bug: `loom comment --run R --resolve <id-in-R>` lost the resolution, because the parent and the writer were two objects over one file and the second write clobbered the first — the case an agent hits on every re-check, which the one test covering resolution happened to avoid.

DR-148 · 2026-09-18 · `loom bundle` goes; the closure document stays · 9.7, 11.5, 12.2, 12.11, Appendix C, Appendix D · The command is withdrawn. Reading a key with its dependencies is `loom source KEY [--closure]`, which prints; checking that a proposal or a draft compiles is `loom compile KEY --with FILE` and `loom compile --draft FILE`. `build_bundle` is untouched and still writes `build/bundles/<key>.tex`, because `loom compile KEY` and `loom check --bundles` already ran latexmk on it. · Two jobs were fused in one command and only one of them wanted a file. A bundle copied into a run went stale the moment the author edited, and an agent reasoning from it reasoned about text that no longer existed; printing removes the artifact, the gitignore line it needed, and that whole failure mode. Verification genuinely needs a compile, and `loom compile KEY` was already doing the bundling, so `--with` is one option on a command that did the work rather than a second mechanism beside it.

DR-149 · 2026-09-18 · `loom ai start` stops launching the agent · 11.2, 11.3.3, 11.4, 4.2, 12.2, 12.11 · `launch_agent`, `launch_prompt`, `[ai] agent` and `--no-launch` are deleted; `[ai] agent` stays accepted-and-ignored and `loom upgrade` removes the line, as `[ai] runner` is handled (DR-111). `run.toml` becomes `created`, `name`, `discarded`, the name authored and mutable. Retires DR-72. · Launching the agent meant running it as a subprocess of loom, owning its argv convention and holding the process tree for an entire interactive session while forwarding an exit code and nothing else — the runner declined as WQ-15, in miniature. It was also redundant: `loom ai init` already writes `CLAUDE.md`, whose one sentence is the same instruction the launch prompt passed. The author types their agent's own command, and `loom ai start` is what the agent runs when asked to begin a run.

DR-150 · 2026-09-18 · Record intent, derive fact · 11.4, 11.4.7 · `run.toml` holds only what nothing else can supply. A `target` field was designed and dropped: it is not scope, since a run may annotate any number of keys and nothing restricts it, and as "what a viewer opens on" it is derivable from the annotations. The modes a run used and their order are derived too, from the `<mode>-<target>.notes.md` files it wrote. Runs are addressed by name, by a prefix of one, or by path; an ambiguous prefix names its matches and refuses. · A declared field that duplicates derived information drifts the moment a run does anything twice, and then a reader has two answers and no way to choose. Deriving also works backwards over runs already on disk, which a recorded field cannot.

DR-151 · 2026-09-18 · `CLAUDE.md` is the author's file with one line of loom's in it · 11.3.3, 4.2 · `loom ai init` and `loom upgrade` ensure one line is present in `CLAUDE.md` and `AGENTS.md`, replacing a known earlier version of it in place, and touch nothing else. They previously wrote both files whole whenever the content differed from the shipped asset. · The file is where the ecosystem expects a person to put instructions for their own project, and an author who added any lost them on the next upgrade. Loom already had the right pattern twice over — one appended line in `.gitignore`, and hash-tracked mode files that keep an edit and write `.new` beside it — and the file with the strongest claim to being the author's had the crudest policy of the three.

DR-152 · 2026-09-18 · An annotation's text is frozen when the author edits under it · 7.5 · `.loom/last-seen.json` holds the own text of every key as loom last saw it. On each build, a key whose text moved and whose old hash an annotation points at has that text written into the history's `texts/`. Two edits between two scans leave the annotation unanchored rather than anchored to a version loom never recorded. · `write_snapshot` already stored texts content-addressed and write-once, but only `loom accept` called it, and nothing held the previous text to write: once the author saves, it exists nowhere, since loom reads files and not history. The cache keeps text rather than hashes for exactly that reason. A key nobody annotated costs one hash comparison, so an unreviewed quilt pays for the cache and nothing more.

DR-153 · 2026-09-18 · Review mode, and quick mode restored · 11.5, Appendix C, docs/source/global-rules.md · `ai/modes/review.md` is the author's mode 7: nine kinds of finding, each graded by severity, reusing `[referee-review]` for errors and prose rather than adding blocks. It writes no compiled LaTeX/PDF pair, which the mode's own text asks for. `quick.md` returns, rewritten as a short durable answer with `[answer]` alone. · Blocks are output structures catalogued once; modes are procedures that select and order them, so review emitting `[referee-review]` no more makes it referee mode than both emitting `[summary]` makes them the same — and that block already carries severity, location, fix and an annotation id per item. The PDF pair is superseded by annotations anchored in the source and rendered in place, with WQ-28 for anyone who wants an export. Quick was dropped in an earlier draft as producing nothing durable; walking a session end to end found the gap it fills, between a clarification in chat that vanishes and a mode that writes a five-block document.

DR-154 · 2026-09-18 · Reference notes, and a test that keeps the deny list honest · 8.9.1, 11.8 · `reference-notes.jsonl` records citation suggestions the author accepted: what the work was, which keys wanted it, which run proposed it. It never touches `refs.bib`, `refs/` or the manifest, deduplicates nothing and resolves nothing. `loom refs note` and the file are denied to the agent, and a test asserts that every loom command writing outside `ai/runs/` appears in the deny list. · An agent's citation suggestion names a work usually absent from the bibliography, so there is no entry to resolve and nothing for `loom:unresolved-work` to complain about; the author adds the entry and always did (DR-122). What was missing was a breadcrumb for the day they get round to it, since a verification done inside a run that is later discarded is a verification done twice. The deny list admits every new command by default, so it needed a test rather than a habit.


DR-155 · 2026-09-18 · `loom source` takes a document path; an agent can read a whole paper · 9.7, 11.4, 12.5, Appendix C, Appendix D · `loom source TARGET` prints a key's text as before, and when `TARGET` names a scanned `.tex` file it prints that document flattened, every `\input`, `\include` and `\nest` expanded in place with `\nest`'s level shift applied. It writes nothing. `--closure` is a key's option and is refused for a document, which already carries what it includes. Chapter 11's rule 7, which sent the agent to `loom linearize`, is replaced. · The rule contradicted the chapter that contained it: 11.4 told the agent to run `loom linearize`, and 11.5 denied it, as the shipped `.claude/settings.json` does. It was also advice that would have been destructive had it run, `linearize` superseding the spine and every file it inlined and reassigning `main` — and stale twice over besides, naming `$LOOM_RUN`, which nothing has set since DR-149, and `bundle`, withdrawn in DR-148. The engine was already pure and already there: `flatten(root, rel)` takes a root and a path and reads from disk, so what `linearize` adds over it is exactly the supersession the agent must not cause. This is DR-148's shape a second time — the command wrapping an engine was the part that did not belong in an agent's hands, and printing rather than writing removes the artifact, the staleness and the cleanup at once.

DR-156 · 2026-09-18 · `publishes`: the read side's capability block · 10.2, 15.2, specs/manifest.md §1 · The manifest may carry `publishes`, four booleans — `documents`, `review`, `bibliography`, `discussions` — saying whether the corpus is the kind of thing that has one. loom writes all four true for every quilt. A viewer derives the views it offers from it, and falls back to deriving from the data when the block is absent. A capability says what exists, never what to draw. · Deriving the answer from emptiness cannot tell "empty because not yet" from "empty because never". A quilt with no runs yet would lose a view and get it back later, and since the manifest is re-polled every second the furniture would move while someone worked. The value is a property of the publisher and its corpora rather than of this corpus's contents, which is why a quilt with no documents yet still declares that it has them.

DR-157 · 2026-09-18 · A run's pipeline is derived from what it wrote, and the viewer never parses a filename · 11.4, 15.8, specs/manifest.md §10 · A run thread carries `pipeline`: the modes it applied, in order, each with its target, its report, the rendered fragment and a block index. It is derived from the `<mode>-<target>.notes.md` files in the run directory, in name order with a numbered second pass after its first. A publisher with no modes emits none, and a viewer must not recover a mode by parsing a filename itself. · Every mode writes that file without exception, so the directory already says which modes ran and against what — retroactively, for runs written before the field existed. Name order and not clock order because two builds of one quilt must not disagree about a manifest. The prohibition on the viewer is the point: `referee-` is one publisher's vocabulary, and a viewer that reads it is a viewer that only works for loom.

DR-158 · 2026-09-18 · An agent's report is parsed into the blocks it was already written in · 11.4, 15.8, specs/dialect.md §2.1, specs/manifest.md §10 · Each `*.notes.md` becomes a fragment of `data-block` sections, one per bracketed heading, with every finding carrying `id="finding-<annotation>"`. The brackets and the trailing annotation id are carried as attributes and not shown as prose. A file with no bracketed heading becomes one unnamed block. Rendering gained a math pass — `$...$` lifted out before Commonmark sees it — and every element carries `data-src` into the notes file, taken from markdown-it's line maps. · `ai/rules.md` has always said "write each block under a heading with its name in brackets", every finding has always ended with its id, and nothing read either; the structure was there and only the reader was missing. Rendering the file whole was rejected because it gives a document you can read rather than one you can use — "show in report" has nothing to scroll to. Emitting blocks through a CLI was rejected because agents author markdown well and author through a CLI badly, and it invents a format that must then be versioned. The source offsets are what let a report be a fragment like any other rather than an exception to the rule that a fragment says where it came from.

DR-159 · 2026-09-18 · Reviewing a run is `/thread/[id]` rendered differently, and the second tab is the Journal · 15.8 · A thread of kind `run` renders as the document on the left and the run on the right, linked both ways: a finding scrolls the document to the sentence it is about, a mark scrolls the report to the finding that made it. A comment session keeps the list it had. The tabs are Report and Journal; there is no Transcript. Findings about the document come first, in a section of their own. · A run review is not a second kind of page, it is that page done properly, and a reader arriving from the index should not have to know which kind they clicked. "Transcript" was a word with nothing behind it — loom cannot see a chat and should not transcribe one — while `thread.md` is a journal and the orientation has always said so. The two-way link is the whole reason this is one page rather than two.

DR-160 · 2026-09-18 · A closure is a panel, never a route · 15.3.2 · The node page answers both closure questions without leaving it: the local graph, now navigable, for what a change would disturb, and a stacked closure at depth 1 or 2 for what the result rests on. The stack seeds from the result and its proofs, and is built only when opened. · A closure is a way of looking at a node, not a place to go, and the two questions are opposite enough that they belong beside each other. Seeding from the statement alone answers a question nobody asked: in most quilts a statement's own `uses` is empty and every dependency is declared inside the argument. Building it collapsed put six statements and their annotation marks on a page that shows one.

DR-161 · 2026-09-18 · The write API, with no bridge and no model behaviour · 10.1, 15.9, specs/write-api.md · `GET /_api` answers with the capabilities the publisher serves, and `POST /_api/<endpoint>` performs one write through the same library function the CLI calls. Six endpoints: `comment`, `reply`, `resolve`, `edit`, `discard`, `refs-note`. `discard` takes an annotation, not a record. There is no `message` endpoint and nothing wakes an agent. A viewer detects the API and shows no editing affordance without it. · The specification routed a reply through "the bridge", a component that invoked the runner declined as WQ-15, so the bridge had nothing left to invoke. The direction is the other way round and already works: an agent pulls, reading open findings with `loom status` and answering with `loom comment --reply`, which needs no credentials held by loom and no vendor flags to track. `discard` took a record from when there was a file per review; what a person withdraws is a finding, and it stays distinct from `resolve` because resolved means the fault was addressed while discarded means it should not have been raised — collapsing them loses the only record of which agent findings were worth having. §3's "no authentication in version 1" rests entirely on the socket binding to loopback, which is now a test rather than an assumption.

DR-162 · 2026-09-18 · An annotation's `run` is the thread's id · specs/manifest.md §9, §10 · One function says what a run or comment session is published under — a run by its directory name, a session by its path — and the annotation and the thread both use it. · DR-147 made `run` "the grouping key a viewer needs, which a file path no longer is", and then wrote a file path. A comment session matched by accident, since its id is its path; a run never did, so nothing could ask which thread a finding belonged to. The split view was the first thing that needed to.

DR-163 · 2026-09-18 · arras declares its tokens on a class the host applies, not on `:root` · 10.8, 15.6, plan 0.9.5 §§2-3 · `theme.css` declares every token on `.arras` and scopes its box-sizing reset to the same subtree; the app puts the class on `<html>`, which is where the preference attributes already go, so nothing about theming changes. A host restyles with an ordinary stylesheet at ordinary specificity. · Declaring on `:root` had arras defining `--ink` and `--paper` across a page it does not own and resetting `box-sizing` on elements that are not its own, which is a library a host cannot safely embed. This is the half of seam 2 that pays for itself as theming, kept when the component API was declined (WQ-31); the rest of that design is in plan 0.9.5 §8.

DR-164 · 2026-09-18 · An inclusion cycle stops the walk rather than the process · 9.3, specs/dialect.md §2.3, specs/manifest.md §7 · Two walks follow the inclusion graph — the fragment renderer and the manifest's inclusion tree — and each now keeps the files it is expanding and refuses to enter one twice. The renderer emits the empty `div.include` it emits for any unexpanded inclusion, carrying `data-cycle` with the chain that closed; the tree marks the node `"cycle": true` with no children. Both stop at the inclusion, never at a claimant, because a section claimant lives in the file it was already in and guarding those cuts every master's own sections off the tree. · `loom lint` has always reported `inclusion-cycle` as an error, and both walks followed the cycle anyway until Python's recursion limit stopped them — a traceback where loom had already written the message. The guard belongs in the walk and not in a depth limit: forty levels of `\nest`, deeper than LaTeX has sectioning levels, render fine, so depth was never what was wrong.

DR-165 · 2026-09-18 · The log's columns are written down, and a line that is wrong is reported rather than assumed · 7.4 · Chapter 7 carries the column list, spelled as the code writes it. `replay` reports four further conditions as `loom:foreign-annotations`: an `annotation_kind` outside the five, an `id` not of the form `a-YYYY-MM-DD-NNNN`, and a reply naming an annotation the log does not contain. None is corrected — an unknown kind is published as written, because `kind` is an open string a viewer must tolerate from any publisher. · The only written column list was in plan 0.10 and it said `annotation-kind` and `reply-to`, with hyphens the code has never written or read; Chapter 7, which is normative for the log, listed no columns at all. A log written from the documentation therefore lost its kind and its parent on every line, and said nothing, because an absent kind silently became `objection`. The silence was the fault rather than the value: a viewer tolerating an unknown kind is the interface working, while a publisher being told nothing about a column it spelled wrong is not. The id is checked because it becomes a DOM id and a URL fragment and would become a filename the first time anything stored one per annotation; a dangling reply is reported because it is otherwise in the record and on no page at all.

DR-166 · 2026-09-18 · The work-link scheme is `cited:`, not `loom:` · 10.4.1, 15.3.7, specs/dialect.md §2.13 · A link into a cited work is written `cited:SCHEME:VALUE#page=N` or `#quote=TEXT`. No other spelling is accepted; `loom:` is not read, and the host-neutrality guard forbids it in `src/` with nothing exempt. · The interface named its own URI scheme after one of its publishers, which is the same fault as a publisher-shaped manifest field and was hiding as a viewer problem: plan 0.9.5 §3 listed `worklink.ts`'s `PREFIX` as a vocabulary leak to be taken from `manifest.publisher`, and doing that would have *broken* conformance, since a corpus published by anything else would have had a viewer hunting for links the dialect says are written one way. Arras naming the scheme was arras implementing the format; the format was what named a publisher. `cited:` is the interface's own words for what the link is — §2.13 has always said it "names a place in a cited work by its global identifier" — and it collides with nothing: not the inner `work` identifier scheme, which would have made `work:work:8f2a…` for exactly the entries hardest to identify, nor `locator`, `source`, `external` or `\ref`, and it cannot be mistaken for a citekey, which §2.13 insists it never is. Renamed rather than aliased because no corpus outside the fixtures exists and none contains a work link.

DR-167 · 2026-09-18 · One `--run` resolver, which matches names before paths and creates nothing · 11.4, 7.4 · `find_run` is the single resolver every `--run` option calls. It matches a run's name and its directory slug first, then a prefix of either, and only falls back to a path when nothing matched; a path is accepted only when it is an existing directory under `ai/runs/`, and one outside is refused with the rule it broke. A value matching nothing is an error, exit 2. No code path creates a run directory except `loom ai start`. · Loom had two resolvers: the careful one behind `ai orient --run` and a bare `root / p` behind the automatic log, which then `mkdir`'d what it had resolved. Passing a run's name to any logging command therefore created `ai/runs/<name>/` as a sibling of the real run, and that empty directory shadowed the name in every later lookup — the read commands mined the ground the write commands walked on. The beta experiment lost nineteen findings to it: the agent's annotations were filed under a phantom run, `loom ai findings` reported "no findings yet", and nothing anywhere was an error. Names come first because a name is what the author and the orientation both use; a path is the fallback, not the meaning. The confinement to `ai/runs/` is the same rule the orientation states to the agent — an agent writes only in its own run directory — enforced where the path is resolved rather than trusted.

DR-168 · 2026-09-18 · A finding can be read back: the whole annotation, the same filters in both forms, and an honest `anchored` · 7.5.5, 7.7, 11.4, 12.9, specs/manifest.md §9 · `loom ai findings --json` carries `message`, `payload` and `placement`; withdrawn findings are hidden unless `--all`, which prints the reason each was withdrawn, and `--severity`, `--kind` and `--status` filter. One function applies every row filter `loom status` offers, so the text form and `--json` answer the same question, and the summary line counts the rows it is printed under. Each row carries its title and taxon, and the live annotations on the key, so `--severity`, `--kind`, `--status` and `--detached` filter there too. `loom unravel` fills the `ledger:` and `annotations:` blocks it has always headed. An annotation is `recorded: false` when the text it was written against is neither the current text nor a frozen version, and `anchored` is false with it. · Nothing in loom printed an annotation's message, so a second pass could not tell a finding it had already made from a new one — which is the contract 0.10 was built to support, and the beta agent's re-checks duplicated findings because of it. The three other faults are the same fault in different places: a command that heads a block and prints `(none)` under it makes a positive statement that the node is unreviewed; `--json` returning 150 keys where the text form returned 56 gives the flag a tool reaches for the answer a person would not accept; and a `status` line reading `rl-000G (Lemma) draft` sent the agent to flatten the master and grep `\label` to find out what the lemma was about. The discard reason was typed into the log by `loom comment --discard` and dropped on replay, so the one state change that carries a reason displayed it nowhere. `anchored` is separated from `detached` because they answer different questions and the quote can survive the version: reporting an annotation in good order when its subject is unrecoverable is the outcome plan 0.10 Part B set out to avoid, in its own words.

DR-169 · 2026-09-18 · The closure covers what is printed; a compile has three outcomes; a batch line is checked · 9.7, 7.4, 11.4 · A statement's bundle prints the statement and its proofs, so its closure is the union of theirs. `loom compile` reports `compiled with warnings` and exits 0 when latexmk exits nonzero but wrote a PDF and the log holds no `!` line, printing every warning, and names the log's first `!` line as the error or says there was none. `--with` takes a file first and then an annotation id, whose payload is the proposal. `--batch` refuses an unknown key, accepts `edit` and `discard` beside `reply` and `resolve`, and allows one verb to a line. `--severity` with `--kind ok` is refused. `loom id` and `loom new` take `--run`. · `ai/rules.md` tells an agent that `loom source KEY --closure` is complete context and that it may assume nothing outside it, and the statement form then excluded every lemma the printed proof invokes — the hostile agent called it "the one most likely to produce a confidently wrong review", and both agents hit it independently. The downstream cost was the same document failing to compile: latexmk exits nonzero on an undefined reference, `res.ok` was `returncode == 0 and pdf.exists()`, and so `loom compile rl-000G --with proposal.tex` and `loom compile rl-000G` both printed FAILED with an identical message, leaving the agent unable to tell its own proposal from a document that was already like that. The message itself was `  latexmk after you've corrected the files.` — the tail of latexmk's closing advice, reached because `first_error` fell back to the last line of stdout when the log held no `!`. A narrow `--statements-only` was considered and dropped: no caller wants the narrow thing, and the rule is better stated once than made optional. `--with` growing an annotation id and `--batch` growing `edit` and `discard` are the same observation — every verb the command line has should be reachable from the form a program uses — and an unknown key must be an error there for the same reason a typo'd flag is one on the command line.

DR-170 · 2026-09-18 · A shipped document points at the command that knows, and a document the author edits is kept · 11.2, 4.2, 3, specs/manifest.md §3 · `ai/orientation.md` joins `tracked_docs()`, so `loom upgrade` keeps an edited one and writes `orientation.md.new` beside it as it does for a mode file. The orientation stops describing the shape of a digest id and names `loom search CITEKEY --json` instead. `loom init` creates no `comments/` directory and writes no empty `[ai]` table; the quilt contract names `annotations/log.jsonl`; the generated permission file denies writes under `annotations/` rather than under `comments/`; `loom ai check` skips the annotation log and resolves its `RUN` argument like every other run address. `loom refs note --accept` writes the suggestion's payload as `work` and its message as `claim`, and refuses a suggestion that names no work. · One pattern, found in four places by two agents: loom ships a document that **describes** a fact instead of **pointing at** the thing that knows it, and the description goes stale. The orientation gave digest ids as `<citekey>-<label>` with an example whose citekey had no punctuation, so it could not show the rule that punctuation is stripped; the agent built an id from `refs.bib`, got "no such key", and standing rule 4 then had it write the dead id into prose, where nothing validates. The quilt contract said review records live in `comments/`, four plans after the annotation log replaced them, and `loom init` was still creating the empty directory — which left the deny list guarding a directory nothing writes to while `annotations/log.jsonl` stayed writable, and left `loom ai check` reporting the agent's own `loom comment` as a write outside its run. `orientation.md` was the crudest case of all: DR-151 gave loom three policies for files an author may also own, and the one document every agent reads first and every author tailors had none — an edit was overwritten with no warning and no backup. `refs note` is the same class one layer down: the record it wrote put the agent's prose in the field a bibliography entry would come from.

DR-171 · 2026-09-18 · A verb that names its annotation by id takes the one positional as the body · 7.4 · `--reply`, `--resolve`, `--edit` and `--discard` each name their annotation with the flag, so `loom comment --reply a-… "the answer"` reads that argument as the message. A reply with no message is refused; so is an `--edit` that changes nothing, and one that replaces a body with an empty string. A resolution with no message stays legal, because 7.4 item 7 designs it that way. · `--edit` and `--discard` did this and `--reply` and `--resolve` did not: they read the single argument as a `TARGET`, which the reply branch then overwrote from the parent, and filed `body: ""`. Exit 0, no warning. **The author's answer to a finding was being discarded** — the half of the conversation an agent's second pass exists to read, and the half that makes the review loop a loop. Loom's own fixture generator wrote the pattern twice, so the synthetic quilt has shipped two empty replies since 0.10 and nobody saw it. The guards go with the fix because the reason it stayed invisible is that an empty body was legal everywhere: with a reply refused when it says nothing, this class of mistake is loud the first time. `--discard` keeps its bodiless form — DR-168 already publishes "no reason given" for one — and `--resolve` keeps its own, since resolving without comment is a thing a person does.

DR-172 · 2026-09-18 · `loom status` is the author's to-do list: the literature is named, not counted, a section carries findings, and a verified transcription says so · 7.5.3, 7.6.2, 7.7 · Three changes to the one command every session opens with. **Digests by reachedness**: `status` lists the external keys the author's own arguments reach and leaves the rest out, `--include-digests` shows them all, and the counting line names both groups; external keys are never in the arithmetic. **Sections**: a section appears when it carries a live annotation, with its review facts and no state; `loom accept` refuses one, and `status --explain` answers for one. **Verified transcriptions**: `loom accept` on an external node prints `verified KEY as a faithful transcription of CITEKEY`, `status` and the review panel read `transcription verified`, and the stale cause is `transcription-changed`. The ledger value and the manifest state stay `accepted`. · On the relloc paper `status` printed 150 rows of which 92 were Manolache's, the author's own work started at line 93, and the summary read `1 incomplete; 92 loose` — the incomplete was Manolache's standing assumptions and every loose node was his, so two of six numbers were entirely somebody else's and both read as defects in the author's paper. Reachedness rather than digest-ness is the line because arras's review panel has drawn it since 0.9 and it is the better one: 14 of relloc's 93 external nodes are load-bearing and each is a real task, while the other 79 are there because one paper was digested carefully. Sections were the same fault in a different place: `loom comment` accepts a section, 11 of relloc's nodes are sections including where its orphan proofs live, and a `major` objection filed on one through the sanctioned command was stored and displayed nowhere. The transcription wording answers a finding that had read the mechanism backwards: accepting a digest was reported as "as though the author had settled it", when the row claims only that loom's copy is faithful. Measured rather than assumed — a key that depends on an external node reaches `proved` and `settled` while that node has never been accepted, so accepting one gates nothing and refusing it would have deleted a seal that costs nothing. What it buys is the diff on the day the copy moves, which is the one question a digest cannot otherwise answer.

DR-173 · 2026-09-18 · What an agent may run is one table, and nothing it writes is copied into the quilt · 11.7, 11.8, 8.9 · `AGENT_COMMANDS` in `ai/layout.py` names every loom command an agent may run. `ai/rules.md`'s `Never` section is generated from it, `loom ai init --permissions` generates `.claude/settings.json` from it, and the denied set is the complement of the command tree, so a command added to loom later is the author's until the table admits it. `loom ai promote` is withdrawn. Ingest mode no longer authors a digest: it runs `loom digest extract`, checks the output against the paper, and proposes corrections as a diff and as findings. · `§Never` named four of about sixteen mutating commands and the permission file named sixteen of twenty-three, and the two were separately maintained lists of one fact. They had drifted: `loom upgrade` — the command that rewrites the rules the agent is operating under — was in neither, and `loom canonise` was in neither although `loom canonize` was in the file, so the alias walked straight through the deny. An enumeration of prohibitions cannot be right about a command that does not exist yet; an allow-list can, and the read set is small and stable where the mutating set grows with every plan. Withdrawing promote follows from the digest decision rather than from any fault in the command: a digest an agent typed is unfalsifiable, an extracted one can be diffed against its source, and that difference is the whole reason a reader trusts a digest without opening the paper — so the agent's job there is to verify the extraction, which is also what makes accepting a digest (DR-172) mean something. The consequence is accepted deliberately: a work with no LaTeX source cannot be digested at all, and ingest mode says so and stops.

DR-174 · 2026-09-18 · The four verbs an annotation was missing, and the two bugs that hid them · 15.3.4a, 7.4, 9.8, specs/write-api.md · An annotation box carries `reply`, `resolve`, `edit` and `discard`, each gated on the capability the publisher advertises. The three that need text open a panel above the row; `resolve` is one click and the button that fired becomes its own undo in place, as `discard` does. `undo: true` on either endpoint appends the reverse event, and `loom comment --undo` does the same from the command line — which required the log to replay an undone `resolved`, something only `discarded` did. In a gutter slot `edit` and `discard` fold behind `⋯`, by container query. A reply is withdrawn with `discard` on its own id and an edit is reverted with another `edit`; nothing in the viewer deletes. · Plan 0.11 built six write endpoints and four surfaces, and the four were `comment`, payloads, reference notes and whole-document annotations — so an annotation could be created and never answered. Building the rest found two faults that had made the whole API look absent. **`loom serve` never rebuilt when anything was written**: the watcher's directory list still read `comments/`, the name the annotation log replaced in 0.10, and `.jsonl` was not among its suffixes, so `annotations/log.jsonl` failed both tests and a write through the publisher's own API changed nothing the reader could see. **`content-visibility: auto` clipped the read view's gutter**: it was added to `.fragment > section` for a 308ms settings click, it brings paint containment with it, and the margin apparatus is built out of children that leave their node's box — a gutter comment at `left: 100%`, a margin annotation opposite. Both were clipped and a gutter comment could not be clicked at all, which nothing noticed because a gutter comment had no controls until now. It is kept where nothing escapes and dropped in the read view, at no measurable cost: a settings click there is 23–32ms against the 308ms it was added for.

DR-175 · 2026-09-18 · Three colours for every taxon, and the compiled page as the default setting · 15.3.8, 15.5, 15.7 · `taxonTone` returns one of three tokens chosen by the taxon's published `style`: a result, a definition, an aside. The document and both graph drawings share it. The four settings a document is read in are `p1` (the compiled page), `p2` (the same with the compiled paper's page boundaries), `b1` (the measured column with a taxon accent) and `b2` (the wide web setting), named by their letters so the row fits, and `p1` is the default. · Eight hues, one per taxon by sorted position, used up every register the page had: an annotation wore `--state-incomplete`, which sits in the same family as the taxa, so a finding had nothing of its own and was hard to pick out of a coloured page — which is what a reader reported. Three by style leaves a register free, and the annotation hue is now one no taxon can wear. Style rather than name keeps what the eight had: a corpus whose taxa are Widget and Gadget is coloured like one using Bourbaki's, and nothing counts or orders taxa, so adding a node repaints nothing. A style the viewer has never heard of reads as a result rather than as nothing. **The paper setting is faithful to the compiled page rather than to a picture of it**: showing the PDF was considered and refused, because a PDF has no text nodes to anchor a quote to, no elements to hang a mark on and no links to rewrite, so every annotation, every `\ref` and every preview would stop working — the reader would have a picture of their paper beside a viewer that could no longer point at anything in it. `p2`'s boundaries come from the numbers loom already publishes out of the `.aux`, so a divider falls where a page really ended and its number is the one a reader would cite; where that cannot be known — between two numbered results, or in a master nobody has compiled — no divider is drawn rather than a plausible one.

DR-176 · 2026-09-19 · The reference layer lives under `loom refs`, and `refs fetch` absorbs `digest fetch` · 8.9, 8.14, 12.2, 12.11, plan 0.12 · Every command that reads, records or relates other people's results is a `loom refs` verb: `build`, `coverage`, `find`, `grep`, `page`, `overview`, `map`, `match`, `ingest`, `locate`, `propose`, `verify`, `discard`, `why`, `link`, `links`, `unlink`, `recheck`, `drop`, beside the existing `path`, `add`, `resolve` and `note`. `loom digest fetch` is withdrawn; `loom refs fetch` puts an artifact in `refs/<work-id>/` on a declared identifier, a strong resolver candidate, or an entry's `url` when it is a PDF. `loom refs build` runs every mechanical step and reports what is left in two handoff lines. Page text is committed; PDFs and sources are not. · Fetching and filing are one act, and it was spelled under `digest` while `refs add` did its manual half. The candidate rule is DR-122's line drawn where it belongs: a candidate is never an identity, but downloading on one is reversible, costs a request and is checked on arrival, and it took the study's bibliography from 2 fetchable works to 16. Everything stays inside DR-144: loom fetches only what the author's bibliography cites. Page text is committed because it is what an anchor is checked against, 3.35 MB for 1,317 pages in the study, and it lets a coauthor with no PDF re-check every anchor.

DR-177 · 2026-09-19 · `loom accept` refuses external keys; `loom refs verify` owns the transcription claim · 7.5.3, 7.7, 8.14 · `loom accept` exits with a refusal on a digest node and names `loom refs verify`, which takes a proposal id or an existing digest key and writes the ledger row `accept` used to write for one: `verified KEY as a faithful transcription of CITEKEY`, stale as `transcription-changed`. `loom accept` has one job, the author's claim about the author's own mathematics. · DR-172 patched the output — printing "verified … as a faithful transcription" from `accept` — where the command needed splitting: one verb was making two claims, one about mathematics the author owns and one about the fidelity of a copy of mathematics they do not. With proposals the second claim became frequent and the first remained rare, and an agent reading "accept" in the orientation could not tell which it was being told never to run. Two claims, two verbs, no shared word (plan 0.12 §5.6, decision C).

DR-178 · 2026-09-19 · An agent proposes anchored results for a work with no source · 8.6, 11.7, 8.14 · Ingest mode no longer stops at a PDF-only work. The agent reads it through `loom refs page` and proposes each result with `loom refs propose`; the source text is checked against the page before anything is stored, the rendering waits in `digests/<citekey>.proposed.tex`, which nothing inputs, and only the author's `loom refs verify` moves it into the digest. · DR-173 ruled out a typed digest because it is unfalsifiable, and accepted that a work with no LaTeX source could not be digested at all. The study bibliography's hard citations are exactly such works — a 1997 journal paper, a 679-page set of lecture notes — and the argument does not reach them: a proposal is anchored, so the copy is falsifiable at the one point that matters, the words on the page, and nothing enters the digest without a person. This narrows DR-173 to what it was about, the unanchored transcription, and keeps its consequence that an agent never writes `digests/` itself.

DR-179 · 2026-09-19 · The verification surface shows the page itself · 8.14, 10, 15; plan 0.12 §5.3, §5.4 · `loom build` renders the anchor page of every pending proposal from the local PDF into `build/pages/`, checked against the artifact's hash and never committed; the manifest's result row names the images and how far down the page the quotation starts, and the proposal box shows the page under the two texts, opened at the statement, or says there is no image when the PDF is not on the building machine. The box also lists the words of the rendering the quotation does not contain. · Plan 0.12 made "both texts, always" the non-negotiable and assumed the page text was the page. It is a text layer: one `X` for a stack and its coarse space, no superscripts or subscripts, display formulae as control bytes. In the study three wrong renderings passed every text check, one moving a hypothesis from the space to the stack; and the author, reviewing thirteen statements against the page text, removed the paper's own words twice in seven edits — "∈ S^m_Q" and a clause naming the weights — which the page image shows plainly. Both texts are necessary and not sufficient, and an author's edit is judged against the image too. The words check exists because a sentence in the orientation did not stop agents glossing statements; measured on the study's proposals it named every gloss and no faithful statement.

DR-180 · 2026-09-19 · A mechanical digest is checked, not trusted · 8.4, 8.5; plan 0.12 §4.3 · Extraction reads a cited paper's own idioms (a `\newenvironment` around a theorem-like environment is theorem-like on its counter; theorems declared after `\begin{document}` count), writes `numbering: mixed` when a compile succeeded but some numbers were counted, fills the `-setup` node from the paper's own conventions, and `refs build` flags a digest far thinner than its page count. · Plan 0.12 said a mechanical digest "is correct by construction". The study falsified it three ways: Behrend–Fantechi's digest was silently mis-numbered (its `lem-3.6` was the paper's Lemma 3.8, every definition being a wrapper on the shared counter), Romagny's and Totaro's extracted 1 of 27 and 0 of 13 results, and Chang–Kiem–Li's was a faithful copy of a preprint whose Theorem 3.4 is the published Theorem 3.5 with a different conclusion (DR-181). A mechanical digest is correct about the file it parsed; what can be checked mechanically — thinness, numbering provenance, version — now is.

DR-181 · 2026-09-19 · A fetched source records the identifier it was fetched on · 8.4, 8.9 · `loom refs fetch` writes `src.json` beside a source it keeps: the identifier (`arxiv:…`), whether it was declared or a candidate, and the date. `extracted-from:` reads it, so a digest extracted from a preprint under a published citation says so, and `loom:unverified-locators` fires. · DR-108 names a work's directory by its identifier, and plan 0.12 fetches a candidate preprint into the directory named for the work's DOI. The extractor named the artifact by its directory, so every such digest claimed `extracted-from: doi:…`, equal to `published-as:`, and DR-109's warning could not fire — for 11 of the study's 16 digests. An agent found the consequence by auditing Chang–Kiem–Li's digest against the published PDF: the preprint's numbering and a conclusion stated in A^T_* X where the published paper has A^T_* X(σ). A regression this plan introduced, restoring DR-109 rather than departing from it.

DR-182 · 2026-09-19 · A proposal is named by the paper's own number, and a numbered display may be a result · 8.14, digest contract §3.2 · `--local` must be `<abbrev>-<number>`, the number dot-separated parts each digits (with at most one trailing letter) or up to four capitals, or `<abbrev>-star-<n>`; the taxon is read off the abbreviation and a given `--taxon` must agree. `eq` is an abbreviation only proposals use: a numbered display the paper names as a result, with locator `Equation (n)`, written into the quilt's theorem environment. Every proposal is written into the quilt's own environment for its taxon, chosen by display name. The author may correct the name when verifying. · An agent invented `cor-2.3-quotient` for the first corollary a paper numbers within 2.3, and filed the paper's Corollary 3.2.1 as "Theorem 3.2": neither is findable by a citation by number, which is how contract §6.2 finds a result. Graber and Pandharipande's main result is their equation (1); proposed as "Theorem 1", its locator named something the paper never says. Plan 0.12 left the display case as an open question; the study answered it with a real paper.

DR-183 · 2026-09-19 · The anchor check is never called verification · 8.14, digest contract §9.5 · Loom and the contract call the mechanical test "the anchor check", a quotation "checked against the page"; `verified` is only the state a person's act produces. Under an agent marker `refs propose` ends by saying the proposal waits for the author instead of printing the author's command. · Contract §9.5 read "verification is source_text appearing at the anchor" while §9.9's `verified` means a person accepted the rendering, and `refs propose --help` said "verified against the page". An agent then reported thirteen proposals nobody had read to its author as "All 13 verified" — the collision plan 0.12 §5.6 forbids, reproduced from loom's own words.

DR-184 · 2026-09-19 · The digest view is the seventh view, and the graph draws what the author wrote · 10, 15.2.3, specs/manifest.md · The view list gains `digest` when the corpus publishes a bibliography; its route lists every cited work with what is known of it and carries the proposals, filtered in the URL rather than on a separate page. Every node in a digest file is external, and `/graph` draws no node that is in a digest and in no document. · Sixteen mechanical digests put 875 results and 226 section nodes into a graph of the author's 47, and a filter for them was a control nobody wanted on; the graph is what the author wrote and how it hangs together. The literature has its own view, and a list of proposals is that view filtered because a proposal is met while browsing the paper it belongs to, and needs both texts and the page, which no queue column can hold.

DR-185 · 2026-09-19 · The author's verbs refuse under an agent marker · 11.8, 8.14 · `loom accept`, `loom refs verify` and `loom refs discard` refuse when `AI_AGENT`, `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CODEX_SANDBOX`, `CURSOR_AGENT` or `GEMINI_CLI` is set; `loom refs link` requires `--run` under one. The write API is not guarded. The test suite and the fixture generator clear the markers, since they legitimately play the author. · An agent verified its own proposal in the first study run and loom recorded the author as the verifier, because the author's name comes from git and an agent's shell shares it; links an agent wrote were attributed to the author for the same reason. The allow-list (DR-173) keeps the verbs out of an agent's permission file, but a permission file is one vendor's and a command line is every agent's. The write API is the author's own click in the view that shows both texts and the page, so guarding it would guard nothing. The refusal also stops an author asking their own agent to verify on their behalf; that is the safe default, and the digest view is where verifying is meant to happen.

DR-186 · 2026-09-19 · A paper is compiled from a clean copy of its directory · 6.2, 6.7, 8.5 · `import`'s check and identity test, and `digest extract`, copy the paper directory into scratch without its build products (`.aux`, `.bbl`, `.bcf`, `.fdb_latexmk`, `.log` and the rest; a `.bbl` stays when the directory holds no `.bib`) or hidden directories, place any file the closure reaches outside the directory at the same relative position, and compile the copy. When latexmk leaves no `! ` line, the reported error is the first line of its collected error summary. · latexmk reads and rewrites the `.bbl` and `.fdb_latexmk` in its working directory even under `-outdir`. On the author's relative localization paper, an editor's leftovers made latexmk run bibtex where the paper uses biber, and the import refused a paper that compiles with "latexmk exited 12 with no error line"; the same runs rewrote the `.bbl` in the author's directory, a file the author wrote (4.8 rule 1). A copy makes the check depend only on the sources and leaves the author's files alone.

DR-187 · 2026-09-19 · `notes/` holds the author's reference material and is never scanned · 4.1, 4.8, 11 · A top-level `notes/` joins `ai/`, `refs/`, the canon directory and `retired/` as a directory the scan never enters. It is in version control, loom never writes to it, and the orientation lists it as context an agent reads and never cites, digests or proposes from. · An author collects material that is not a cited work — a colleague's draft, an excerpt, a note from a seminar — and wants it where an agent will see it. None of the existing places fits: `refs/` is keyed by a work's identifier and is loom's to fill, `digests/` is exactly what this material is kept out of, and `ai/` is rewritten by `ai init` and `upgrade`. It is excluded from the scan because a `.tex` someone else wrote carries labels, and scanning it would define nodes, or conflict with ids, the author never wrote. Committed rather than ignored so a coauthor sees the same notes; a file the author wants private is theirs to ignore.

DR-188 · 2026-09-19 · The quilt keeps its own bibliography, gathered from the canon documents · 4.1, 4.2, 8.14, 8.15, 6.2 · `digests/bibliography.bib` is the only bibliography loom reads. `loom refs scan` adds to it what every canon document cites — the `.bib` files it names, copied verbatim, and its inline `\bibitem`s, converted with their identifiers and a heuristic author, title and year beside the item's own text — and `import`, `canonize` and `refs build` run it. Loom only appends: an entry is never rewritten or removed, and two canon documents disagreeing keep the first. · The seed was `refs.bib`, which assumed the author keeps one. Four of the five real paper quilts do not: Manolache, ACGS, Kenig-Pavlović-Staffilani-Velasco and Manolescu-Marengon-Piccirillo all carry `thebibliography` inside the source, and nothing in loom read a `\bibitem`, so each had a bibliography of zero entries — no references in the viewer, nothing for `refs build` to resolve or fetch, and no way for a PDF to be filed against an entry. Reading them adds 208 entries across those four. Canon rather than drafting because a landmark is fixed, which suits a file that only grows; append-only because the heuristic fields are guesses and an author's correction must outlive the next scan.

DR-189 · 2026-09-19 · A quilt's own config settles who its records name · 4.2, 4.3 · `[author] name` is a key in the quilt's `config.toml`, read before the user config and git. A quilt that states an `[author]` table answers for itself: an empty name refuses, naming that file, and never falls through to `git config user.name`. `loom init` writes the table, empty unless `--author` gives a name. · The name came from the user config or git, so a record was signed by whoever's machine ran the command — and, as DR-185 found, an agent's shell shares the author's git identity. A quilt is usually one person's and can say so; where it does, no machine's identity can be mistaken for the author's, and where it does not, the old order still applies.


DR-190 · 2026-09-19 · The README `loom init` writes is an orientation for the author, with the contract as its last section · 4.7, 2.3, 5 · `src/loom/assets/init/readme.md` replaces `assets/readme-contract.md`: what the drafting directory, the canon directory, `nodes/` and `refs/` each hold, the promise that a canon file is only ever read and copied somewhere else, the draft–canonize loop with a worked example of both commands, and then the contract page. `init` substitutes the quilt's own directory names and id prefix as it writes it, as it already did for `.gitignore`. · The contract page answers "what may I write?" and a new quilt's first question is "where does my work go, and what is this canon directory for?" — which nothing in the quilt answered, since the loop and the never-touched rule live in Chapters 17 and 4.8. The contract stays, as the last section rather than the whole page, because 2.3 counts it as the one page an author needs and 4.4.4 and 4.6 require the README to state the root-relative compile and the Overleaf setting. Its examples were also `rl-0004` in every quilt; an author's own prefix is written in now, so an id in the README is never one to copy blindly.

DR-191 · 2026-09-19 · `loom init` asks who the quilt's records name · 4.2, 4.3, 4.7 · `init` asks for `[author] name` once, after the prefix and under the same rule: only when a terminal is attached and `--yes` is absent. `--author` is an answer, the empty string included, and no terminal, `--yes` or an empty answer writes the key empty. 4.3 rule 2's "no interactive prompt" is narrowed to the commands that need a name in order to act. · DR-189 made the quilt's own config settle the author, and left the key empty at init, so the first `loom accept` in a new quilt refuses and sends the author to a file to edit — a stop at the one moment the answer is obvious and the person is present. Asking costs one line and only happens where a person is sitting; an agent, a script and `--yes` see no question, which is what rule 2 was protecting.

DR-192 · 2026-09-19 · `refs/` is the author's seed space and `digests/storage/` is loom's store · 4.1, 4.2, 4.7, 8.14, 8.16 · `refs/` at the quilt root is where the author drops reference PDFs and `.bib` files; loom reads it and never writes it, and it is not in version control. Everything loom fetched or copied lives in `digests/storage/<scheme>/<id>/`, or `file/<hash>/` for a document that states no identifier, with page text and section maps committed and PDFs and sources not. `loom refs scan` copies what the store has not seen, keyed by content hash in `digests/storage/copied.json`: a document is copied once and never again, not when it is renamed, not when the author deletes it, and nothing is removed from the store when they do — it is a store, not a mirror. A `cited:` locator, `loom refs path` and the manifest's `artifacts.dir` all name the store. A document arriving for a work that already has one is filed under what it says it is and gets a sibling entry, `<key>A`, rather than replacing it: a preprint often carries results the published version drops, and a copy anchors are checked against must never change under them. A PDF the bibliography does not name is copied too and offered an entry, from its own identifier and a reference manager's filename; a title is never guessed from page one, where the longest early line is a sentence of the abstract. · One directory was doing two jobs. `refs/` was machine-managed, gitignored except for the page text inside it, and named by identifiers nobody types — but it reads as the place an author puts references, and an author who drops a PDF there found that nothing happened to it: `refs ingest` copied nothing it could not match to an entry, and `refs add` needed a citekey the bibliography already had, so a PDF with no entry had no way in at all. Two named places, one the author's and one loom's, make both halves of the seed work in the same direction as DR-188's bibliography: entries get documents fetched, and documents get entries offered.

DR-193 · 2026-09-19 · A flag is one run's consent, and a switch that is off says how to turn it on · 4.2, 4.8, 8.9, 8.14 · `[refs] resolve` joins `[refs] fetch` in the config `loom init` writes, both `false`. `loom refs build`, `loom refs fetch` and `loom refs resolve` take `--fetch` and `--resolve`, which allow the network for that run and write nothing. A build whose resolve or fetch step was off **and had work waiting** ends with a line naming the config key and the flag; a step that was off with nothing to do stays quiet. · DR-122 made the author's consent a config key, and it still is: the flags are per-run and never persist. But the key was undiscoverable — `resolve` was not in the config `init` writes at all, so an author had to read the book to learn it existed — and a build reported `(lookup off)` and `(fetching off)` beside counts of zero without saying that sixteen works were waiting behind them or what to do about it. Consent stated once in a file and consent stated once on a command line are the same act; what was missing was telling the author either was available at the moment it mattered.


DR-194 · 2026-09-19 · One quilt commits the PDFs in its store, and it is the one whose PDFs we wrote · 8.16, 13.4, 14.3 · The showcase quilt's `.gitignore` negates `digests/storage/**/paper.pdf`, so the two documents in its store are in version control. No other quilt does this and `loom init` still writes the ignore, unnegated. What makes it safe is that the showcase cites nobody: its two works are invented papers compiled from LaTeX under `loom/tests/quilts/sources/showcase-works/`, by a script beside them, so the bytes are the repository's own. · 8.16's rule is about other people's files, and it is the right default for every quilt an author will ever make. The showcase exists to be opened by someone who has just cloned a public repository, and the reference layer is most of what it has to show: a `cited:` locator that says "nothing is fetched, here is the publisher's page" demonstrates the fallback rather than the feature, the verification surface of 8.14 has no page to render beside the rendering, and `refs page` and `refs grep` answer from page text with no document behind it. Two invented papers at 44 KB each buy every one of those, and the negation is written into the showcase's own `.gitignore` with the reason above it, so nothing about it is inherited by a quilt made from the same template.

DR-195 · 2026-09-20 · Loom may hand a message to a local agent; it still runs no model · 4.8, specs/write-api.md, 11 · Rule 4.8.6 becomes: loom holds no credentials and calls no model provider, and may dispatch a message to a local agent the author is already running. A `message` endpoint on the write API is therefore permitted in a later version, detected like every other capability. · The rule was written for the proof of concept, and the discussion that produced it said it might change. What it protects is loom not becoming a vendor client: no API keys, no provider flags to track, no model behaviour to test. Dispatching to a process the author already started protects all of that and removes the thing the rule cost them — switching between the browser and a terminal to answer their own agent. DR-161 and `specs/write-api.md` §4 refused a `message` endpoint because it existed to invoke the declined runner; that reason does not cover handing a message to a session that is already open.

DR-196 · 2026-09-20 · The tools are directories of one repository · 16 · `loom`, `arras`, `loom-lsp`, `loom-nvim` and `loom-vscode` are merged into `loom-arras` at the paths they already occupied, each with its history; the five repositories are archived and keep resolving. Each tool keeps its own build, tests, release and CI workflow, scoped to its directory. `loom-nvim/` is pushed to `ikmartin/loom-nvim` by a workflow on every change, since a plugin manager installs a plugin from a repository whose root is the plugin, and the split reproduces that repository's own commits, so the mirror fast-forwards and a pinned commit still resolves. · A feature crossing the language server and its two clients was three commits in three repositories, reviewed and merged separately, with nothing recording that they belonged together, and a second developer joining made each such change three branches and three pull requests. The separate release cycles that 16 gave as the reason survive the merge; what does not survive is a reviewer having to hold three pull requests in their head.

DR-197 · 2026-09-20 · A quilt may commit the PDFs in its store when we wrote them, which is now two quilts · 8.16, 14.3, DR-194; plan 0.13 §4 · DR-194's rule was stated as "the showcase, and no other quilt". The principle it argued is narrower than its wording: what makes committing safe is that the cited works are invented papers this repository compiles, not that there is exactly one such quilt. The demo now has one too — `demo-works/calloway-fixed-loci.tex`, an invented paper about fixed loci of involutions — so `src/loom/assets/demo/` and `tests/quilts/demo/` negate the ignore for the same reason and with the same comment. `loom init` still writes the ignore unnegated, and `write_demo_quilt` takes `.gitignore` from `assets/init/` rather than from the demo's own copy, so no quilt an author makes inherits the negation. · `loom init --demo` shipped a hand-written digest, `method: manual`, about Manolache's virtual pull-backs — a real paper nobody holds a copy of. Under the artifact invariant (DR-198) that is renderable content with nothing behind it, so a fresh `loom init --demo` followed by the `loom lint` its own next-steps line suggests printed a warning on the first command a new user runs. The showcase had solved this a plan earlier; the demo simply had not been given the same treatment.

DR-198 · 2026-09-20 · Renderable content needs an artifact, and which artifact decides the guarantee · 8.14, 8.16; plan 0.13 §4 · Plan 0.13 §4 decided that a digest must have a PDF behind it. Relaxed: a digest must have **an artifact** in loom's store, and what it is settles what can be done with it. **Source** is the paper's own LaTeX — better evidence for a statement than a page image, and what `digest extract` reads — but carries no pagination and no page, so `loom:no-readable-copy` reports it as `info` and page locators into it stay unverified. **A PDF** adds the page, the anchors and verified locators. **Neither** is the state the invariant is about and stays a `warning`. `refs build`'s extract step no longer skips a work for want of a PDF, since source-derived content is backed. · The strict rule conflated two guarantees. Checkability is satisfied by either artifact; readability and pagination need the PDF, and only the second is about the PDF. Enforcing the first through the second cost `loom refs add` its only offline route for source — it refused everything but a PDF — and would have forced every fixture with a source-derived digest either to acquire a document or to record a warning as expected. What the strict rule was really protecting, and what the relaxation does not yet close, is that `pages/` is committed and `src/` is not: a PDF-derived digest ships with a trail a collaborator can re-check, and a source-derived one does not.
