INSTRUCTIONS FOR THIS DOCUMENT

This contains a running list of open instructions for modifying/fixing the projects contained in the loom-arras directory. Everything in the closed section is a completed/closed item recorded for historical purposes, the only open items are in the open section.

When pointed at this document an AI should do its best to resolve ALL open questions EXCEPT those marked with an [x] or a [FOR HUMAN] tag. Upon completing it, mark the item with [x] and include a brief indented summary of the fix/change applied below the item.

## Open
- annotations anchored to latex mangle mathjax rendoring. Please fix. Also the annotation view should 
- [x] loom is a little slow and could be optimized. Look for 5 potential optimizations and perform the 3 which are easiest to implement.
- [x] the viewer could generally be sped up and optimized. Many things feel a little slow. Clicking buttons in the settings is laggy. Test out 6 potential optimizations, implement the top 3.
- [x] [FOR HUMAN] this is a question to investigate rather than a change to implement: how are taxons implemented today? Is there a fixed list or is it computed from a source? It SHOULD be computed from a source -- anything defined in a newtheorem enironment for instance.
  Answer, no change made: **taxa are already computed from the source.** `scan/preamble.py` reads every `\newtheorem`, `\newtheorem*` and thmtools `\declaretheorem` in the master's preamble closure (its local `.sty` and `\input` files included), plus any `% !LOOM environment: name = Display, style` directive; each taxon's style is the `\theoremstyle` in force at its declaration. The manifest publishes that set as `taxa`, and arras colours by the published style, never by name (DR-175), so a corpus whose taxa are Widget and Gadget works the same. For a cited paper, `loom digest extract` also accepts theorems declared after `\begin{document}` and `\newenvironment` wrappers around a theorem-like one.
  Three fixed lists exist, but none of them decides what a taxon is: `COMMON_THEOREM_ENVS` in `scan/lint.py` only makes `loom lint` flag a `\begin{lemma}` that no preamble declares; `ABBREV` in `digest/extract.py` maps a display name to the short prefix in a digest id (`lem-3.6`), falling back to the environment's own name; and `ABBREV` in `scan/postnote.py` normalises the words in `\cite[Thm.~3.2]`. A preamble-declared environment with a name absent from all three is a full taxon.
- [FOR HUMAN] the modes should be reviewed and heavily modified. This likely needs to be verified and completed by a human.


## Closed
- [x] create a new relloc demo from scratch (delete the current one) along with the reference pdfs. Use it to create a detailed demo of all features in loom as they exist today, complete with a robust digest and an exhaustive set of example annotations. Mathematical correctness is not important here, the point is to showcase loom. In particular anchors to the pdf should appear in the annotations to showcase the pdf locator feature. This demo should be regenerable quickly, as with the synthetic demo, from assets in the repo (so copy the reluuative-localization/ reference pdfs into a stable location within the repo). This demo should be updated periodically after large changes and feature additions.
  Built as `demos/showcase/`, a third committed quilt beside `demo/` and `synthetic/`; `demos/relloc` is untouched, since it is the realistic corpus the measurements are taken on. Four decisions settled with the author first: **everything is invented**, because this repository is public and the relative-localization pile is third-party journal PDFs. The paper (*Balanced flows on finite quivers*) and its two cited works are LaTeX under `loom/tests/quilts/sources/showcase*/`, and `sources/showcase-works/build.sh` compiles the works to the 44 KB PDFs committed beside them, hermetically and reproducibly.
  **What it shows.** `atomize` into nodes and sections, two landmarks, a stamp, a fork into a beamer talk, a revert, `loom new`, and a sketch made live again over its own atomize, which is the `duplicate-id` conflict. The reference layer end to end: two PDFs dropped in the seed space and filed by `refs scan`, one digest extracted from a source and one built from proposals a run read off pages, of which the author verified two (one with the rendering corrected), discarded one with a reason, and left one pending, with its page image rendered beside it; two typed links. Nineteen annotations over five kinds, three severities and every state — open, resolved, resolved-then-reopened, discarded, superseded by `--edit` — on statements, proofs, a section, an equation, a whole document and a digest node, with replies in both directions, a detached one, two whose version loom never saw, a citation suggestion the author accepted, and `cited:` locators using `#page=` and `#quote=` that open the stored PDF in the viewer.
  **Regenerating** is `python demos/build.py --skip-papers`, or `gen_quilts.py showcase` alone: 7 seconds, offline, no TeX needed. 244 KB committed in the workspace. The store PDFs are committed by exception, which took DR-194 and a negation in the quilt's own `.gitignore`. `test_the_checked_in_quilt_is_what_the_generator_writes[showcase]` proves the committed copy is the generator's output, on the TeX tier because the page text comes from poppler.
- [x] resolve should be listed in the default config and set to false. When loom refs build is run and fetch and resolve are set to false AND there are files which could be fetched/resolved but aren't the message should say some version of "switch these flags in the config and rerun to fetch and resolve". There should also be --fetch and --resolve flags added and they should be suggested options
  `resolve = false` now sits beside `fetch = false` in the config `loom init` writes, so both switches are visible in every new quilt; before, `resolve` existed only in the book.
  `--fetch` and `--resolve` are on `loom refs build`, `loom refs fetch` and `loom refs resolve`. A flag allows the network **for that run only** and writes nothing, so the config stays the standing answer. A build whose step was off **and had work waiting** now ends with the line naming both routes — `75 entries could be looked up: set resolve = true under [refs] in config.toml, or pass --resolve, and run again` — and a step that was off with nothing to do stays quiet, so a quilt that cites nothing is not nagged. The refusals from `refs fetch` and `refs resolve` name the flag too. DR-193; book 4.2, 4.8, 8.9, 8.14.
- [x] when a new quilt is created it creates a README.md file in the new quilt. It should be an orientation written for THE AUTHOR primarily. It should explain the role of the canon/ and drafting/ and nodes/ clearly, as well as the canon/refs/ folder (initially empty). It explains that files in canon are never annotated or modified or touched in any way, they are only copied to other locations. It has a 2-3 sentence summary of the canon and drafting workflow with a verbatim small example showing how to create a draft file and then canonize it.
  `loom init` now writes an author's orientation in place of the contract page. It opens with what the four directories hold — `drafting/` (every document live), `canon/` (flat self-contained landmarks, written only by `canonize`, never scanned), `nodes/` (a convention, nothing inferred) and `refs/` at the quilt root, the seed space for reference PDFs and `.bib` files, created empty and never written by loom. Then, in bold, that a canon file is never edited, annotated, re-id'd, reformatted or deleted, only read and copied elsewhere. Then the loop in three sentences and a worked example that was actually run, with its real output. The contract page stays as the last section, because book 2.3 counts it as the one page an author needs and 4.4.4 and 4.6 require the README to state the root-relative compile and the Overleaf setting. Init substitutes the quilt's own directory names and id prefix as it writes, so the examples name this quilt. DR-190.
- [x] when a quilt is created, it asks for an author name if none is given and adds it to the quilt config.
  `loom init` asks once, after the prefix question, and writes the answer to `[author] name`. Under `--yes` or with no terminal it writes the key empty, and `--author NAME` (or an explicit `--author ""`) answers it without asking. DR-191, on top of the quilt-config author work below.
- [x] more tex improvements needed. See the mZK paper for instance, many package errors that don't work with mathjax, is my suspicion. 
  Measured by feeding every published formula through MathJax with the manifest's macros: 32 failures of 2,463 formulas in mZK and 3 of 3,753 in mmp, **0 in all five paper quilts afterwards** (acgs, man12 and relloc already had none). All four causes were loom's, and none was an author error.
  **Fixed.** (1) A comment inside a formula: loom published the raw source, and MathJax reads `%` to end of line as TeX does, so a commented-out line swallowed the `\end{align*}` after it, and a commented-out `\label` claimed the block's tag. Comments are blanked before math is published. (2) Commands a loaded package defines and MathJax lacks, now published as stand-ins the way `old-arrows` was: `bm`, `bbm`'s `\mathbbm`, amsmath's capitalised accents (`\Tilde`), `dsfont`, `esint`, `stmaryrd`, `nicefrac`; `mathtools` implies `amsmath`, and a document class implies what it loads, so an `amsart` paper that never names `amsmath` gets them too. (3) `\qedhere`, which means nothing to a viewer, is dropped from math. (4) `\DeclarePairedDelimiter` was never parsed, so `\ceil` would have been red; the declaration is published, so `\ceil*{}` and `\ceil[\big]{}` work as well.
  `\DeclarePairedDelimiterX` is still unparsed, and no demo quilt uses it. The `esint` integrals publish as Unicode glyphs, so they draw at text size rather than as large operators.
- [x] add reply to annotation and: clicked submit, when reclick on annotation could see a flickering in the spot it should appear and then disappeared, reclicking did nothing, arras appeared to freeze.
  Two causes, and the freeze was loom's. **Loom rebuilt once a second, for ever, after the first write:** every build rewrites `.loom/last-seen.json`, and the watcher had `.loom` among its roots since DR-174, so each rebuild triggered the next. The manifest's hash changed every second, so arras re-fetched the fragment and re-wired the comments every second — closing any open box and re-typesetting the page. That is the flicker, the vanishing box and the frozen feel, and it followed any edit, not just a reply. The watcher now ignores its own cache.
  **And in hover mode a click within the 120ms hover beat opened the box and the timer then closed it**: both paths called the same toggle. A click now cancels the timer, and the timer only opens. Third, smaller: a legitimate refresh destroyed the box the reply was written in, so the reply was never seen in place; the controller now reopens the comments that were open. Two write-mode e2e tests cover it, both failing before the fix.
- [x] more robust bib parsing for digests. loom should work when the bibliography is within the latex source file, all CANON documents should be scanned and their in-file bib entries added to a single bib file in the digest. So the new behavior: quilt maintains its OWN digest bib file. running loom digest scan will scan for new entries to be added to it from canon documents; nothing is ever removed. It is used as the "seed" along with the pdf references, the seed is no longer the bib file maintained by the author
  Built as discussed, under `loom refs scan` (DR-176 put the reference layer there), writing `digests/bibliography.bib`. It reads every canon document: the `.bib` files it names, copied verbatim, and its inline `\bibitem`s. It runs by itself at `import`, `canonize` and the start of `refs build`, and that file is now the only bibliography loom reads — resolving, fetching, extracting, `refs ingest` and the manifest all seed from it. An author's `.bib` reaches it through a canon document that names it.
  **A `\bibitem` becomes an entry** holding the item's own text in `loom-text`, the identifiers found in it (arXiv, DOI, MR, zbMATH), and a heuristic author, title and year marked `loom-parsed`. Two conventions are read: the title in italics, and the plain `K. Behrend, B. Fantechi, The intrinsic normal cone. Invent. Math. …` of a hand-written list. **Loom only appends** — an entry is never rewritten or removed, so a correction by hand survives every later scan, and two canon documents disagreeing keep the first and say so.
  **What it recovered:** 208 entries in four quilts that had none at all — kpsv 38, mmp 75, man12 27, acgs 46, every one with an author and a title, 26 of 27 in man12 — and relloc's 22 copied verbatim as before. Those papers now show references in the viewer and have something for `refs build` to resolve and fetch. A quilt with no canon document has an empty bibliography and the scan says so. DR-188; book 8.15, 4.1, 4.2.
- [x] author name error, should look in the config file for the QUILT not in the global config. If no author name is passed on loom init then it should have an empty name field in the config.toml waiting to accept it.
  `[author] name` is now a key in the quilt's own `config.toml`, read before the user config and git, and `loom init` writes it — `--author`'s value, or empty. A quilt that states the table answers for itself: an empty name refuses and names that file, and never falls through to `git config user.name`, so a record cannot be signed by whichever machine or agent shell ran the command (DR-185 found an agent's shell shares the author's git identity). A quilt with no `[author]` table falls back as before. DR-189; book 4.2, 4.3.
- [x] loom is a little slow and could be optimized. Look for 5 potential optimizations and perform the 3 which are easiest to implement.
  Measured first. Most commands were already fast on relloc (warm `build` 0.34s, `build --force` 0.65s, `status` and `lint` 0.20s, `--help` 0.11s). The slow path is a first build with no SVG cache: man12 took 56s.
  **Taken.** (1) Canon documents were rendered one at a time after the node pool finished, which was 48s of man12's 56s. They now go into the same pool, submitted first because each is one long job, so **a cold man12 build went from 56s to 32s**. (2) Every fragment's cache key re-hashed every master's whole `.aux` table and citation labels, 42ms per build on relloc. Those bytes are now computed once per build, and the keys come out identical. (3) `publish` rewrote all 325 files on every build, including cached fragments it had just read back (95ms). Skipped fragments are now left in place, and a file whose bytes are already on disk is not written again. A warm relloc build now takes 0.30s. relloc and acgs builds are byte-identical before and after.
  **Not taken:** `_code_hash` reads every loom source file once per process (27ms, and only in a development checkout), and startup imports every subcommand module up front (93ms of `--help`), where lazy loading is more invasive. Also found and not fixed: `dvisvgm` writes different bytes on each run and two threads can compile the same diagram at once, so a cold build is not reproducible byte for byte.
- [x] the viewer could generally be sped up and optimized. Many things feel a little slow. Clicking buttons in the settings is laggy. Test out 6 potential optimizations, implement the top 3.
  A second round, measured on relloc's main-atomic (43,865 elements, 1,249 formulas) as click-to-paint time. The three changes from the first round were not proposed again.
  **Taken.** (1) Formulas off screen are skipped when painting. The existing rule only caught display formulas, because inline math is an inline box. (2) The Comments setting no longer re-runs the fragment's mount. Before, each change wired the page a second time, ran 27 MathJax passes over every formula and jumped back to the URL's anchor. (3) A hover preview fetches its fragment during the 300ms delay, so the body appears 2–3ms after the card instead of about 15ms. **Settings clicks, before → after:** Size 112–128 → 64ms, Face 88 → 56, Theme 64 → 48, Format b1/b2/p1 104/88/120 → 80/48/88, Comments 24–40 → 16–24 with no re-typesetting. Format b1/p1 is still around 80ms, because it changes custom properties on the root and every element restyles.
  **Not taken:** skipping off-screen paragraphs (the page changes height by 1,481px, which breaks in-page jumps); making inline math `inline-block` (the biggest style saving, but it changes line breaks); dropping MathJax's hidden MathML (every formula would leave the accessibility tree). A known gap remains: the existing `content-visibility` rule on display formulas already hides 88 off-screen formulas from screen readers. The change is in arras but not yet vendored into loom, so `loom serve` shows it after the next bundle copy.
- [x] another possible latex rendering error: is "\longhookrightarrow" a real latex error or is it a parsing error in rl-0011/proof?
  Neither: `\longhookrightarrow` is real LaTeX, defined by the `old-arrows` package the preamble loads, and loom parsed it correctly. MathJax simply has no such command (nor the `\lhook\joinrel` it is built from), so it drew the name in error red. Loom now keeps a small table of commands a loaded package defines and the renderer lacks (`PACKAGE_COMMANDS` in `scan/macros.py`), and publishes a stand-in for each when the preamble loads its package: `\longhookrightarrow` and `\longhookleftarrow` become `\xhookrightarrow{}` and `\xhookleftarrow{}`. The rl-0011 formula typesets cleanly in MathJax with it; a document that does not load `old-arrows` gets nothing, since the command would not be LaTeX there either.
- [x] links are ugly, they use the full citation slug rather than the abbreviation. [graber-pandharipande_LocalizationVirtualClasses1999] should be rendered as [GP99] instead.
  A citation now reads as the compiled paper prints it: `[GP99]`, `[GP99, Theorem 1]`. Loom reads the label from the master's compile rather than computing one — biblatex's `labelalpha` (plus `extraalpha` as a letter) from the `.bbl`, BibTeX's `\bibcite` from the `.aux` (so man12 reads `[3]` and ACGS `[ACGS]`) — and `data-citekey` still carries the key, so previews and digest links are unchanged. Before a first compile, or under a numeric biblatex style that records no label, it shows the key rather than a guess. Book 9 and `specs/dialect.md` say so; a rebuild of relloc shows `[GP99]` in rl-001F.
- [x] the current "paper" view should be "blog1" and the current "blog" view should be "blog2". The new paper view should be AS FAITHFUL TO THE COMPILED VIEW OF THE PDF AS POSSIBLE.
  Already done yesterday in DR-175 (arras eb10a4c); verified the Format row today. It has four settings, named by letters so the row fits: `b1` is the old paper column and `b2` the old blog setting; `p1`, the default, is the compiled page (4.6in justified, hyphenated text block, indented unspaced paragraphs, centred small-caps section heads, run-in theorem heads, no colour or rule on an environment), and `p2` is the same with the real PDF's page boundaries taken from the `.aux` page numbers. Showing the PDF itself was refused because annotations, `\ref`s and previews need text to anchor to.
- [x] add a third view option to annotations: hover. Annotations open as expandable hover boxes, clicking outside of the box hides then, they can overlap the text or the gutter.
  A third value on the Comments setting, beside margin and inline. It is the same `AnnotationBox` and the same controller — the three differ only in where the box goes — so a hover box carries the quote, the severity, the replies and the typeset mathematics exactly as the other two do. The pointer opens it after 120ms, so passing over a line of marked text does not flash a box per mark; a click opens it too, and a click away or Escape closes it. Fixed to the viewport and appended inside the fragment rather than the page's root, because arras is a guest that writes only in its own subtree; a scroll closes it rather than letting it drift.
- [x] fix latex rendering in annotations, currently it fails. Take screenshots, diagnose, fix. Also notice the text size is small and hard to read in the annotations. Match text size to the size of the surrounding text, dependent on the size chosen in settings.
  Two causes, one in each tool. **Nothing ever typeset an annotation body**: fragments go through `Fragment.svelte` and titles through `Tex.svelte`, and the three places that show a published body — an annotation, a reply, a thread message — all did `{@html body_html}` and never called MathJax, so every `$…$` arrived as literal `\(n \le 2\)`. They share `math/Prose.svelte` now. And loom put display math inside the paragraph's `<p>`: an equation written on a soft line break between two sentences, which is how annotations are actually written, produced a `div` nested in a `p` that the browser repairs by closing the paragraph early. Every equation is lifted out of its paragraph, splitting it. The box takes `calc(var(--body-size) * 0.92)` instead of a fixed 11px, so it follows the size the reader chose.
- [x] where is the toggle for page render style, blog vs paper? This should be a row in settings.
  It is already there: the **Format** row, between Theme and Comments. Verified against the built viewer.
- [x] more latex processing errors: rl-000Y/proof fails for the following display math environment: "\begin{align*}S_{(j_1, \mathfrak E_1)}(\beta) &= e_T(E^0_1) \cap S_{C_{E^1_1}}(p'^*_1(\beta)) \tag{Equation \text{(9)}}\\ &= e_T(E^0_1) \cap e_T(E^0_2) \cap S_{C}(r_1^*p'^*_1(\beta)) \tag{Lemma~\text{3.11}}\\ &= e_T(E^0_2) \cap e_T(E^0_1) \cap S_{C}(r_2^*p'^*_2(\beta)) \tag{top square commutativity}\\ &= e_T(E^0_2) \cap S_{C_{E^1_2}}(p'^*_2(\beta)) \tag{Lemma~\text{3.11}}\\ &= S_{(j_2, \mathfrak E_2)}(\beta), \tag{Equation \text{(9)}}\end{align*}"
  `\text is only supported in math mode`, from MathJax. The author wrote `\tag{Equation \eqref{…}}` and `\tag{Lemma~\ref{…}}`; loom substitutes a reference for its number and wraps it in `\text{…}` to keep it upright, and a `\tag` argument is **already** text mode, so the wrapper is an error there and MathJax refuses the whole formula — which is why the aligned chain published as its own source on a yellow ground. A reference inside a text-mode argument (`\tag`, `\text`, `\mbox`, `\intertext` and their kin, brace-matched because they nest) is now substituted bare; everywhere else it keeps the wrapper.
- [x] the view for canon documents is broken. Investigate, take screenshots of the read view, diagnose, fix it.
  The read view opened on its own preamble: four kilobytes of `\renewcommand{\Ab}{\operatorname{Ab}}…` set as the document's first paragraph. A landmark renders with its own macro set, and arras prepended that set to the first formula on the page — **outside** its `\(…\)`, where MathJax reads it as prose rather than executing it. So the definitions showed as text and defined nothing. They now go inside the delimiter: 1,249 formulas on that document, of which exactly one was failing, and it was the first.
- [x] text size: small is too small, discard. The current Medium size should be S, the current L size should be M, and there should be a third size L larger than any current sizes.
  The scale moved up one step: S is 13.5px (the old M), M is 16px (the old L), and L is a new 19px. The old 11px is gone. Three names, three sizes, and a stored preference keeps its letter.
- [x] the document selection dropdown should display FILENAMES (draft1.tex) rather than tex titles (Relative Virtual Localization)
  The picker names both groups by file now. Two drafts of one paper share a title and differ only in their path, which is what you type and what `--master` and the read view's URL name them by.
- [x] the viewer could generally be sped up and optimized. Many things feel a little slow. Clicking buttons in the settings is laggy. Test out 6 potential optimizations, implement the top 3.
  Measured on the relloc paper: 160 nodes, 1,254 formulas, 44,000 elements. Six candidates, measured before choosing.
  **Taken.** (1) Typesetting batches from 160 formulas to 48 — one pass was a 90–100ms stall and a first screen holds about 25. (2) `content-visibility: auto` on the read view's environments, so the browser stops laying out and painting the 44,000 elements nobody is looking at. (3) Changing where comments stand re-keyed the fragment, which re-rendered its HTML and re-typeset every formula in it; the marks read their options live now, so the slots are taken out and put back the other way instead. **That one click went from 787ms to 45ms**, and long tasks during a read-view load went from eight of 50–100ms to one of 51ms.
  **Not taken, and why:** the manifest is 0.5MB and parses in 1.4ms; client-side navigation dispatches in 2–4ms; the cold load is 18ms of DOM work with the assets an afterthought. None of the three is what feels slow — typesetting was, and the first two changes are about not doing it in one bite.
- [x] a toggle in settings for "paper" or "blog" format display, applied globally so that it affects the read display as well as the display of nodes.
  One switch on the root element, so the read view and a node's own page change together: a result should not read as a different kind of thing depending on which page it is standing on. Paper is the measured, numbered column; blog is wider, with sans headings, environments carried by a tinted panel in their taxon's colour rather than by a hairline accent, and no theorem numbers -- a paper points at a result by number and a website points at it by name.
- [x] color coding in the graph view: currently no color coding at all. should match the color of the taxons, and there should be one place to set these colors.
  Graph nodes were coloured by state, and since nearly everything is `draft` that read as no coding at all. They take the taxon's colour now, and it is the same colour the accent down an environment's left edge uses. One palette, declared once in `theme.css`; `$lib/taxonomy` decides which token a taxon gets and never knows a value. Assigned by the manifest's own `style` and the taxon's position rather than by name, so a corpus whose taxa are "Widget" and "Gadget" is coloured like one using Bourbaki's. Fill is the taxon and the ring is still the state: what a node is does not change while you work, and how it stands does.
- still some latex errors in the ACGS quilt, I suspect it's from bad parsing of style files or preambles. For example: "generator of the -th factor in the universal base log structure \Spec \big(\bigoplus_{\text{nodes of \ul C}} \NN\arr \kk\big). Our choice" in "acgs-002P/proof". This is likely due to text being lost after an accent: $q$ in the \'etale topology. Then there exist unique lifts $s_x,…$ is published as "in the é" followed directly by the next formula. Everything between the accent and the formula is gone. This is a bug in loom's HTML conversion, not in the preamble. Also possibly due to the presence of a macros inside \text{}. The formula containing \text{nodes of \ul C} shows up as raw TeX. \ul is defined in drafts/main.tex and does reach arras. My guess is that MathJax doesn't expand author macros inside \text{}
- add the option for expandable annotation/comment boxes. Comment boxes and annotations no longer are restricted to live in the right gutter, they appear as color coded highlights on the source text. Clicking them expands them.
- the graph view needs work. the layered view is completely broken for instance, and the nodes shouldn't be draggable in this view. see the broken-graph.png screenshot on desktop for an example of the broken view in a zoom.
- it would be nice to have a local graph view that you can open up which changes based on where you are in the draft. Something like the graph viewer in quartz5 -- maybe it's even portable from that project (https://quartz.jzhao.xyz/).
- links in the references should be clickable
- the accepted, stale, incomplete, and errors pages are bizarre, the incomplete page in particular -- it's called "blockers", it has a "views" panel on the side replicating the strip.
- the tabs view should be retired, keep strip and rail
- clicking anywhere outside of the settings box when the box is expanded should close the box, same behavior for other floating boxes.
- it'd be nice to have something similar to the "hover to preview" functionality that exists in my sitegen. For example, if worked-examples or counter examples or stress tests of even other nodes linked to or are linked from a node I'm viewing, it'd be nice to hover over the links/backlinks to get a preview.
- the audit requires [uses-ledger], the report should include it, and thus it should be returned to the audit mode