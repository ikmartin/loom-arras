# Design mockups

Every `mockup-*.png` here is a **drawing of a design that had not been built yet**, made during the conversation that produced `../split-view-reading-view-overhaul.md` and plan `../../plans/0.13-plan-reading-layer.md` on 2026-09-20. None of them is a screenshot of loom or arras. They are static HTML rendered headlessly from the design canvases, so a control in one of them does nothing and a number in one of them is invented.

**What the interface actually looks like once it is built belongs in the report's own directory**, `../../reports/0.13-reading-layer/`, the way 0.12 keeps its figures. Nothing in here is ever overwritten with a photograph of the real thing: a mockup and a record of what shipped answer different questions, and a file that quietly becomes the other one is how a document starts lying.

| File | What it shows | Decided in |
|---|---|---|
| `mockup-split-view.png` | the frame: rail, side panel, content, divider, discussion, composer | item 7 |
| `mockup-side-panel.png` | Documents with the Library group, Nodes, Contents, Sessions, the collapse control, `⚗` | item 3b |
| `mockup-session-selector-b-c.png` | the session selector, variant B+C: write target, showing, grouping, manage, `3 new` | item 3a |
| `mockup-delete-session.png` | the delete confirmation, with the tombstone wording | item 3a |
| `mockup-placement-floating.png` | an annotation as a floating box, crossing into the margin, 4px off the edge | item 9 |
| `mockup-placement-margin.png` | the margin column, several boxes open, one brought to front | item 9 |
| `mockup-placement-inline.png` | the box in the flow, Authoring View only | item 9 |

The canvases they came from, which also hold the variants that were rejected — the compact selector, the side sheet, ticks alone:

- session selector variants: https://claude.ai/artifact/XAiUMfLA1xFkNWMS49bbKA
- annotations in the content pane, and the split view: https://claude.ai/artifact/25s6dgcGEBRVY9iEjupKEL
