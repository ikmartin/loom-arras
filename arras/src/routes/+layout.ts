// Single-page application: no server rendering, no prerender in this build. The prerender build (M2) flips these from the manifest's route list.
export const ssr = false;
export const prerender = false;
