---
description: Apply the loom referee mode to a key of this quilt
argument-hint: KEY
---

Apply `ai/modes/referee.md` to `$ARGUMENTS`. `loom ai runs` lists the open sessions and `loom ai start "a name"` opens one and makes it active; writing lands there, and `--session` names another. Follow the mode file exactly; its checklist is the output contract.
